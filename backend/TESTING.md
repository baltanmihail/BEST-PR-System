# 🧪 Инструкция по тестированию

## Быстрый старт

### 1. Запуск API сервера

```bash
cd backend
python run.py
```

API будет доступен на: `http://localhost:8000`

### 2. Проверка API

#### Тестовый endpoint:
```bash
curl http://localhost:8000/test
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "API работает!",
  "endpoints": {
    "docs": "/docs",
    "health": "/health",
    "api_prefix": "/api/v1"
  }
}
```

#### Health check:
```bash
curl http://localhost:8000/health
```

#### Документация API:
Откройте в браузере: `http://localhost:8000/docs`

### 3. Запуск с Telegram ботом

```bash
cd backend
python run_with_bot.py
```

Это запустит:
- FastAPI сервер (порт 8000)
- Telegram бота параллельно

**ВАЖНО:** Убедитесь, что установлена переменная окружения `TELEGRAM_BOT_TOKEN`

### 4. Тестирование Telegram бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start` - должна произойти регистрация
3. Отправьте `/tasks` - список задач (может быть пустым)
4. Отправьте `/stats` - ваша статистика
5. Отправьте `/help` - справка по командам

### 5. Проверка фронтенда

1. Убедитесь, что API запущен на `http://localhost:8000`
2. Запустите фронтенд:
```bash
cd frontend
npm run dev
```

3. Откройте `http://localhost:5173`
4. Проверьте, что данные загружаются (может быть ошибка аутентификации, это нормально)

## 🔧 Переменные окружения

Создайте `.env` файл в `backend/`:

```env
DATABASE_URL=sqlite:///./best_pr_system.db
SECRET_KEY=your-secret-key-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ADMIN_IDS=5079636941
ENVIRONMENT=development
LOG_LEVEL=INFO
API_URL=http://localhost:8000
```

## 🐛 Решение проблем

### Ошибка "TELEGRAM_BOT_TOKEN не установлен"
- Проверьте, что токен установлен в `.env` или переменных окружения
- Получите токен от [@BotFather](https://t.me/BotFather)

### Ошибка подключения к API в боте
- Проверьте, что API запущен (`http://localhost:8000/test`)
- Убедитесь, что `API_URL` правильно настроен

### Фронтенд не подключается к API
- Проверьте `VITE_API_URL` в `.env` фронтенда
- Убедитесь, что CORS настроен правильно в `app/main.py`

### Ошибки миграций
```bash
cd backend
python -m alembic upgrade head
```

## 📝 Следующие шаги

1. ✅ Проверить работу API endpoints
2. ✅ Протестировать Telegram бота
3. ✅ Подключить фронтенд к API
4. ⏳ Создать миграции Alembic
5. ⏳ Добавить endpoint для геймификации (leaderboard)
6. ⏳ Протестировать создание задач с этапами
