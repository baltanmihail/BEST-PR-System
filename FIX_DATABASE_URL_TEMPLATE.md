# 🔧 Исправление: DATABASE_URL содержит шаблон ${{...}}

## ❌ Проблема

В Railway Variables у вас указан **шаблон URL** вместо **реального URL**:

```
postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}
```

Это **шаблон с переменными**, а не реальный URL! Railway не подставляет эти переменные автоматически в наш сервис.

---

## ✅ Решение (ШАГ ЗА ШАГОМ)

### Шаг 1: Получить реальный DATABASE_URL

**Способ 1: Через Connection Info (рекомендуется)**

1. Откройте Railway Dashboard
2. Откройте ваш проект
3. Найдите вашу **PostgreSQL базу данных**
4. Нажмите на неё
5. Перейдите во вкладку **"Connect"** или **"Connection Info"**
6. Там будет показан **реальный URL** с реальными значениями
7. Скопируйте его - будет что-то вроде:
   ```
   postgresql://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
   ```

**Способ 2: Через Variables (если есть реальный URL)**

1. В Railway Dashboard откройте вашу базу данных
2. Перейдите во вкладку **"Variables"**
3. Найдите `DATABASE_URL`
4. Railway может показывать **два варианта**:
   - Шаблон: `postgresql://${{PGUSER}}:...` - **НЕ используйте!**
   - Реальный URL: `postgresql://postgres:abc123@host:5432/railway` - **используйте этот!**
5. Если видите только шаблон, используйте Способ 1

**Способ 3: Через Railway CLI (если установлен)**

```powershell
railway variables --service <ваша-база-данных>
```

---

### Шаг 2: Изменить URL для async

**ВАЖНО**: Замените `postgresql://` на `postgresql+asyncpg://`

**Пример:**
```
Было:  postgresql://postgres:abc123@containers-us-west-123.railway.app:5432/railway
Стало: postgresql+asyncpg://postgres:abc123@containers-us-west-123.railway.app:5432/railway
```

---

### Шаг 3: Обновить переменную в вашем сервисе

1. В Railway Dashboard откройте ваш **сервис (Backend)** (не базу данных!)
2. Перейдите во вкладку **"Variables"**
3. Найдите переменную `DATABASE_URL`
4. Нажмите на неё для редактирования
5. **Удалите** шаблон с `${{...}}`
6. **Вставьте** реальный URL из Шага 1 (с `postgresql+asyncpg://`)
7. Нажмите **"Save"**

---

## 🔍 Как отличить шаблон от реального URL

### ❌ ШАБЛОН (неправильно):
```
postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}
```
- Содержит `${{...}}`
- Это переменные Railway, которые нужно подставлять вручную

### ✅ РЕАЛЬНЫЙ URL (правильно):
```
postgresql://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
```
- Содержит реальные значения
- Нет `${{...}}`
- Есть реальный хост (например, `containers-us-west-123.railway.app`)
- Есть реальный пароль (например, `abc123def456`)

---

## 📝 Пример правильного DATABASE_URL

После всех шагов у вас должно быть:

```
postgresql+asyncpg://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
```

Где:
- `postgres` - реальный пользователь
- `abc123def456` - реальный пароль (из Railway)
- `containers-us-west-123.railway.app` - реальный хост (из Railway)
- `5432` - порт (число!)
- `railway` - имя базы данных

---

## ✅ После исправления

1. Railway автоматически пересоберёт проект
2. Подождите 2-3 минуты
3. Проверьте логи: Railway Dashboard → Deployments → Logs
4. Не должно быть ошибок подключения

---

## 🆘 Если не можете найти реальный URL

1. **Попробуйте вкладку "Connect"** в базе данных - там обычно показывается реальный URL
2. **Или используйте Railway CLI** для получения переменных
3. **Или создайте новую базу данных** и сразу скопируйте URL из Connection Info

---

## 💡 Важно

**НИКОГДА не используйте шаблоны с `${{...}}` в DATABASE_URL для вашего сервиса!** 
Всегда используйте реальный URL с реальными значениями.
