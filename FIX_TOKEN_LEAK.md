# 🚨 КРИТИЧНО: Токен попал в Git!

## ⚠️ Проблема

GitHub обнаружил, что токен Telegram бота попал в историю Git. Это **опасно**, так как любой может увидеть токен и использовать его.

## ✅ Что нужно сделать СРОЧНО

### Шаг 1: Отозвать старый токен

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/revoke`
3. Выберите вашего бота: `@BESTPRSystemBot`
4. BotFather отправит **новый токен**

### Шаг 2: Удалить токен из Git истории

**ВАЖНО**: Это удалит токен из всей истории Git. Если репозиторий уже на GitHub, нужно будет сделать force push.

```powershell
# 1. Установите git-filter-repo (если нет)
pip install git-filter-repo

# 2. Удалите токен из истории
git filter-repo --path-glob 'docs/RAILWAY*.md' --invert-paths

# ИЛИ если git-filter-repo не работает, используйте BFG:
# java -jar bfg.jar --replace-text passwords.txt

# 3. Force push (ОСТОРОЖНО! Это перезапишет историю)
git push origin --force --all
```

**Альтернативный способ (проще):**

```powershell
# 1. Удалите файлы с токеном из последнего коммита
git rm --cached docs/RAILWAY_SETUP_SIMPLE.md docs/RAILWAY_VARIABLES.md

# 2. Исправьте файлы (токен уже удалён)
git add docs/RAILWAY_SETUP_SIMPLE.md docs/RAILWAY_VARIABLES.md

# 3. Создайте новый коммит
git commit -m "fix: Remove token from documentation"

# 4. Force push
git push origin --force
```

### Шаг 3: Обновить токен в Railway

1. Откройте Railway Dashboard
2. Перейдите в Variables
3. Найдите `TELEGRAM_BOT_TOKEN`
4. Замените на **новый токен** из BotFather

### Шаг 4: Проверить .gitignore

Убедитесь, что `.gitignore` содержит:
```
*.md
!README.md
!docs/*.md
```

Или добавьте конкретные файлы:
```
docs/RAILWAY_SETUP_SIMPLE.md
docs/RAILWAY_VARIABLES.md
```

---

## 🔒 Предотвращение в будущем

1. **Никогда не коммитьте токены** в файлы
2. Используйте **переменные окружения** или **Railway Variables**
3. Используйте **git-secrets** для проверки перед коммитом:
   ```powershell
   git secrets --install
   git secrets --register-aws
   ```

---

## 📝 После исправления

1. ✅ Токен отозван в BotFather
2. ✅ Новый токен получен
3. ✅ Токен удалён из Git истории
4. ✅ Новый токен добавлен в Railway
5. ✅ .gitignore обновлён

---

## 🆘 Если не можете удалить из истории

Если репозиторий публичный и токен уже виден:

1. **СРОЧНО отзовите токен** в BotFather
2. Создайте **новый токен**
3. Используйте новый токен в Railway
4. Старый токен больше не работает

**Важно**: Даже если не удалите из истории, новый токен будет безопасным.
