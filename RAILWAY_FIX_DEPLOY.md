# 🔧 Исправление ошибки деплоя Railway

## ❌ Проблема

```
/bin/bash: line 1: pip: command not found
```

Railway не может найти Python/pip.

## ✅ Решение

### Что было исправлено:

1. **Создан `requirements.txt` в корне** - Railway использует его для определения Python проекта
2. **Обновлён `railway.json`** - исправлен путь к requirements.txt
3. **Создан `nixpacks.toml`** - правильная конфигурация для Railway

### Файлы для Railway:

- ✅ `requirements.txt` (в корне) - для определения Python проекта
- ✅ `backend/requirements.txt` - реальные зависимости
- ✅ `nixpacks.toml` - конфигурация сборки
- ✅ `railway.json` - конфигурация Railway
- ✅ `Procfile` - команда запуска
- ✅ `runtime.txt` - версия Python

---

## 🚀 Что делать сейчас

### 1. Закоммитьте изменения:

```powershell
cd "BEST PR System"
git add .
git commit -m "fix: Add requirements.txt and fix Railway build config"
git push
```

### 2. Railway автоматически пересоберёт проект

---

## 📝 Про переменные (8 vs 12)

Вы добавили **8 переменных** - это правильно для начала!

**Минимально необходимые (8):**
1. DATABASE_URL
2. SECRET_KEY
3. TELEGRAM_BOT_TOKEN
4. TELEGRAM_ADMIN_IDS
5. GOOGLE_SHEETS_ID
6. ENVIRONMENT
7. LOG_LEVEL
8. ACCESS_TOKEN_EXPIRE_MINUTES (у вас как shared variable)

**Дополнительные (если нужны):**
9. GOOGLE_CREDENTIALS_1_JSON
10. GOOGLE_CREDENTIALS_2_JSON
11. GOOGLE_CREDENTIALS_3_JSON
12. GOOGLE_CREDENTIALS_4_JSON
13. GOOGLE_CREDENTIALS_5_JSON
14. GOOGLE_DRIVE_FOLDER_ID
15. CORS_ORIGINS
16. API_V1_PREFIX

**Для MVP достаточно 8 переменных!** Google credentials можно добавить позже, когда будете настраивать интеграцию.

---

## ✅ Проверка после деплоя

1. Откройте Railway Dashboard → Deployments
2. Проверьте логи - не должно быть ошибок
3. Откройте ваш домен: `https://your-app.railway.app/health`
4. Должно вернуть: `{"status": "ok"}`

---

## 🐛 Если всё ещё не работает

### Проверьте логи в Railway:

1. Railway Dashboard → Deployments → Logs
2. Найдите ошибку
3. Проверьте:
   - Все ли переменные добавлены?
   - Правильный ли DATABASE_URL? (должен начинаться с `postgresql+asyncpg://`)
