# Настройки фронтенда BEST PR System

Этот файл содержит описание всех настроек и параметров, которые можно изменить для кастомизации внешнего вида и поведения фронтенда.

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/          # React компоненты
│   │   ├── layout/          # Layout компоненты (Header, Sidebar, Layout)
│   │   ├── Background3DModels.tsx  # 3D модели логотипов BEST на фоне
│   │   └── StaticCursor3D.tsx      # 3D курсор в правом нижнем углу
│   ├── pages/               # Страницы приложения
│   ├── store/               # Zustand stores (тема, авторизация)
│   ├── hooks/               # React хуки
│   └── index.css            # Глобальные стили (glassmorphism, темы)
└── public/                  # Статические файлы (логотипы, 3D модели)
```

---

## 🎨 Настройки темы

### Файл: `src/store/themeStore.ts`

**Текущая тема:** Сохраняется в `localStorage` под ключом `best-pr-theme`

**Доступные темы:**
- `'dark'` - Тёмная тема (по умолчанию)
- `'light'` - Светлая тема

**Как изменить тему по умолчанию:**
```typescript
export const useThemeStore = create<ThemeStore>((set) => ({
  theme: (localStorage.getItem('best-pr-theme') as Theme) || 'dark', // Измените 'dark' на 'light'
  // ...
}))
```

---

## 🌈 Настройки цветов и градиентов

### Файл: `src/index.css`

#### Тёмная тема - Градиент фона (`.bg-gradient-best.dark`)

**Позиции цветов:**
- **Центр:** Голубой (`rgba(30, 136, 229, ...)`)
- **Слева (20% 50%):** Зелёный (`rgba(67, 160, 71, ...)`)
- **Справа снизу (80% 70%):** Жёлтый (`rgba(255, 179, 0, ...)`)
- **Снизу (50% 90%):** Жёлтый (`rgba(255, 179, 0, ...)`)

**Как изменить:**
```css
.bg-gradient-best.dark {
  background: 
    radial-gradient(ellipse at center, rgba(30, 136, 229, 0.6) 0%, ...),
    /* Измените цвета и позиции здесь */
}
```

#### Светлая тема - Градиент фона (`.bg-gradient-best.light`)

**Аналогичная структура, но с более приглушёнными цветами.**

---

## 🔲 Glassmorphism эффекты

### Файл: `src/index.css`

#### Основные параметры:

**`.glass-enhanced.dark`** и **`.glass-enhanced.light`**:
- `backdrop-filter: blur(80px)` - Сила размытия (измените `80px` на другое значение)
- `saturate(180%)` - Насыщенность цветов (измените `180%`)
- `background: rgba(255, 255, 255, 0.08)` - Прозрачность панели (измените `0.08`)
- `border: 1px solid rgba(255, 255, 255, 0.2)` - Прозрачность границы

**Пример изменения:**
```css
.glass-enhanced.dark {
  backdrop-filter: blur(100px) saturate(200%); /* Более сильный blur */
  background: rgba(255, 255, 255, 0.12); /* Более непрозрачная панель */
}
```

---

## 🎯 3D Модели на фоне

### Файл: `src/components/Background3DModels.tsx`

#### Конфигурация моделей (строка ~50):

```typescript
const getModelsConfig = (): ModelConfig[] => [
  {
    path: '/BEST.glb',              // Путь к модели
    position: [-500, -200, -400],   // Позиция [X, Y, Z]
    scale: 10.0,                     // Масштаб модели
    rotation: [0, 0, 0],            // Вращение [X, Y, Z] в радианах
    floatSpeed: 0.0008,              // Скорость парения
    floatAmplitude: 50,              // Амплитуда парения (высота)
  },
  // ... ещё 2 модели
]
```

#### Дополнительный масштаб (строка ~150):

```typescript
const additionalScale = 5.0 // Умножается на scale из конфига
```

**Как изменить размер моделей:**
1. Измените `scale` в `getModelsConfig()` (например, с `10.0` на `15.0`)
2. Или измените `additionalScale` (например, с `5.0` на `7.0`)

#### Видимость моделей (строка ~290):

```typescript
style={{ opacity: 0.8 }} // Измените от 0.0 до 1.0
```

#### Освещение (строка ~106):

```typescript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.6) // Яркость: 0.6
const directionalLight1 = new THREE.DirectionalLight(0x1e88e5, 0.8) // Голубой свет
const directionalLight2 = new THREE.DirectionalLight(0x43a047, 0.6) // Зелёный свет
const pointLight = new THREE.PointLight(0xffb300, 0.5) // Жёлтый свет
```

**Цвета освещения:**
- `0x1e88e5` - Голубой (BEST primary)
- `0x43a047` - Зелёный (BEST secondary)
- `0xffb300` - Жёлтый (BEST accent)

---

## 🖱️ 3D Курсор

### Файл: `src/components/StaticCursor3D.tsx`

#### Размер курсора (строка ~30):

```typescript
renderer.setSize(60, 60) // Размер рендерера (пиксели)
```

#### Размер контейнера (строка ~140):

```typescript
width: '120px',  // Ширина контейнера (увеличьте, если обрезается)
height: '120px', // Высота контейнера
```

#### Позиция курсора (строка ~135):

```typescript
bottom: '30px', // Отступ снизу
right: '30px',  // Отступ справа
```

#### Размер модели (строка ~59):

```typescript
model.scale.set(1.5, 1.5, 1.5) // Уменьшите для меньшего курсора
```

#### Анимация парения и нажатия (строка ~108):

```typescript
// Скорость парения
const floatY = Math.sin(time * 0.8) * 0.08 // Измените 0.8 (скорость) и 0.08 (амплитуда)

// Анимация нажатия
const pressIntensity = Math.sin(time * 1.2) * 0.15 + 0.15 // Измените 1.2 (скорость) и 0.15 (интенсивность)
const pressZ = -pressIntensity * 0.3 // Глубина нажатия (измените 0.3)
const pressRotX = -pressIntensity * 0.2 // Наклон при нажатии (измените 0.2)
```

---

## 🖼️ Плоский логотип в правом верхнем углу

### Файл: `src/components/layout/Layout.tsx` (строка ~68)

```typescript
<img 
  src="/BEST_Moscow_color_logo (это основные цвета).png" 
  className="h-16 w-auto opacity-80" // h-16 = высота 64px, измените на h-20 для большего
/>
```

**Позиция:**
```typescript
className="fixed top-4 right-4" // top-4 = 16px сверху, right-4 = 16px справа
```

**Эффекты:**
- `opacity-80` - Прозрачность (измените на `opacity-100` для полной непрозрачности)
- `drop-shadow-lg` - Тень
- `hover:opacity-100` - Эффект при наведении

---

## 📝 Текст

### Файл: `src/index.css`

#### Стили текста (`.text-readable`):

**Тёмная тема:**
```css
.text-readable.dark {
  color: white;
  text-shadow: 0 0 8px rgba(30, 136, 229, 0.8), ...;
  -webkit-text-stroke: 0.5px rgba(30, 136, 229, 0.3);
}
```

**Светлая тема:**
```css
.text-readable.light {
  /* Те же стили, что и в тёмной теме */
}
```

**Как изменить:**
- `text-shadow` - Тени и свечение текста
- `-webkit-text-stroke` - Обводка текста (измените `0.5px` на другое значение)

---

## 🎬 Анимации

### Параллакс при наведении

**Файл:** `src/hooks/useParallaxHover.ts`

**Параметры:**
```typescript
const parallax = useParallaxHover(8) // Интенсивность наклона (измените 8)
```

### Эффект скролла (стеклянный шлейф)

**Файл:** `src/components/layout/Layout.tsx` (строка ~20)

```typescript
const offset = Math.min(wheelVelocity * 0.015, 8) // Максимальное смещение (измените 8)
const blur = Math.min(wheelVelocity * 0.002, 1)   // Максимальный blur (измените 1)
```

---

## 🔧 Быстрые настройки

### Увеличить размер 3D моделей:
1. Откройте `src/components/Background3DModels.tsx`
2. Найдите `getModelsConfig()` (строка ~50)
3. Увеличьте значения `scale` (например, с `10.0` на `15.0`)
4. Или увеличьте `additionalScale` (строка ~150) с `5.0` на `7.0`

### Изменить прозрачность панелей:
1. Откройте `src/index.css`
2. Найдите `.glass-enhanced.dark` или `.glass-enhanced.light`
3. Измените `background: rgba(255, 255, 255, 0.08)` - последнее число (0.08) это прозрачность

### Изменить цвета фона:
1. Откройте `src/index.css`
2. Найдите `.bg-gradient-best.dark` или `.bg-gradient-best.light`
3. Измените цвета в `radial-gradient` (например, `rgba(30, 136, 229, 0.6)`)

### Убрать анимацию курсора:
1. Откройте `src/components/StaticCursor3D.tsx`
2. Найдите функцию `animate()` (строка ~108)
3. Закомментируйте строки с `floatY`, `pressZ`, `pressRotX`

---

## 📦 Статические файлы

### Расположение:
- `frontend/public/BEST.glb` - 3D модель логотипа BEST
- `frontend/public/3d_mouse_cursor.glb` - 3D модель курсора
- `frontend/public/BEST_Moscow_color_logo (это основные цвета).png` - Плоский логотип

**Важно:** После изменения файлов в `public/` может потребоваться перезапуск dev-сервера.

---

## 🐛 Отладка

### Проверка загрузки 3D моделей:
1. Откройте консоль браузера (F12)
2. Ищите сообщения:
   - `✅ Загружена модель 1: /BEST.glb` - Успешная загрузка
   - `⚠️ Модель не загрузилась` - Ошибка загрузки (будет использован fallback)
   - `📏 Модель 1 масштаб: 50` - Текущий масштаб модели

### Проверка темы:
1. В консоли: `localStorage.getItem('best-pr-theme')` - Текущая тема
2. Или используйте переключатель темы в Header (справа сверху)

---

## 💡 Советы

1. **Модели слишком большие/маленькие?** Измените `scale` в `getModelsConfig()` или `additionalScale`
2. **Панели слишком прозрачные?** Увеличьте последнее число в `rgba(255, 255, 255, 0.08)`
3. **Цвета слишком яркие?** Уменьшите значения в `radial-gradient` (например, `0.6` на `0.4`)
4. **Курсор обрезается?** Увеличьте `width` и `height` контейнера в `StaticCursor3D.tsx`

---

**Последнее обновление:** 2026-01-05
