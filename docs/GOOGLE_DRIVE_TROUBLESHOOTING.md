# 🔧 Решение проблем с Google Drive и Google Sheets

## ❌ Проблема 1: Ошибка enum task_type - "SMM" вместо "smm"

### Что произошло:
В базе данных enum `task_type` определён как `'smm'` (lowercase), но код пытался вставить `'SMM'` (uppercase).

### ✅ Решение:
Исправлено в модели `TaskTemplate` - добавлены `TaskTypeType` и `TaskPriorityType` TypeDecorator, которые автоматически конвертируют enum в lowercase значения.

**Статус:** ✅ ИСПРАВЛЕНО

---

## ❌ Проблема 2: Не создаются файлы таймлайна и другие файлы в Google Drive

### Ситуация:
1. ⚠️ **ОСНОВНАЯ ПРИЧИНА**: Квота Google Drive сервисного аккаунта **превышена** (каждый аккаунт даёт 15 ГБ)
2. ✅ Папки создаются успешно (почти не занимают места)
3. ❌ **Тяжелые файлы НЕ создаются**: Таблицы (Google Sheets), документы (Google Docs), файлы с контентом занимают место и падают с ошибкой `storageQuotaExceeded`
4. ❌ Дополнительно: У вас может не быть доступа к Shared Drive "LBG Drive" (drive@bmstu-best.ru)

### ✅ РЕШЕНИЕ: Работа без Shared Drive

Система **УЖЕ НАСТРОЕНА** для автоматической работы без Shared Drive! Если Shared Drive недоступен, система автоматически создаст папку в **корне Google Drive сервисного аккаунта**.

### 📋 Что нужно сделать:

#### Шаг 1: Очистите переменные окружения в Railway

В Railway Dashboard для backend сервиса:

```env
# ОЧИСТИТЕ или удалите эту переменную (оставьте ПУСТОЙ)
GOOGLE_DRIVE_FOLDER_ID=

# Опционально: укажите email для автоматического sharing файлов
GOOGLE_DRIVE_OWNER_EMAIL=baltanmihail@gmail.com
```

**Важно:**
- ❌ **НЕ указывайте** `GOOGLE_DRIVE_FOLDER_ID` с ID папки из Shared Drive (например, `1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI`)
- ✅ **Оставьте пустым** - система создаст папку автоматически
- ✅ Убедитесь, что `GOOGLE_CREDENTIALS_1_JSON` настроен правильно

#### Шаг 2: Деплой и проверка

После деплоя в логах Railway вы должны увидеть:

```
⚠️ ROOT_FOLDER_ID ... недоступен (возможно, нет доступа к Shared Drive)
📁 Создаём папку в корне Google Drive сервисного аккаунта вместо Shared Drive...
✅ Папка создана в корне Google Drive: [folder_id]
✅ Google Drive структура инициализирована
✅ Папка 'Tasks': [tasks_folder_id]
✅ Папка 'Gallery': [gallery_folder_id]
...
📊 Инициализация таблицы таймлайна задач...
✅ ID папки бота: [bot_folder_id]
📝 Создание таблицы в папке [bot_folder_id]...
✅ Таблица создана: [spreadsheet_id]
✅ Google таблица таймлайна задач готова: [spreadsheet_id]
```

### 🔍 Если файлы всё ещё не создаются:

#### Проверьте логи на эти ошибки:

1. **"CRITICAL SECURITY: TELEGRAM_BOT_TOKEN is not set"**
   - ❌ Проблема: Отсутствует токен бота
   - ✅ Решение: Установите `TELEGRAM_BOT_TOKEN` в переменные окружения

2. **"Google credentials не найдены"**
   - ❌ Проблема: Отсутствует или невалиден `GOOGLE_CREDENTIALS_1_JSON`
   - ✅ Решение: Проверьте, что JSON валиден и правильно установлен в Railway

3. **"storageQuotaExceeded"** ⚠️ **ОСНОВНАЯ ПРОБЛЕМА**
   - ❌ **Проблема**: Квота Google Drive сервисного аккаунта превышена (каждый аккаунт даёт 15 ГБ)
   - ✅ **Важно**: Папки создаются (почти не занимают места), но **тяжелые файлы** (таблицы, документы) **НЕ могут быть созданы**
   - 🔄 **Автоматическое решение**: Система теперь **автоматически пробует следующий credential** из списка при ошибке квоты. Если у вас настроены несколько credentials (`GOOGLE_CREDENTIALS_1_JSON`, `GOOGLE_CREDENTIALS_2_JSON` и т.д.), они будут использоваться по очереди.
   - 💡 **Решение вручную**: 
     - Освободите место в Google Drive сервисного аккаунта (удалите старые файлы)
     - **Или** создайте дополнительные сервисные аккаунты (каждый даёт 15 ГБ) и добавьте их credentials в `GOOGLE_CREDENTIALS_2_JSON`, `GOOGLE_CREDENTIALS_3_JSON` и т.д. (до 5 аккаунтов = 75 ГБ)
     - **Или** передайте ownership файлов пользователю через `GOOGLE_DRIVE_OWNER_EMAIL` (файлы будут использовать квоту пользователя, а не сервисного аккаунта)

4. **"permissionDenied" или "forbidden"**
   - ❌ Проблема: Сервисный аккаунт не имеет прав на создание файлов
   - ✅ Решение:
     - Проверьте, что сервисный аккаунт имеет доступ к Google Drive API
     - Убедитесь, что API включены в Google Cloud Console:
       - Google Drive API ✅
       - Google Sheets API ✅
       - Google Docs API ✅

5. **"invalid input value for enum task_type: 'SMM'"**
   - ❌ Проблема: Использование uppercase значения для enum
   - ✅ Решение: ИСПРАВЛЕНО в этом коммите - модель `TaskTemplate` теперь использует правильные TypeDecorator

### 📂 Где найти созданные файлы:

После успешного деплоя файлы будут созданы в **Google Drive сервисного аккаунта**.

**Как получить доступ к файлам:**

1. **Через ID папки из логов:**
   - Найдите в логах строку: `✅ Папка создана в корне Google Drive: [folder_id]`
   - Используйте этот `folder_id` для доступа

2. **Через API:**
   ```python
   from app.services.drive_structure import DriveStructureService
   
   service = DriveStructureService()
   folder_id = service.get_bot_folder_id()
   print(f"https://drive.google.com/drive/folders/{folder_id}")
   ```

3. **Автоматический sharing:**
   - Если указан `GOOGLE_DRIVE_OWNER_EMAIL`, файлы автоматически поделятся с этим email
   - Пользователь получит права "Редактор" (Editor)

### 🔐 Важные моменты:

1. **Квота хранилища:**
   - Каждый сервисный аккаунт даёт **15 ГБ** хранилища
   - Для увеличения используйте ротацию credentials (до 5 аккаунтов = 75 ГБ)

2. **Ownership файлов:**
   - В обычном Google Drive ownership нельзя передать между разными доменами
   - Файлы останутся собственностью сервисного аккаунта
   - Но можно настроить автоматический sharing с нужными пользователями

3. **Старые файлы в Shared Drive:**
   - Если у вас нет доступа к Shared Drive, вы не можете удалить старые файлы
   - Новые файлы будут создаваться в Google Drive сервисного аккаунта
   - Для удаления старых файлов обратитесь к администратору Shared Drive

### 📖 Подробные инструкции:

- **Для работы БЕЗ Shared Drive:** [`docs/NO_SHARED_DRIVE_SETUP.md`](./NO_SHARED_DRIVE_SETUP.md)
- **Для работы С Shared Drive:** [`docs/SHARED_DRIVE_SETUP.md`](./SHARED_DRIVE_SETUP.md)
- **Общая инструкция по деплою:** [`docs/DEPLOY.md`](./DEPLOY.md)

---

## ✅ Чек-лист проверки после деплоя:

- [ ] В логах есть: `✅ Google Drive структура инициализирована`
- [ ] В логах есть: `✅ Папка создана в корне Google Drive: [folder_id]`
- [ ] В логах есть: `✅ Google таблица таймлайна задач готова: [spreadsheet_id]`
- [ ] В логах есть: `✅ Системные шаблоны созданы`
- [ ] НЕТ ошибок: `invalid input value for enum task_type`
- [ ] НЕТ ошибок: `CRITICAL SECURITY: TELEGRAM_BOT_TOKEN is not set`
- [ ] НЕТ ошибок: `Google credentials не найдены`

## 🔄 Если что-то не работает:

1. Проверьте логи Railway на наличие ошибок
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте, что сервисный аккаунт имеет доступ к Google APIs
4. Если проблемы остаются, скопируйте полные логи ошибок для диагностики
