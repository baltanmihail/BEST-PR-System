# 🌐 Настройка Public Networking для фронтенда

## Проблема

Ты выбрал `best-pr-crm.railway.internal` — это **приватный** домен.

**`railway.internal`** — для внутренней коммуникации (например, backend → database).  
**Фронтенд** должен быть **публичным** (`railway.app`), чтобы пользователи открывали сайт в браузере.

---

## Что делать

### Шаг 1: Включить Public Networking

1. **Railway Dashboard** → выбери `best-pr-crm` (твой фронтенд сервис)
2. **Settings** → **Networking**
3. Найди раздел **Public Networking**
4. Нажми **Generate Domain** (или **Enable Public Networking**)
5. Railway автоматически создаст публичный домен типа:
   ```
   best-pr-crm-production-abc123.up.railway.app
   ```
6. **Скопируй этот URL!**

---

### Шаг 2: (Опционально) Изменить на короткий домен

Если хочешь короткий URL:

1. В разделе **Public Networking** → **Generate Domain**
2. Удали автоматически сгенерированное имя (если можно редактировать)
3. Попробуй ввести: `best-pr-crm` или `best-pr-frontend`
4. Если свободно → получишь `best-pr-crm.up.railway.app`

**Если имя занято** → Railway даст другой вариант (например, `best-pr-crm-2.up.railway.app`)

---

### Шаг 3: Скопировать публичный URL

После шага 1 или 2 у тебя будет публичный URL типа:
```
https://best-pr-crm-production-xxxxx.up.railway.app
```
или
```
https://best-pr-crm.up.railway.app
```

**Этот URL нужен для:**
- Открытия сайта в браузере
- Ссылки в Telegram боте
- Переменной `FRONTEND_URL` в бэкенде

---

### Шаг 4: Обновить переменные окружения

#### Backend:
**Railway** → `best-pr-api` → **Variables** → добавь/обнови:
```
FRONTEND_URL = https://best-pr-crm-production-xxxxx.up.railway.app
```
(подставь свой публичный URL из шага 3)

#### Frontend:
**Railway** → `best-pr-crm` → **Variables** → добавь:
```
VITE_API_URL = https://best-pr-api.up.railway.app/api/v1
```

---

## Итоговая конфигурация

### Frontend (Public):
- **Service**: `best-pr-crm`
- **Public URL**: `https://best-pr-crm-production-xxxxx.up.railway.app` ✅
- **Private URL**: `best-pr-crm.railway.internal` (можно оставить для внутреннего использования)
- **Variables**:
  - `VITE_API_URL = https://best-pr-api.up.railway.app/api/v1`

### Backend (Public):
- **Service**: `best-pr-api`
- **Public URL**: `https://best-pr-api.up.railway.app` ✅
- **Variables**:
  - `FRONTEND_URL = https://best-pr-crm-production-xxxxx.up.railway.app` ✅

---

## Проверка

После настройки:

1. **Открой публичный URL фронтенда** в браузере:
   ```
   https://best-pr-crm-production-xxxxx.up.railway.app
   ```
   → должен быть **React сайт** (не JSON, не ошибка)

2. **Открой Telegram бота** → `/start` → кнопка "Зарегистрироваться"
   → должна открываться **сайт**, а не JSON

3. **API документация**:
   ```
   https://best-pr-api.up.railway.app/docs
   ```
   → должен быть Swagger UI

---

## В чём разница?

### `railway.internal` (Private):
- ✅ Для внутренней коммуникации между сервисами Railway
- ❌ **НЕ доступен** из браузера
- ❌ **НЕ работает** для фронтенда (пользователи не смогут открыть)

**Примеры использования:**
- Backend → Database
- Frontend → Backend (внутренний запрос, но обычно используется публичный API)

### `railway.app` (Public):
- ✅ Доступен из браузера
- ✅ Можно открыть по ссылке
- ✅ Работает для фронтенда

**Обязательно для:**
- Фронтенд (пользователи открывают в браузере)
- API (если нужно вызывать извне)

---

## Важно

- **`railway.internal`** можно **оставить** — он не мешает
- **`railway.app`** (Public) — **обязательно** для фронтенда
- Оба домена могут работать одновременно для одного сервиса

---

## Следующие шаги

1. ✅ Включи **Public Networking** (Шаг 1)
2. ✅ Скопируй **публичный URL** (Шаг 3)
3. ✅ Обнови переменную `FRONTEND_URL` в бэкенде (Шаг 4)
4. ✅ Добавь переменную `VITE_API_URL` во фронтенд (Шаг 4)
5. ✅ Протестируй (Проверка)

Всё готово! 🎉
