# 🔧 Переменные окружения для Railway - ПРОСТАЯ ИНСТРУКЦИЯ

## 📋 Что нужно сделать

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на [railway.app](https://railway.app)
2. Откройте ваш проект "BEST PR System"
3. Откройте ваш сервис (Backend)
4. Перейдите во вкладку **"Variables"** (Переменные окружения)

---

## 🗄️ Шаг 2: Получите DATABASE_URL

### Что это?
Это адрес вашей базы данных PostgreSQL, которую создаст Railway.

### Как получить:

1. В Railway проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway создаст базу данных автоматически
4. Откройте созданную базу данных
5. Перейдите во вкладку **"Variables"**
6. Найдите `DATABASE_URL` и **скопируйте его значение**
7. **ВАЖНО**: Замените `postgresql://` на `postgresql+asyncpg://` в начале URL

**Пример:**
```
Было:  postgresql://postgres:password@host:port/railway
Стало: postgresql+asyncpg://postgres:password@host:port/railway
```

---

## 🔑 Шаг 3: Создайте SECRET_KEY

### Что это?
Это секретный ключ для шифрования JWT токенов (для авторизации пользователей).

### Как создать:

**Вариант 1: Через Python (рекомендуется)**
```powershell
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Вариант 2: Онлайн генератор**
- Зайдите на [randomkeygen.com](https://randomkeygen.com/)
- Скопируйте любой ключ из "CodeIgniter Encryption Keys"

**Пример SECRET_KEY:**
```
K8jL3mN9pQ2rS5tU7vW0xY1zA4bC6dE8fG
```

---

## 📝 Шаг 4: Добавьте все переменные в Railway

В Railway Dashboard → Variables добавьте следующие переменные:

### 1. DATABASE_URL
```
Значение: postgresql+asyncpg://postgres:password@host:port/railway
(Замените на ваш реальный URL из Шага 2)
```

### 2. SECRET_KEY
```
Значение: K8jL3mN9pQ2rS5tU7vW0xY1zA4bC6dE8fG
(Замените на ваш ключ из Шага 3)
```

### 3. TELEGRAM_BOT_TOKEN
```
Значение: 8275006557:AAFomMYi17esDTbsWBUUDHb154pnKxoqyKY
```

### 4. TELEGRAM_ADMIN_IDS
```
Значение: 5079636941
```

### 5. GOOGLE_SHEETS_ID
```
Значение: 1nNqA_cvUnzPADO3DcwD6ldzHIwnghR7ypKuzec4bk0I
(Из вашего существующего проекта)
```

### 6. GOOGLE_CREDENTIALS_1_JSON
```
Значение: (Скопируйте ВЕСЬ содержимое файла credentials-1.json)
```

**Как скопировать:**
1. Откройте файл `BEST Channel Bot/credentials-1.json`
2. Выделите ВСЁ содержимое (Ctrl+A)
3. Скопируйте (Ctrl+C)
4. Вставьте в Railway как одну строку (без переносов)

### 7. GOOGLE_CREDENTIALS_2_JSON
```
Значение: (Скопируйте ВЕСЬ содержимое файла credentials-2.json)
```

### 8. GOOGLE_CREDENTIALS_3_JSON
```
Значение: (Скопируйте ВЕСЬ содержимое файла credentials-3.json)
```

### 9. GOOGLE_CREDENTIALS_4_JSON
```
Значение: (Скопируйте ВЕСЬ содержимое файла credentials-4.json)
```

### 10. GOOGLE_CREDENTIALS_5_JSON
```
Значение: (Скопируйте ВЕСЬ содержимое файла credentials-5.json)
```

### 11. ENVIRONMENT
```
Значение: production
```

### 12. LOG_LEVEL
```
Значение: INFO
```

---

## ⚠️ ВАЖНО для Google Credentials

Когда копируете JSON файлы в Railway:
- **Удалите все переносы строк** (должна быть одна длинная строка)
- **Или используйте формат JSON в одну строку**

**Пример правильного формата:**
```json
{"type":"service_account","project_id":"auto-scout-bot","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"...","universe_domain":"googleapis.com"}
```

---

## ✅ Чеклист

- [ ] DATABASE_URL добавлен (с `postgresql+asyncpg://`)
- [ ] SECRET_KEY создан и добавлен
- [ ] TELEGRAM_BOT_TOKEN добавлен
- [ ] TELEGRAM_ADMIN_IDS добавлен
- [ ] GOOGLE_SHEETS_ID добавлен
- [ ] GOOGLE_CREDENTIALS_1_JSON добавлен
- [ ] GOOGLE_CREDENTIALS_2_JSON добавлен
- [ ] GOOGLE_CREDENTIALS_3_JSON добавлен
- [ ] GOOGLE_CREDENTIALS_4_JSON добавлен
- [ ] GOOGLE_CREDENTIALS_5_JSON добавлен
- [ ] ENVIRONMENT = production
- [ ] LOG_LEVEL = INFO

---

## 🚀 После добавления всех переменных

1. Railway автоматически пересоберёт проект
2. Проверьте логи в Railway Dashboard → Deployments
3. Откройте ваш домен: `https://your-app.railway.app/health`
4. Должно вернуть: `{"status": "ok"}`

---

## 🆘 Если что-то не работает

1. **Проверьте логи** в Railway Dashboard → Deployments → Logs
2. **Проверьте формат JSON** - должен быть в одну строку
3. **Проверьте DATABASE_URL** - должен начинаться с `postgresql+asyncpg://`
4. **Проверьте SECRET_KEY** - должен быть достаточно длинным (минимум 32 символа)
