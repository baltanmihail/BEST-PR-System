# 🚂 Полное руководство по деплою на Railway

## 📋 Содержание

1. [Первоначальная настройка](#первоначальная-настройка)
2. [Настройка переменных окружения](#настройка-переменных-окружения)
3. [Решение проблем](#решение-проблем)
4. [Обновление кода](#обновление-кода)

---

## 🚀 Первоначальная настройка

### Шаг 1: Создание проекта в Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Подключите GitHub аккаунт
6. Выберите репозиторий `BEST-PR-System`

### Шаг 2: Создание базы данных PostgreSQL

1. В Railway проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway создаст базу данных автоматически
4. Откройте созданную базу данных
5. Перейдите во вкладку **"Variables"**
6. Найдите переменную `DATABASE_URL`
7. **ВАЖНО**: НЕ копируйте шаблон с `${{...}}`! Нужен реальный URL!

**⚠️ КРИТИЧНО**: 
- ❌ **НЕ используйте**: `postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}`
- ✅ **Используйте**: Реальный URL, который Railway показывает в Variables (там будут реальные значения)

**Как получить реальный URL:**

**Вариант 1: Через Variables базы данных**
1. В Variables базы данных найдите `DATABASE_URL`
2. Railway показывает **два варианта**:
   - Шаблон с `${{...}}` - **НЕ используйте этот!**
   - Реальный URL с реальными значениями - **используйте этот!**
3. Если видите только шаблон, используйте Вариант 2

**Вариант 2: Через Connection Pooling (рекомендуется)**
1. В Railway Dashboard откройте вашу базу данных
2. Перейдите во вкладку **"Connect"** или **"Connection Info"**
3. Там будет показан реальный URL с реальными значениями
4. Скопируйте его

**После получения реального URL:**
1. Замените `postgresql://` на `postgresql+asyncpg://` в начале URL

**Пример ПРАВИЛЬНОГО URL:**
```
postgresql+asyncpg://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
```

**Пример НЕПРАВИЛЬНОГО URL (вызовет ошибку):**
```
postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}
```
Это шаблон, а не реальный URL!

### Шаг 3: Создание SECRET_KEY

Откройте PowerShell и выполните:

```powershell
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Скопируйте результат** - это ваш SECRET_KEY (примерно 43 символа).

---

## ⚙️ Настройка переменных окружения

### Где добавить переменные:

1. В Railway проекте откройте ваш сервис (Backend)
2. Перейдите во вкладку **"Variables"**
3. Нажмите **"+ New Variable"** для каждой переменной

### Список обязательных переменных (8 штук):

#### 1. DATABASE_URL
```
Ключ: DATABASE_URL
Значение: postgresql+asyncpg://postgres:password@host:port/railway
(Замените на ваш реальный URL из Шага 2)
```

#### 2. SECRET_KEY
```
Ключ: SECRET_KEY
Значение: (вставьте ключ из Шага 3)
Пример: K8jL3mN9pQ2rS5tU7vW0xY1zA4bC6dE8fG0hI1jK2lM3nO4pQ5rS6tU7vW8xY9z
```

#### 3. TELEGRAM_BOT_TOKEN
```
Ключ: TELEGRAM_BOT_TOKEN
Значение: (ваш токен от @BotFather)
⚠️ НИКОГДА не коммитьте токен в Git!
```

#### 4. TELEGRAM_ADMIN_IDS
```
Ключ: TELEGRAM_ADMIN_IDS
Значение: 5079636941
```

#### 5. GOOGLE_SHEETS_ID
```
Ключ: GOOGLE_SHEETS_ID
Значение: 1nNqA_cvUnzPADO3DcwD6ldzHIwnghR7ypKuzec4bk0I
```

#### 6. ENVIRONMENT
```
Ключ: ENVIRONMENT
Значение: production
```

#### 7. LOG_LEVEL
```
Ключ: LOG_LEVEL
Значение: INFO
```

#### 8. ACCESS_TOKEN_EXPIRE_MINUTES
```
Ключ: ACCESS_TOKEN_EXPIRE_MINUTES
Значение: 1440
```

### Дополнительные переменные (опционально):

#### 9-13. GOOGLE_CREDENTIALS_1_JSON до GOOGLE_CREDENTIALS_5_JSON
```
Ключ: GOOGLE_CREDENTIALS_1_JSON
Значение: (скопируйте ВСЁ содержимое файла BEST Channel Bot/credentials-1.json)

⚠️ ВАЖНО: JSON должен быть в одну строку (без переносов)
```

Повторите для credentials-2.json, credentials-3.json, credentials-4.json, credentials-5.json

#### 14. GOOGLE_DRIVE_FOLDER_ID
```
Ключ: GOOGLE_DRIVE_FOLDER_ID
Значение: (ID папки в Google Drive, если нужен)
```

#### 15. CORS_ORIGINS
```
Ключ: CORS_ORIGINS
Значение: https://your-app.railway.app,http://localhost:3000
```

#### 16. API_V1_PREFIX
```
Ключ: API_V1_PREFIX
Значение: /api/v1
```

---

## ✅ Проверка после настройки

1. Railway автоматически пересоберёт проект
2. Подождите 2-3 минуты
3. Проверьте логи: Railway Dashboard → Deployments → Logs
4. Откройте ваш домен: `https://your-app.railway.app/health`
5. Должно вернуть: `{"status": "ok"}`

---

## 🔄 Обновление кода

### Как обновить код на Railway:

```powershell
# 1. Перейдите в папку проекта
cd "BEST PR System"

# 2. Добавьте изменения
git add .

# 3. Создайте коммит
git commit -m "описание изменений"

# 4. Отправьте в GitHub
git push origin main
```

Railway автоматически пересоберёт проект после push в GitHub.

---

## 🐛 Решение проблем

### Проблема 1: "pip: command not found"

**Решение:**
- Убедитесь, что в корне проекта есть файл `requirements.txt`
- Убедитесь, что есть файл `nixpacks.toml`
- Закоммитьте изменения и отправьте в GitHub

### Проблема 2: "Repository not found"

**Решение:**
1. Проверьте, что репозиторий подключен к Railway
2. Убедитесь, что используете правильный URL репозитория
3. См. [GIT_SETUP.md](./GIT_SETUP.md)

### Проблема 3: База данных не подключается

**Проверьте:**
1. DATABASE_URL правильный (из Variables базы данных)
2. Для async нужно `postgresql+asyncpg://` (не просто `postgresql://`)
3. База данных создана и доступна

### Проблема 4: Токен попал в Git

**СРОЧНО:**
1. Отзовите токен в [@BotFather](https://t.me/BotFather): `/revoke`
2. Получите новый токен
3. Обновите токен в Railway Variables
4. **НИКОГДА не коммитьте токены в Git!**

### Проблема 5: Ошибки миграций

**Решение:**
```powershell
# Через Railway CLI
railway run alembic upgrade head

# Или проверьте логи в Railway Dashboard
```

---

## 📝 Чеклист перед деплоем

- [ ] Проект создан в Railway
- [ ] База данных PostgreSQL создана
- [ ] DATABASE_URL скопирован и изменён (добавлен `+asyncpg`)
- [ ] SECRET_KEY создан
- [ ] Все 8 обязательных переменных добавлены в Railway
- [ ] Токен Telegram НЕ в Git (только в Railway Variables)
- [ ] Код закоммичен в Git
- [ ] Репозиторий подключен к Railway
- [ ] Health check работает: `/health` возвращает `{"status": "ok"}`

---

## 🔐 Безопасность

**ВАЖНО:**
1. **Никогда не коммитьте** токены, пароли, секретные ключи в Git
2. Используйте **Railway Variables** для всех секретов
3. Проверяйте `.gitignore` перед коммитом
4. Если токен попал в Git - **срочно отзовите** его в BotFather

---

## 📞 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
