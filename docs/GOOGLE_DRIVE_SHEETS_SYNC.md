# Документация по синхронизации Google Drive и Google Sheets

## Обзор

Система BEST PR System интегрирована с Google Drive и Google Sheets для двусторонней синхронизации данных:
- **Google Drive**: Хранение файлов задач, материалов, галереи проектов
- **Google Sheets**: Визуализация таймлайнов с задачами и этапами

## Настройка

### 1. Переменные окружения

#### Обязательные:
```bash
# Google Credentials (5 сервисных аккаунтов для ротации и увеличения квоты)
GOOGLE_CREDENTIALS_1_JSON='{"type": "service_account", ...}'
GOOGLE_CREDENTIALS_2_JSON='{"type": "service_account", ...}'
GOOGLE_CREDENTIALS_3_JSON='{"type": "service_account", ...}'
GOOGLE_CREDENTIALS_4_JSON='{"type": "service_account", ...}'
GOOGLE_CREDENTIALS_5_JSON='{"type": "service_account", ...}'
```

#### Рекомендуемые:
```bash
# ID главной папки бота в Google Drive (создаётся автоматически при первом запуске)
GOOGLE_DRIVE_FOLDER_ID=11A1lVlBkHKLjGtrEmndS__5MOqNDqEJn

# ID таблицы с таймлайнами (создаётся автоматически при первой синхронизации)
GOOGLE_TIMELINE_SHEETS_ID=your_sheets_id_here
```

### 2. Структура папок Google Drive

При первом запуске система автоматически создаёт следующую структуру:

```
BEST PR System/                    # Главная папка (GOOGLE_DRIVE_FOLDER_ID)
├── Tasks/                        # Папка для задач
│   ├── {task_id}_{task_name}/   # Папка задачи (создаётся автоматически)
│   │   ├── materials/            # Материалы задачи
│   │   ├── final/               # Финальные работы
│   │   └── drafts/              # Черновики
├── Gallery/                      # Галерея проектов
├── Equipment/                    # Оборудование BEST Channel
├── Support/                      # Файлы поддержки
└── Users/                        # Профили пользователей
```

### 3. Google Sheets таблица с таймлайнами

При первой синхронизации календаря система создаёт таблицу **"BEST PR System - Таймлайны"** с листами:
- **Общий** - все задачи всех типов
- **SMM** - задачи типа SMM
- **Design** - задачи типа Design
- **Channel** - задачи типа Channel
- **PR-FR** - задачи типа PR-FR

## Функциональность

### Синхронизация Google Drive

#### Автоматическое создание папок
- При создании задачи автоматически создаётся папка в `Tasks/{task_id}_{task_name}/`
- Создаются подпапки: `materials/`, `final/`, `drafts/`
- ID папки сохраняется в поле `drive_folder_id` задачи

#### Отслеживание изменений (планируется)
- Polling каждые 5 минут для проверки изменений файлов
- Обновление БД при обнаружении новых/изменённых файлов
- Синхронизация изменений из системы в Drive

### Синхронизация Google Sheets

#### Структура таймлайна

**Простота и полнота:**
- Каждая задача занимает одну колонку
- Дни месяца в строках
- Этапы задачи отображаются в ячейках под задачей
- Цветовая кодировка по типам задач

**Формат ячеек:**
- Дата в первой колонке
- Название задачи в заголовке колонки (с гиперссылкой на карточку)
- В ячейках дня:
  - 📅 Дедлайн (если дедлайн задачи на этот день)
  - ✅/🔄/⏳ Этап (если дедлайн этапа на этот день)
  - 🆕 Создана (если задача создана в этот день)

**Цветовая кодировка:**
- **SMM** = Зелёный (#4CAF50)
- **Design** = Синий (#2196F3)
- **Channel** = Оранжевый (#FF9800)
- **PR-FR** = Фиолетовый (#9C27B0)

#### Гиперссылки
- Заголовки задач содержат гиперссылки на карточки задач на сайте
- Формат: `=HYPERLINK("https://best-pr-system.up.railway.app/tasks/{task_id}"; "{task_title}")`

## API Endpoints

### Синхронизация календаря с Google Sheets

```http
POST /api/v1/calendar/sync/sheets
Authorization: Bearer {token}
Content-Type: application/json

{
  "month": 1,
  "year": 2026,
  "roles": ["smm", "design", "channel", "prfr"]
}
```

**Ответ:**
```json
{
  "status": "success",
  "sheets_id": "1ABC...",
  "sheets_url": "https://docs.google.com/spreadsheets/d/1ABC...",
  "month": 1,
  "year": 2026,
  "roles": ["smm", "design", "channel", "prfr"]
}
```

## Ограничения API

### Google API Rate Limits
- **60 запросов в минуту** на один сервисный аккаунт
- **5 сервисных аккаунтов** = **300 запросов в минуту** всего
- Система автоматически ротирует аккаунты для распределения нагрузки

### Оптимизация
- Использование batch операций для множественных обновлений
- Кэширование метаданных файлов (TTL: 3-5 минут)
- Фоновые запросы используют отдельные аккаунты
- Rate limiting на каждый аккаунт отдельно

## Использование

### Ручная синхронизация календаря

1. Откройте сайт и перейдите в раздел "Календарь"
2. Выберите месяц и год
3. Нажмите кнопку "Синхронизировать с Google Sheets"
4. Система создаст/обновит таблицу с таймлайнами

### Автоматическая синхронизация (планируется)

Фоновая задача будет автоматически синхронизировать:
- Изменения задач → Google Sheets (каждые 5 минут)
- Изменения на Google Drive → БД (каждые 5 минут)
- Изменения в Google Sheets → БД (каждые 5 минут)

## Устранение неполадок

### Таблица не создаётся
1. Проверьте наличие `GOOGLE_CREDENTIALS_*_JSON` в переменных окружения
2. Убедитесь, что сервисные аккаунты имеют права на создание файлов в папке
3. Проверьте логи на наличие ошибок

### Папки задач не создаются
1. Проверьте `GOOGLE_DRIVE_FOLDER_ID` в переменных окружения
2. Убедитесь, что папка существует и доступна сервисным аккаунтам
3. Проверьте логи на наличие ошибок

### Ошибки rate limit
- Система автоматически обрабатывает rate limits
- При превышении лимита запросы будут задержаны автоматически
- Используйте фоновые операции для больших объёмов данных

## Будущие улучшения

1. **Webhooks для Google Drive** - мгновенное отслеживание изменений
2. **Двусторонняя синхронизация Sheets** - изменения в таблице → БД
3. **Автоматическое создание файлов ТЗ** - Google Docs с ТЗ для каждой роли
4. **Синхронизация материалов** - автоматическое отображение файлов из Drive
5. **Интеграция с галереей** - автоматическое добавление финальных работ
