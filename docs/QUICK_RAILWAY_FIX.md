# ⚡ Быстрое исправление деплоя на Railway

## ✅ Что было исправлено:

1. **Удалён `nixpacks.toml`** - он вызывал ошибку
2. **Обновлён `railway.json`** - добавлено обновление pip
3. **Создан `runtime.txt`** - указывает версию Python

## 🚀 Что делать сейчас:

### 1. Закоммитьте изменения:

```powershell
cd "BEST PR System"
git add .
git commit -m "fix: Remove nixpacks.toml and fix Railway deployment"
git push
```

### 2. Railway автоматически пересоберёт проект

### 3. **ВАЖНО**: Настройте переменные окружения в Railway!

1. Откройте ваш проект в Railway Dashboard
2. Перейдите в ваш сервис (Backend)
3. Откройте вкладку **"Variables"**
4. Добавьте:

```env
DATABASE_URL=postgresql+asyncpg://postgres:password@host:port/railway
SECRET_KEY=your-secret-key-here
TELEGRAM_BOT_TOKEN=your-token
TELEGRAM_ADMIN_IDS=5079636941
ENVIRONMENT=production
LOG_LEVEL=INFO
```

**Где взять DATABASE_URL:**
- В Railway проекте откройте вашу PostgreSQL базу данных
- Перейдите в "Variables"
- Скопируйте `DATABASE_URL`
- Замените `postgresql://` на `postgresql+asyncpg://`

---

## 📝 Структура файлов для Railway:

- ✅ `Procfile` - команда запуска
- ✅ `railway.json` - конфигурация сборки и запуска
- ✅ `runtime.txt` - версия Python
- ❌ `nixpacks.toml` - удалён (не нужен)

---

## 🔍 Проверка после деплоя:

1. Откройте Railway Dashboard
2. Перейдите в "Deployments"
3. Проверьте логи - не должно быть ошибок
4. Откройте ваш домен: `https://your-app.railway.app/health`
5. Должно вернуть: `{"status": "ok"}`

---

## 🐛 Если всё ещё не работает:

1. **Проверьте логи** в Railway Dashboard → Deployments → Logs
2. **Проверьте переменные окружения** - все ли установлены?
3. **Проверьте DATABASE_URL** - правильный ли формат?

**Подробнее**: [RAILWAY_FIX.md](./RAILWAY_FIX.md)
