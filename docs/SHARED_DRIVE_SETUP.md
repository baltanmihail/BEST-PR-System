# Настройка Shared Drive (Team Drive) для BEST PR System

## 📋 Быстрая настройка

### Шаг 1: Обновите переменные окружения в Railway

В Railway Dashboard для backend сервиса обновите:

```env
# Замените на ваш аккаунт в домене организации (bmstu-best.ru)
GOOGLE_DRIVE_OWNER_EMAIL=mikhail.baltyan@bmstu-best.ru
```

**ВАЖНО:** 
- ❌ **НЕ удаляйте** переменную `GOOGLE_DRIVE_OWNER_EMAIL` полностью - оставьте пустой или укажите правильный email
- ✅ Используйте аккаунт в том же домене, что и Shared Drive (bmstu-best.ru)
- ✅ Старая переменная `baltanmihail@gmail.com` **может быть удалена** или заменена

### Шаг 2: Проверьте доступ сервисного аккаунта к Shared Drive

1. Откройте Shared Drive "LBG Drive" (drive@bmstu-best.ru)
2. Нажмите "Настройки" → "Управление доступом"
3. Найдите email сервисного аккаунта (из `GOOGLE_CREDENTIALS_1_JSON`, поле `client_email`)
4. Убедитесь, что у сервисного аккаунта есть права **"Content Manager"** или **"Manager"**
5. Если нет - добавьте с этими правами

### Шаг 3: Проверьте корневую папку

Убедитесь, что папка `1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI` находится **внутри Shared Drive "LBG Drive"**.

Если папка находится в другом месте:
1. Переместите её в Shared Drive "LBG Drive"
2. Или создайте новую папку в Shared Drive и обновите `ROOT_FOLDER_ID` в коде (не рекомендуется)

### Шаг 4: Очистка старой папки (если нужно)

Если была создана папка "BEST PR System" или "BEST PR-System" **не в Shared Drive**:

**Вариант 1 (рекомендуется):** Удалите старую папку
- Найдите старую папку в Google Drive
- Удалите её (или переместите содержимое вручную, если нужно)
- Система автоматически создаст новую структуру в правильном месте (Shared Drive) при следующем запуске

**Вариант 2:** Оставьте старую папку
- Система будет использовать папку в Shared Drive (если она там есть)
- Старая папка останется, но не будет использоваться

### Шаг 5: Деплой

```bash
cd "BEST PR System"
git add .
git commit -m "Настройка для Shared Drive"
git push origin main
```

## ✅ Проверка после деплоя

1. **Проверьте логи в Railway:**
   - ✅ `✅ Google Drive структура инициализирована`
   - ✅ `✅ Ownership папки передан пользователю mikhail.baltyan@bmstu-best.ru`
   - ✅ `✅ Google таблица таймлайна задач готова`

2. **Проверьте Google Drive:**
   - Откройте Shared Drive "LBG Drive" (drive@bmstu-best.ru)
   - Найдите папку `1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI`
   - Внутри должна быть папка "BEST PR System"
   - Проверьте ownership файлов - должен быть `mikhail.baltyan@bmstu-best.ru` или Shared Drive

3. **Проверьте квоту:**
   - Файлы в Shared Drive используют квоту организации, а не сервисного аккаунта
   - Квота общая для всех файлов в Shared Drive

## 🔧 Как работает ownership в Shared Drive

- **В Shared Drive:** Файлы принадлежат организации (Shared Drive), квота общая
- **Ownership между аккаунтами одного домена:** Можно передать (например, с сервисного аккаунта на mikhail.baltyan@bmstu-best.ru)
- **Автоматическая передача:** Система пытается передать ownership каждому созданному файлу/папке

## ❓ FAQ

**Q: Нужно ли удалять переменную `GOOGLE_DRIVE_OWNER_EMAIL` со старым значением?**
A: Нет, просто обновите её на `mikhail.baltyan@bmstu-best.ru`. Старое значение `baltanmihail@gmail.com` можно удалить.

**Q: Нужно ли удалять старую папку "BEST PR System"?**
A: Да, рекомендуется удалить, если она создана не в Shared Drive. Система создаст новую структуру в правильном месте.

**Q: Что делать, если ownership не передаётся?**
A: Проверьте:
- Сервисный аккаунт имеет права "Content Manager" или "Manager" на Shared Drive
- Email `mikhail.baltyan@bmstu-best.ru` указан правильно
- Аккаунты в одном домене (bmstu-best.ru)

**Q: Как проверить, что файлы используют квоту Shared Drive?**
A: Файлы в Shared Drive автоматически используют квоту организации. Проверьте в настройках Shared Drive "LBG Drive" - там будет видно использование квоты.
