# 🔧 Исправление ошибок Railway

## Проблемы и решения

### 1. TypeScript ошибки (исправлено ✅)

**Ошибки:**
- `trailRefs is declared but its value is never read` — удалена неиспользуемая переменная
- `Property 'telegram_username' does not exist on type 'User'` — добавлено в тип `User`
- `Property 'email' does not exist on type 'User'` — добавлено в тип `User`

**Исправления:**
- ✅ Удалён `trailRefs` из `Cursor2.tsx`
- ✅ Добавлены `telegram_username` и `email` в `User` интерфейс
- ✅ Обновлён `Support.tsx` для использования правильных полей

---

### 2. Курсор смещён (исправлено ✅)

**Проблема:** Курсор смещён влево-вниз от места нажатия

**Причина:** `transform: translate(-50%, -50%)` центрирует курсор, но нужно убедиться, что позиция мыши корректная

**Исправление:**
- ✅ Проверено центрирование через `translate(-50%, -50%)`
- ✅ Увеличена скорость курсора (`lerpFactor: 0.35`) для быстрого отклика

**Если проблема остаётся:**
Можно попробовать другой подход к центрированию:
```typescript
// Вместо translate(-50%, -50%) использовать:
style={{
  left: `${currentPos.current.x - 20}px`, // 20 = половина ширины (40px / 2)
  top: `${currentPos.current.y - 20}px`,  // 20 = половина высоты (40px / 2)
}}
```

---

### 3. Бот не запускается (нужно добавить переменную)

**Ошибка:**
```
⚠️ Бот не запускается в окружении 'development'. Запустите только в production.
```

**Решение:**
Добавь переменную окружения на Railway:

**Railway** → `best-pr-api` → **Variables** → **New Variable**:
```
ENVIRONMENT = production
```

**ВАЖНО:** Это **обязательная** переменная для запуска бота на Railway!

---

### 4. Google Drive структура (реализовано ✅)

**Структура папок:**
```
BEST (Координаторы)/  (10A2GVTrYq8_Rm6pBDvQUEQxibHFdWxBd)
└── BEST PR System/  (создаётся автоматически)
    ├── Поддержка/  (файлы от пользователей)
    ├── Файлы/  (общие файлы)
    ├── Задачи/  (файлы связанные с задачами)
    └── Оборудование/  (документы по оборудованию)
```

**Реализация:**
- ✅ Создан `drive_structure.py` — автоматическая инициализация структуры
- ✅ Поддержка загрузки файлов в `/support/request`
- ✅ Файлы загружаются в папку "Поддержка" при первом запуске

**Настройка:**

1. **Дать доступ service accounts к папке координаторов:**
   - Открой: https://drive.google.com/drive/folders/10A2GVTrYq8_Rm6pBDvQUEQxibHFdWxBd
   - Нажми **Share** (Поделиться)
   - Добавь email каждого service account (`xxx@yyy.iam.gserviceaccount.com`)
   - Права: **Editor** (Редактор)
   - Нажми **Send**

2. **(Опционально) Установить GOOGLE_DRIVE_FOLDER_ID:**
   После первого запуска система создаст папку и выведет её ID в логи:
   ```
   ✅ Папка бота: abc123xyz...
   💡 Установите GOOGLE_DRIVE_FOLDER_ID=abc123xyz... в переменные окружения
   ```
   
   **Railway** → `best-pr-api` → **Variables** → **New Variable**:
   ```
   GOOGLE_DRIVE_FOLDER_ID = abc123xyz...
   ```

---

## Переменные окружения на Railway

### Backend (`best-pr-api`):

**Обязательные:**
```bash
ENVIRONMENT = production  # ⚠️ ОБЯЗАТЕЛЬНО для запуска бота!

TELEGRAM_BOT_TOKEN = xxx
TELEGRAM_ADMIN_IDS = 5079636941,xxx,xxx,xxx
FRONTEND_URL = https://best-pr-system.up.railway.app

# Google Credentials (1-5 service accounts)
GOOGLE_CREDENTIALS_1_JSON = {"type":"service_account",...}
GOOGLE_CREDENTIALS_2_JSON = {...}
# ... и т.д.

# База данных
DATABASE_URL = postgresql://...
```

**Опциональные:**
```bash
GOOGLE_DRIVE_FOLDER_ID = abc123xyz...  # Если хочешь использовать свою папку
GOOGLE_SHEETS_ID = 1nNqA_cvUnzPADO3DcwD6ldzHIwnghR7ypKuzec4bk0I
GOOGLE_EQUIPMENT_SHEETS_ID = 1nNqA_cvUnzPADO3DcwD6ldzHIwnghR7ypKuzec4bk0I
```

---

### Frontend (`best-pr-crm`):

**Обязательные:**
```bash
VITE_API_URL = https://best-pr-api.up.railway.app/api/v1
```

---

## Проверка после деплоя

1. **Проверь логи backend:**
   - Должно быть: `✅ Google Drive структура инициализирована`
   - Или: `⚠️ Не удалось инициализировать Google Drive структуру` (если нет credentials)

2. **Проверь логи бота:**
   - Должно быть: `🤖 Starting Telegram bot...`
   - Или: `⚠️ Бот не запускается в окружении...` (если `ENVIRONMENT != production`)

3. **Проверь Google Drive:**
   - Открой: https://drive.google.com/drive/folders/10A2GVTrYq8_Rm6pBDvQUEQxibHFdWxBd
   - Должна быть папка **"BEST PR System"** с подпапками

---

## Что делать если проблемы

### Бот не запускается:
1. Проверь `ENVIRONMENT = production` на Railway
2. Проверь `TELEGRAM_BOT_TOKEN` установлен
3. Проверь логи: Railway → Service → Deployments → Logs

### Google Drive не работает:
1. Проверь, что service accounts имеют доступ к папке координаторов
2. Проверь `GOOGLE_CREDENTIALS_1_JSON` установлен на Railway
3. Проверь логи при запуске (должна быть инициализация структуры)

### Файлы не загружаются:
1. Проверь, что создана папка "Поддержка" в Google Drive
2. Проверь размер файла (макс. 10MB)
3. Проверь логи API при загрузке файла

---

Всё готово! 🎉
