# 🚀 Деплой фронтенда на Railway

## Вариант 1: Деплой на Railway (рекомендуется)

### Шаг 1: Создайте новый сервис в Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите проект `BEST-PR-System`
3. Нажмите **"New"** → **"Service"**
4. Выберите **"GitHub Repo"** и подключите тот же репозиторий

### Шаг 2: Настройте переменные окружения

В настройках нового сервиса добавьте:
```
VITE_API_URL=https://best-pr-system.up.railway.app/api/v1
NODE_ENV=production
```

### Шаг 3: Настройте деплой

Файл `railway.json` уже создан в папке `frontend`. Railway автоматически:
1. Установит зависимости (`npm install`)
2. Соберёт проект (`npm run build`)
3. Запустит сервер (`npx serve -s dist -l $PORT`)

**Важно:** Убедитесь, что в настройках сервиса установлен **Root Directory** = `frontend`

### Шаг 4: Установите зависимости

Railway автоматически установит зависимости из `package.json`.

### Шаг 5: Проверьте деплой

После деплоя проверьте:
- Фронтенд доступен по URL (Railway предоставит)
- API запросы работают
- Нет ошибок в консоли браузера

---

## Вариант 2: Деплой на Vercel (альтернатива)

### Шаг 1: Установите Vercel CLI

```powershell
npm install -g vercel
```

### Шаг 2: Задеплойте

```powershell
cd "BEST PR System/frontend"
vercel
```

### Шаг 3: Настройте переменные окружения

В Vercel Dashboard → Settings → Environment Variables:
```
VITE_API_URL=https://best-pr-system.up.railway.app/api/v1
```

---

## Вариант 3: Статический хостинг (Netlify, GitHub Pages)

### Для Netlify:

1. Создайте `netlify.toml` в папке `frontend`:
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

2. Подключите репозиторий в Netlify
3. Установите переменную окружения `VITE_API_URL`

---

## 🔧 Локальная разработка

```powershell
cd "BEST PR System/frontend"
npm install
npm run dev
```

Фронтенд будет доступен на `http://localhost:5173`

---

## ✅ Проверка после деплоя

1. Откройте URL фронтенда
2. Проверьте, что главная страница загружается
3. Проверьте консоль браузера (F12) - не должно быть ошибок
4. Попробуйте зарегистрироваться/войти
5. Проверьте, что API запросы работают

---

## 🐛 Решение проблем

### Проблема: Белый экран
- Проверьте консоль браузера
- Убедитесь, что `VITE_API_URL` установлен правильно
- Проверьте, что API доступен

### Проблема: 404 на роутах
- Убедитесь, что настроен redirect на `index.html` (SPA routing)

### Проблема: CORS ошибки
- Проверьте настройки CORS в бэкенде
- Убедитесь, что фронтенд URL добавлен в `CORS_ORIGINS`
