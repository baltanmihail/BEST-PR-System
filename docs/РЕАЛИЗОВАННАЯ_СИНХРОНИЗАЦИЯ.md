# Реализованная синхронизация Google Drive и Sheets

## ✅ Что реализовано

### 1. Фильтрация по статусам задач
- ✅ Фильтр добавлен в API endpoint `/api/v1/calendar/sync/sheets`
- ✅ Фильтр применяется при запросе задач из БД
- ✅ Поддерживаемые статусы: `draft`, `open`, `assigned`, `in_progress`, `review`, `completed`, `cancelled`

**Использование:**
```http
POST /api/v1/calendar/sync/sheets?month=1&year=2026&statuses=open&statuses=assigned&statuses=in_progress
```

### 2. Цветовое отображение в Google Sheets

**Цвета для типов задач:**
- SMM = Зелёный (#4CAF50)
- Design = Синий (#2196F3)
- Channel = Оранжевый (#FF9800)
- PR-FR = Фиолетовый (#9C27B0)

**Цвета для статусов задач:**
- `draft` = Светло-серый
- `open` = Зелёный
- `assigned` = Синий
- `in_progress` = Золотой
- `review` = Фиолетовый
- `completed` = Зелёный
- `cancelled` = Красный

**Цвета для этапов (из `status_color`):**
- `green` = Зелёный
- `yellow` = Жёлтый
- `red` = Красный
- `purple` = Фиолетовый
- `blue` = Синий

**Просроченные дедлайны:**
- Автоматически окрашиваются в красный цвет (#F44336)

### 3. Отображение этапов в таблице

**Формат ячеек:**
- 📅 Дедлайн (если дедлайн задачи на этот день)
- 🟢/🟡/🔴/🟣/🔵 ✅/🔄/⏳ Этап (цвет + статус + название этапа)
- 🆕 Создана (если задача создана в этот день)

**Особенности:**
- Все этапы на один день отображаются в одной ячейке (через перенос строки)
- Цвет ячейки соответствует цвету этапа (`status_color`)
- Просроченные этапы автоматически окрашиваются в красный

### 4. Синхронизация изменений из Google Sheets обратно в систему

**Реализовано:**
- ✅ Метод `sync_sheets_changes_to_db()` в `SheetsSyncService`
- ✅ API endpoint `/api/v1/calendar/sync/sheets-from-db`
- ✅ Отслеживание изменений дедлайнов задач
- ✅ Обновление БД при обнаружении расхождений

**Как работает:**
1. Читает данные из таблицы
2. Извлекает ID задач из гиперссылок в заголовках
3. Сравнивает дедлайны в таблице с дедлайнами в БД
4. Обновляет БД при обнаружении изменений

**Использование:**
```http
POST /api/v1/calendar/sync/sheets-from-db?sheet_name=Общий
```

### 5. Системные шаблоны задач

**Реализовано:**
- ✅ Модель `TaskTemplate` с полями для шаблонов
- ✅ API для CRUD операций с шаблонами
- ✅ Сохранение шаблонов на Google Drive в папке `Templates/`
- ✅ Скрипт для создания системных шаблонов
- ✅ Автоматическое создание при первом запуске

**Системные шаблоны:**
1. **Пост в соцсети** (SMM)
   - Этапы: Концепт → Текст → Дизайн → Публикация

2. **Графика для поста** (Design)
   - Этапы: Бриф → Концепт → Эскиз → Финальная версия → Публикация

3. **Видеоролик** (Channel)
   - Этапы: Сценарий → Съёмка → Монтаж → Озвучка → Публикация
   - С возможностью получения оборудования

4. **Организация мероприятия** (PR-FR)
   - Этапы: Планирование → Подготовка → Проведение → Отчёт

**Структура папок на Drive:**
```
BEST PR System/
└── Templates/
    ├── Coordinators/
    └── VP4PR/
```

### 6. Автоматическая синхронизация этапов

**Реализовано:**
- ✅ Синхронизация при изменении этапа через API
- ✅ Автоматическое обновление Sheets при изменении этапа
- ✅ Фоновая синхронизация (не блокирует ответ)

**Как работает:**
- При изменении этапа через `PATCH /api/v1/stages/{stage_id}`
- Автоматически запускается синхронизация текущего месяца с Sheets
- Выполняется в фоне, не блокирует ответ API

## 📋 API Endpoints

### Синхронизация календаря с Sheets
```http
POST /api/v1/calendar/sync/sheets
Query params:
  - month: int (1-12)
  - year: int
  - role: "smm" | "design" | "channel" | "prfr" | "all"
  - statuses: List[str] (опционально)
```

### Синхронизация изменений из Sheets в БД
```http
POST /api/v1/calendar/sync/sheets-from-db
Query params:
  - sheet_name: str (по умолчанию "Общий")
```

### Шаблоны задач
```http
GET    /api/v1/task-templates              # Список шаблонов
GET    /api/v1/task-templates/{id}         # Шаблон по ID
POST   /api/v1/task-templates              # Создать шаблон
PATCH  /api/v1/task-templates/{id}         # Обновить шаблон
DELETE /api/v1/task-templates/{id}          # Удалить шаблон
POST   /api/v1/task-templates/{id}/create-task  # Создать задачу из шаблона
```

## 🔄 Поток синхронизации

### Создание задачи:
1. Задача создаётся в БД
2. Автоматически создаётся папка в Google Drive
3. `drive_folder_id` сохраняется в задаче
4. При следующей синхронизации Sheets → задача появляется в таблице

### Изменение этапа:
1. Этап обновляется через API
2. Автоматически запускается синхронизация Sheets
3. Таблица обновляется с новыми данными этапа

### Изменение в Sheets:
1. Координатор изменяет дедлайн в таблице
2. Вызывается `/api/v1/calendar/sync/sheets-from-db`
3. Система читает данные из таблицы
4. Обновляет дедлайн задачи в БД

## 🎨 Визуализация в таблице

**Структура:**
- Первая колонка: Даты месяца
- Остальные колонки: Задачи (по одной колонке на задачу)
- Заголовки колонок: Название задачи (гиперссылка на карточку)

**Цвета:**
- Заголовок колонки: цвет по статусу задачи
- Ячейки с дедлайнами: цвет по типу задачи (или красный если просрочено)
- Ячейки с этапами: цвет из `status_color` этапа (или красный если просрочено)

**Иконки:**
- 📅 = Дедлайн задачи
- 🟢/🟡/🔴/🟣/🔵 = Цвет этапа
- ✅ = Этап завершён
- 🔄 = Этап в работе
- ⏳ = Этап ожидает
- 🆕 = Задача создана

## ⚙️ Настройка

### Переменные окружения:
```bash
# Обязательно
GOOGLE_CREDENTIALS_1_JSON='...'
GOOGLE_CREDENTIALS_2_JSON='...'
GOOGLE_CREDENTIALS_3_JSON='...'
GOOGLE_CREDENTIALS_4_JSON='...'
GOOGLE_CREDENTIALS_5_JSON='...'

# Рекомендуется
GOOGLE_DRIVE_FOLDER_ID=11A1lVlBkHKLjGtrEmndS__5MOqNDqEJn
GOOGLE_TIMELINE_SHEETS_ID=your_sheets_id_here
```

### Первый запуск:
1. Система автоматически создаст структуру папок на Drive
2. Система автоматически создаст системные шаблоны задач
3. При первой синхронизации создастся таблица с таймлайнами

## 🚀 Что дальше (опционально)

1. **Автоматическая фоновая синхронизация:**
   - Задача каждые 5 минут
   - Синхронизация Drive → БД
   - Синхронизация Sheets → БД
   - Синхронизация БД → Sheets

2. **Webhooks для Google Drive:**
   - Мгновенное отслеживание изменений
   - Вместо polling каждые 5 минут

3. **UI для управления этапами:**
   - Добавление/удаление этапов в карточке задачи
   - Изменение цветов и сроков этапов
   - Drag-and-drop для изменения порядка

4. **UI для шаблонов:**
   - Выбор шаблона при создании задачи
   - Редактирование шаблонов
   - Создание своих шаблонов
