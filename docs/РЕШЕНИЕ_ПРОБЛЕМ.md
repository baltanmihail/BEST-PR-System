# 🔧 Подробное решение ваших проблем

## ❌ Проблема 1: Ошибка enum "invalid input value for enum task_type: 'SMM'"

### Что произошло:
Код пытался вставить значение `'SMM'` (uppercase) в базу данных, но enum `task_type` в PostgreSQL определён только с lowercase значениями: `'smm'`, `'design'`, `'channel'`, `'prfr'`.

### ✅ Решение (УЖЕ ИСПРАВЛЕНО):
1. ✅ Добавлены TypeDecorator классы (`TaskTypeType` и `TaskPriorityType`) в модель `TaskTemplate`
2. ✅ Они автоматически конвертируют enum объекты в их значения (lowercase)
3. ✅ Добавлена дополнительная проверка и fallback для некорректных значений
4. ✅ Улучшено логирование для диагностики

**После деплоя эта ошибка должна исчезнуть.**

---

## ❌ Проблема 2: Не создаются файлы в Google Drive (таймлайн, шаблоны)

### 📝 Объяснение ситуации:

#### Основная проблема - превышение квоты хранилища:
1. ⚠️ **Главная причина**: Квота Google Drive сервисного аккаунта **превышена** (каждый аккаунт даёт 15 ГБ)
2. ✅ **Папки могут создаваться** - они почти не занимают места (метаданные ~1-2 КБ)
3. ❌ **Тяжелые файлы НЕ могут создаваться** - таблицы (Google Sheets), документы (Google Docs), файлы с контентом занимают место
4. ⚠️ Когда квота превышена, папки создаются успешно, но создание таблиц/документов падает с ошибкой `storageQuotaExceeded`

#### Дополнительные возможные проблемы:
1. ❌ У вас **нет доступа** к Shared Drive "LBG Drive" (drive@bmstu-best.ru)
2. ❌ В коде захардкожен `ROOT_FOLDER_ID = "1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI"`, который находится в Shared Drive
3. ❌ Сервисный аккаунт не может создать файлы в Shared Drive, к которому нет доступа

#### ✅ ЧТО МОЖНО СДЕЛАТЬ:

**Вариант 1: Работа БЕЗ Shared Drive (РЕКОМЕНДУЕТСЯ для вашей ситуации)**

Система **УЖЕ НАСТРОЕНА** для автоматической работы без Shared Drive! Код автоматически обнаружит, что Shared Drive недоступен, и создаст папку в корне Google Drive сервисного аккаунта.

**Что делать:**

1. **В Railway Dashboard очистите переменную:**
   ```env
   GOOGLE_DRIVE_FOLDER_ID=
   ```
   (Оставьте ПУСТОЙ или удалите переменную полностью)

2. **Опционально укажите email для sharing:**
   ```env
   GOOGLE_DRIVE_OWNER_EMAIL=baltanmihail@gmail.com
   ```
   (Это даст вам автоматический доступ к файлам, но ownership останется у сервисного аккаунта)

3. **Убедитесь, что есть credentials:**
   ```env
   GOOGLE_CREDENTIALS_1_JSON={"type":"service_account",...}
   ```

4. **Деплой:**
   ```bash
   git add .
   git commit -m "Исправление enum и настройка для работы без Shared Drive"
   git push origin main
   ```

**Что произойдёт:**
- Система попытается использовать `ROOT_FOLDER_ID` (Shared Drive)
- Обнаружит, что доступ запрещён
- Автоматически создаст папку "BEST PR System" в корне Google Drive сервисного аккаунта
- Создаст все нужные подпапки (Tasks, Gallery, Equipment, Support, Users, Admin)
- Создаст таблицу "BEST PR System - Таймлайны"
- Создаст системные шаблоны задач

**Где будут файлы:**
- Файлы будут в Google Drive сервисного аккаунта
- Для доступа к файлам нужно либо:
  - Использовать API с credentials сервисного аккаунта
  - Настроить автоматический sharing через `GOOGLE_DRIVE_OWNER_EMAIL`
  - Найти папку через ID из логов и поделиться вручную

**Квота хранилища:**
- Каждый сервисный аккаунт даёт **15 ГБ**
- Для увеличения создайте дополнительные сервисные аккаунты и добавьте их credentials в `GOOGLE_CREDENTIALS_2_JSON`, `GOOGLE_CREDENTIALS_3_JSON` и т.д.

**Ownership файлов:**
- ❌ Ownership нельзя передать между разными доменами (сервисный аккаунт vs личный Gmail)
- ✅ Но можно настроить автоматический sharing - вы получите права "Редактор"
- ✅ Файлы будут функционально доступны, но формально останутся у сервисного аккаунта

---

### 📋 Пошаговая инструкция для вашей ситуации:

#### Шаг 1: Откройте Railway Dashboard

1. Зайдите в ваш проект на Railway
2. Выберите backend сервис (`best-pr-api`)
3. Перейдите в раздел **Variables** (Переменные окружения)

#### Шаг 2: Очистите GOOGLE_DRIVE_FOLDER_ID

Найдите переменную `GOOGLE_DRIVE_FOLDER_ID` и:
- **Вариант А:** Удалите её полностью
- **Вариант Б:** Оставьте пустой: `GOOGLE_DRIVE_FOLDER_ID=`

#### Шаг 3: Проверьте другие переменные

Убедитесь, что установлены:
- ✅ `GOOGLE_CREDENTIALS_1_JSON` - валидный JSON сервисного аккаунта
- ✅ `TELEGRAM_BOT_TOKEN` - токен бота (для исправления ошибки безопасности)
- ✅ `DATABASE_URL` - строка подключения к PostgreSQL
- ✅ Остальные обязательные переменные

#### Шаг 4: Деплой

```bash
cd "BEST PR System"
git add .
git commit -m "Исправление enum и настройка Google Drive без Shared Drive"
git push origin main
```

#### Шаг 5: Проверка логов

После деплоя проверьте логи Railway. Вы должны увидеть:

✅ **Успешные логи:**
```
⚠️ ROOT_FOLDER_ID 1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI недоступен (возможно, нет доступа к Shared Drive)
📁 Создаём папку в корне Google Drive сервисного аккаунта вместо Shared Drive...
✅ Папка создана в корне Google Drive: [folder_id]
✅ Google Drive структура инициализирована: [bot_folder_id]
✅ Папка 'Tasks': [tasks_folder_id]
✅ Папка 'Gallery': [gallery_folder_id]
✅ Папка 'Equipment': [equipment_folder_id]
✅ Папка 'Support': [support_folder_id]
✅ Папка 'Users': [users_folder_id]
✅ Папка 'Admin': [admin_folder_id]
📊 Инициализация таблицы таймлайна задач...
✅ ID папки бота: [bot_folder_id]
📝 Создание таблицы в папке [bot_folder_id]...
✅ Таблица создана: [spreadsheet_id]
✅ Google таблица таймлайна задач готова: [spreadsheet_id]
✅ Системные шаблоны созданы
```

❌ **Если видите ошибки:**

1. **"Google credentials не найдены"**
   - Проверьте, что `GOOGLE_CREDENTIALS_1_JSON` установлен и валиден
   - Убедитесь, что JSON не содержит ошибок (можно проверить через JSON validator)

2. **"storageQuotaExceeded"** ⚠️ **ОСНОВНАЯ ПРОБЛЕМА**
   - ❌ **Причина**: Квота Google Drive сервисного аккаунта превышена (каждый аккаунт даёт 15 ГБ)
   - ✅ **Важно**: Папки создаются (почти не занимают места), но **тяжелые файлы** (таблицы, документы) **НЕ могут быть созданы**
   - 🔄 **Автоматическое решение**: Система теперь автоматически пробует следующий credential из списка при ошибке квоты
   - 💡 **Решение вручную**: 
     - Освободите место в Google Drive сервисного аккаунта (удалите старые файлы)
     - **Или** создайте дополнительные сервисные аккаунты и добавьте их credentials в `GOOGLE_CREDENTIALS_2_JSON`, `GOOGLE_CREDENTIALS_3_JSON` и т.д. (до 5 аккаунтов = 75 ГБ)
     - **Или** передайте ownership файлов пользователю через `GOOGLE_DRIVE_OWNER_EMAIL` (файлы будут использовать квоту пользователя)

3. **"permissionDenied" или "forbidden"**
   - Сервисный аккаунт не имеет прав
   - Решение: Проверьте, что в Google Cloud Console включены API:
     - Google Drive API ✅
     - Google Sheets API ✅
     - Google Docs API ✅

4. **"invalid input value for enum task_type: 'SMM'"**
   - ✅ Должно быть исправлено в этом коммите
   - Если ошибка остаётся, проверьте логи на другие проблемы

---

## 🔍 Диагностика проблем

### Как проверить, что всё работает:

1. **Проверьте логи Railway:**
   - Найдите строки с `✅ Google Drive структура инициализирована`
   - Найдите строки с `✅ Google таблица таймлайна задач готова`
   - Найдите строки с `✅ Системные шаблоны созданы`

2. **Проверьте переменные окружения:**
   - `GOOGLE_DRIVE_FOLDER_ID` - должна быть ПУСТОЙ
   - `GOOGLE_CREDENTIALS_1_JSON` - должен быть валидным JSON
   - `TELEGRAM_BOT_TOKEN` - должен быть установлен

3. **Проверьте доступ к API:**
   - Откройте [Google Cloud Console](https://console.cloud.google.com/)
   - Найдите ваш проект
   - Проверьте, что включены API: Drive, Sheets, Docs

### Если файлы всё ещё не создаются:

1. **Проверьте полные логи ошибок:**
   - Скопируйте полный traceback из логов Railway
   - Найдите строку с `❌` или `ERROR`

2. **Проверьте, что инициализация запускается:**
   - В логах должна быть строка: `Environment: production`
   - Если `ENVIRONMENT` не установлен как `production`, инициализация не запустится

3. **Проверьте credentials:**
   - Убедитесь, что JSON валиден (нет лишних запятых, правильные кавычки)
   - Проверьте, что `client_email` в JSON соответствует сервисному аккаунту

---

## 📖 Дополнительная документация:

- **Работа БЕЗ Shared Drive:** [`docs/NO_SHARED_DRIVE_SETUP.md`](./NO_SHARED_DRIVE_SETUP.md)
- **Работа С Shared Drive:** [`docs/SHARED_DRIVE_SETUP.md`](./SHARED_DRIVE_SETUP.md)
- **Общая инструкция:** [`docs/DEPLOY.md`](./DEPLOY.md)

---

## ❓ Частые вопросы:

**Q: Можно ли очистить старые файлы из Shared Drive?**
A: Если у вас нет доступа к Shared Drive, вы не можете удалить файлы напрямую. Обратитесь к администратору Shared Drive для удаления. Новые файлы будут создаваться в Google Drive сервисного аккаунта.

**Q: Где находятся новые файлы?**
A: В Google Drive сервисного аккаунта. Для доступа используйте ID папки из логов или настройте автоматический sharing через `GOOGLE_DRIVE_OWNER_EMAIL`.

**Q: Можно ли передать ownership файлов?**
A: Нет, между разными доменами (сервисный аккаунт vs личный Gmail) ownership передать нельзя. Но можно настроить sharing - вы получите полные права редактирования.

**Q: Что делать, если квота закончилась?**
A: Создайте дополнительные сервисные аккаунты (каждый даёт 15 ГБ) и добавьте их credentials в `GOOGLE_CREDENTIALS_2_JSON`, `GOOGLE_CREDENTIALS_3_JSON` и т.д.
