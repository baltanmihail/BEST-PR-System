# 🔧 Переменные окружения для Railway

## Обязательные переменные

### Backend (API + Bot)

```bash
# Database
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# API
SECRET_KEY=your-secret-key-here-min-32-chars
ENVIRONMENT=production

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ADMIN_IDS=5079636941  # ID администраторов через запятую, например: 5079636941,123456789

# CORS (если фронтенд на другом домене)
CORS_ORIGINS=https://your-frontend-domain.com,https://best-pr-system.up.railway.app
```

### Frontend

```bash
VITE_API_URL=https://best-pr-system.up.railway.app/api/v1
```

---

## Как добавить переменные в Railway

### Через веб-интерфейс

1. Откройте ваш проект на Railway
2. Выберите сервис (Backend или Frontend)
3. Перейдите на вкладку **Variables**
4. Нажмите **+ New Variable**
5. Добавьте переменную и значение
6. Нажмите **Add**

### Через CLI

```bash
railway variables set TELEGRAM_ADMIN_IDS="5079636941" --service backend
railway variables set VITE_API_URL="https://best-pr-system.up.railway.app/api/v1" --service frontend
```

---

## Важные замечания

### TELEGRAM_ADMIN_IDS

- **Формат**: ID через запятую, например: `5079636941,123456789,987654321`
- **Или один ID**: `5079636941`
- **Если не задано**: вернётся пустой список (администраторы не будут определены)
- **Где используется**: 
  - Уведомления администраторам (поддержка, модерация)
  - Админ-панель
  - Специальные права доступа

### SECRET_KEY

- **Минимальная длина**: 32 символа
- **Рекомендуется**: использовать случайную строку
- **Генерация**: `openssl rand -hex 32` или онлайн-генератор

### DATABASE_URL

- Автоматически создаётся Railway при добавлении PostgreSQL
- Формат: `postgresql+asyncpg://user:password@host:port/dbname`
- Не нужно менять вручную

---

## Проверка переменных

После добавления переменных:

1. Перезапустите сервис (Railway сделает это автоматически)
2. Проверьте логи на наличие ошибок
3. Проверьте работу бота: `/start` должен работать
4. Проверьте API: `https://best-pr-system.up.railway.app/docs`

---

## Пример полного списка для Backend

```bash
DATABASE_URL=postgresql+asyncpg://...
SECRET_KEY=your-32-char-secret-key-here
ENVIRONMENT=production
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_ADMIN_IDS=5079636941
CORS_ORIGINS=https://best-pr-system.up.railway.app
LOG_LEVEL=INFO
```

---

## Пример для Frontend

```bash
VITE_API_URL=https://best-pr-system.up.railway.app/api/v1
```

---

## Безопасность

⚠️ **Никогда не коммитьте переменные окружения в Git!**

- Используйте `.env` файлы локально (они в `.gitignore`)
- На Railway используйте Variables
- Не делитесь секретными ключами
