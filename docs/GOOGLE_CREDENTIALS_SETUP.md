# 🔐 Настройка Google Credentials на Railway

## Проблема

При запуске приложения появляется ошибка:
```
ValueError: No valid Google credentials found in environment variables
```

Приложение не может запуститься, потому что `GoogleService` требует Google credentials.

---

## Решение

### 1. Подготовка credentials JSON

У тебя должно быть 5 файлов service account credentials:
- `credentials-1.json`
- `credentials-2.json`
- `credentials-3.json`
- `credentials-4.json`
- `credentials-5.json`

Каждый файл выглядит так:
```json
{
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "xxx@yyy.iam.gserviceaccount.com",
  "client_id": "...",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "..."
}
```

---

### 2. Добавление credentials на Railway

**Railway** → `best-pr-api` → **Variables** → **New Variable**

Создай 5 переменных:

#### Переменная 1:
```
GOOGLE_CREDENTIALS_1_JSON = {"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"xxx@yyy.iam.gserviceaccount.com",...}
```

**⚠️ ВАЖНО:**
- Скопируй **весь** JSON из `credentials-1.json`
- Вставь **одной строкой** (без переносов строк)
- Или замени все `\n` на реальные переносы строк в JSON (но Railway обычно работает с одной строкой)

#### Переменная 2:
```
GOOGLE_CREDENTIALS_2_JSON = {...}
```

#### Переменная 3:
```
GOOGLE_CREDENTIALS_3_JSON = {...}
```

#### Переменная 4:
```
GOOGLE_CREDENTIALS_4_JSON = {...}
```

#### Переменная 5:
```
GOOGLE_CREDENTIALS_5_JSON = {...}
```

---

### 3. Альтернативный способ (если одна строка не работает)

Если Railway не принимает JSON как одну строку, можно:

**Вариант 1: Использовать экранирование**
```
GOOGLE_CREDENTIALS_1_JSON = {"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n",...}
```

**Вариант 2: Конвертировать в одну строку**
```python
import json
with open('credentials-1.json', 'r') as f:
    data = json.load(f)
    # Сохрани как одну строку
    print(json.dumps(data))
```

---

### 4. Быстрая команда для конвертации

Если хочешь быстро скопировать JSON для Railway:

```bash
# Windows PowerShell
$json = Get-Content credentials-1.json | ConvertFrom-Json | ConvertTo-Json -Compress
$json

# Linux/Mac
cat credentials-1.json | jq -c .
```

---

### 5. Проверка после добавления

После добавления всех переменных:

1. **Перезапусти деплой:**
   - Railway Dashboard → `best-pr-api` → **Deployments**
   - Нажми **"Redeploy"** или дождись автоматического перезапуска

2. **Проверь логи:**
   - Railway Dashboard → `best-pr-api` → **View Logs**
   - Должно быть:
     ```
     ✅ Google Drive структура инициализирована: {'bot_folder_id': '...'}
     ```

3. **Если всё ещё ошибка:**
   - Проверь, что JSON валидный (можешь проверить через https://jsonlint.com/)
   - Убедись, что `private_key` содержит `\n` (если используешь одну строку) или реальные переносы
   - Проверь, что все 5 переменных добавлены

---

### 6. Дать доступ service accounts к Google Drive

После успешной инициализации:

1. **Открой папку координаторов:**
   - https://drive.google.com/drive/folders/10A2GVTrYq8_Rm6pBDvQUEQxibHFdWxBd

2. **Нажми "Share" (Поделиться)**

3. **Добавь email каждого service account:**
   - Из `credentials-1.json` → `client_email` (типа `xxx@yyy.iam.gserviceaccount.com`)
   - Права: **Editor** (Редактор)
   - Повтори для всех 5 service accounts (credentials-1.json, credentials-2.json, ...)

4. **Нажми "Send"**

---

## Минимальная настройка (если не нужен Google Drive)

Если не нужны функции Google Drive, можешь **не добавлять** credentials. Приложение запустится, но:
- ❌ Не будет загрузки файлов в Google Drive
- ❌ Не будет синхронизации с Google Sheets
- ✅ Остальные функции будут работать

**Но:** Нужно будет удалить или закомментировать импорты, использующие Google Service, если они вызывают ошибки при импорте (но мы уже сделали это - теперь это ленивая инициализация).

---

## Чеклист

- [ ] Все 5 файлов `credentials-*.json` готовы
- [ ] Добавлены все 5 переменных `GOOGLE_CREDENTIALS_*_JSON` на Railway
- [ ] JSON валидный (можно проверить через jsonlint.com)
- [ ] Перезапущен деплой на Railway
- [ ] Проверены логи - нет ошибок
- [ ] Service accounts имеют доступ к папке координаторов на Google Drive
- [ ] Проверена работа загрузки файлов (через `/support`)

---

## Если проблемы

### Ошибка: "Invalid JSON"
- Проверь JSON через jsonlint.com
- Убедись, что нет лишних кавычек или запятых

### Ошибка: "Private key invalid"
- Проверь, что `private_key` содержит весь ключ (от `-----BEGIN` до `-----END`)
- Если используешь одну строку, замени `\n` на реальные переносы или используй `\\n`

### Ошибка: "Permission denied" при загрузке файлов
- Проверь, что service accounts имеют доступ к папке координаторов
- Права должны быть **Editor**, а не Viewer

---

Готово! После добавления credentials приложение запустится без ошибок! 🎉
