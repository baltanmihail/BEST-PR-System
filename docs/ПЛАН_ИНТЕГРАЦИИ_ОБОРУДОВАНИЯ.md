# План интеграции оборудования с Google Sheets

## 📋 Анализ существующего проекта BEST Channel Bot

### Структура таблицы Google Sheets

**Таблица:** https://docs.google.com/spreadsheets/d/1gJ7muzAY00IK82QlMFRu4EaJdrwKw3nizjZ_I0nUe3s/edit

**Листы:**
1. **"Вся оборудка"** (или "Учёт оборудки")
   - Колонки: Номер | Оборудование | Фото | Статус
   - Список всего оборудования с номерами

2. **"Заявки на оборудку"**
   - Колонки: Номер заявки | Что берёт (формула) | Кто берёт | Дата выдачи | Дата возврата | Дата съёмки | Комментарий | Статус
   - Все заявки на оборудование

3. **"История оборудки"**
   - Колонки: Номер оборудования | Название | Кто брал | Дата выдачи | Дата возврата | Комментарий
   - История выдачи оборудования

4. **"Календарь занятости оборудования"**
   - Строка 1: Даты (01.01, 02.01, ...)
   - Строки 2+: Номер оборудования | Дата1 | Дата2 | ...
   - В ячейках: номер заявки или пусто (если свободно)

### Функциональность BEST Channel Bot

**Что работает:**
- ✅ Получение списка оборудования из Sheets
- ✅ Получение занятых дат из календаря
- ✅ Запись заявок в Sheets
- ✅ Обновление статусов заявок
- ✅ Запись в историю
- ✅ Кэширование данных (30 сек для календаря, 3 мин для списка оборудования)
- ✅ Rate limiting (ротация между 5 credentials)
- ✅ Обработка ошибок квоты (fallback на кэш)

**Что можно улучшить:**
- ⏳ Двусторонняя синхронизация (изменения в Sheets → БД)
- ⏳ Автоматическое обновление календаря занятости
- ⏳ Интеграция с задачами (связь заявки с задачей)
- ⏳ Улучшенное отображение занятости

## 🎯 План интеграции в BEST PR System

### Фаза 1: Базовая синхронизация (приоритет: ВЫСОКИЙ)

#### 1.1 Синхронизация оборудования из Sheets → БД
- ✅ Чтение списка оборудования из листа "Вся оборудка"
- ✅ Создание/обновление записей в БД
- ✅ Синхронизация статусов оборудования
- ⏳ Определение категории оборудования (по названию или отдельной колонке)

**Реализация:**
```python
async def sync_equipment_from_sheets(db: AsyncSession) -> dict:
    """Синхронизировать оборудование из Google Sheets в БД"""
    # Читаем лист "Вся оборудка"
    # Для каждого оборудования:
    #   - Проверяем, есть ли в БД (по номеру или названию)
    #   - Если нет - создаём
    #   - Если есть - обновляем статус
```

#### 1.2 Синхронизация заявок БД → Sheets
- ✅ Запись новой заявки в лист "Заявки на оборудку"
- ✅ Обновление статуса заявки при одобрении/отклонении
- ✅ Запись в историю при одобрении
- ⏳ Обновление календаря занятости

**Реализация:**
```python
async def sync_request_to_sheets(request: EquipmentRequest) -> dict:
    """Синхронизировать заявку в Google Sheets"""
    # Записываем в "Заявки на оборудку"
    # При одобрении:
    #   - Обновляем статус
    #   - Записываем в "История оборудки"
    #   - Обновляем календарь занятости
```

#### 1.3 Календарь занятости
- ✅ Чтение занятых дат из календаря
- ✅ Отображение доступности оборудования
- ⏳ Автоматическое обновление календаря при одобрении заявки
- ⏳ Двусторонняя синхронизация (изменения в календаре → БД)

**Структура календаря:**
```
Даты:        | 01.01 | 02.01 | 03.01 | ...
Оборудование #1 |      | REQ1  | REQ1  | ...
Оборудование #2 | REQ2 | REQ2  |       | ...
```

### Фаза 2: Улучшенная функциональность (приоритет: СРЕДНИЙ)

#### 2.1 Двусторонняя синхронизация
- ⏳ Отслеживание изменений в Sheets (polling каждые 5 минут)
- ⏳ Обновление БД при изменении статусов в Sheets
- ⏳ Обновление БД при изменении календаря занятости

#### 2.2 Интеграция с задачами
- ✅ Связь заявки с задачей (уже есть поле `task_id`)
- ⏳ Автоматическое предложение оборудования при создании задачи типа Channel
- ⏳ Отображение оборудования в карточке задачи

#### 2.3 Улучшенное отображение
- ⏳ Календарь занятости на сайте (визуализация)
- ⏳ Фильтры по категориям, статусам, датам
- ⏳ Поиск оборудования

### Фаза 3: Дополнительные функции (приоритет: НИЗКИЙ)

#### 3.1 Уведомления
- ⏳ Уведомления о новых заявках координаторам
- ⏳ Уведомления о статусе заявки пользователю
- ⏳ Напоминания о возврате оборудования

#### 3.2 Статистика
- ⏳ Статистика использования оборудования
- ⏳ Самые популярные единицы оборудования
- ⏳ Графики занятости

## 🔧 Технические детали

### Структура данных

**Оборудование в БД:**
- `id` (UUID)
- `name` (String)
- `category` (Enum: camera, lens, lighting, ...)
- `quantity` (Integer) - количество экземпляров
- `status` (Enum: available, rented, maintenance, broken)
- `specs` (JSONB) - дополнительные характеристики

**Заявка на оборудование:**
- `id` (UUID)
- `equipment_id` (UUID)
- `user_id` (UUID)
- `task_id` (UUID, опционально) - связь с задачей
- `start_date` (Date)
- `end_date` (Date)
- `status` (Enum: pending, approved, rejected, active, completed, cancelled)
- `rejection_reason` (String, опционально)

### Синхронизация с Google Sheets

**ID таблицы:** `1gJ7muzAY00IK82QlMFRu4EaJdrwKw3nizjZ_I0nUe3s`

**Методы синхронизации:**
1. **БД → Sheets** (при создании/изменении заявки):
   - Запись в "Заявки на оборудку"
   - Обновление календаря занятости
   - Запись в историю

2. **Sheets → БД** (polling каждые 5 минут):
   - Чтение изменений в "Заявки на оборудку"
   - Обновление статусов заявок в БД
   - Чтение изменений в календаре занятости
   - Обновление занятости в БД

### Ограничения API

- **60 запросов/мин на аккаунт**
- **5 аккаунтов = 300 запросов/мин**
- Использовать batch операции где возможно
- Кэширование данных (TTL: 2-3 минуты для календаря, 5 минут для списка оборудования)
- Rate limiting на каждый аккаунт отдельно

## 📝 Вопросы для уточнения

1. **Структура таблицы:**
   - Нужно ли создавать новую таблицу или использовать существующую?
   - Если использовать существующую, нужно ли её модифицировать?

2. **Календарь занятости:**
   - Как лучше отображать занятость: по датам в строке или отдельные ячейки?
   - Нужна ли визуализация на сайте (календарь с цветами)?

3. **Интеграция с задачами:**
   - Автоматически предлагать оборудование при создании задачи Channel?
   - Или только опционально при взятии задачи?

4. **Права доступа:**
   - Кто может видеть все заявки? (Координаторы + VP4PR)
   - Кто может одобрять/отклонять? (Координаторы + VP4PR)

5. **Уведомления:**
   - Нужны ли уведомления в боте о новых заявках?
   - Нужны ли напоминания о возврате оборудования?

## 🚀 План реализации

### Шаг 1: Доработка EquipmentSheetsSync
- ✅ Реализовать `sync_equipment_from_sheets()`
- ✅ Реализовать `sync_request_to_sheets()`
- ⏳ Реализовать `update_calendar()`
- ⏳ Реализовать `sync_calendar_from_sheets()`

### Шаг 2: Интеграция в API
- ✅ Добавить endpoint для синхронизации оборудования
- ✅ Интегрировать синхронизацию при создании/изменении заявки
- ⏳ Добавить endpoint для получения календаря занятости

### Шаг 3: Интеграция в бот
- ⏳ Добавить команду `/equipment` в бот
- ⏳ Добавить процесс подачи заявки через бот
- ⏳ Добавить календарь выбора дат

### Шаг 4: Интеграция на сайте
- ⏳ Страница со списком оборудования
- ⏳ Страница с календарём занятости
- ⏳ Форма подачи заявки
- ⏳ Страница моих заявок

### Шаг 5: Автоматическая синхронизация
- ⏳ Фоновая задача для синхронизации каждые 5 минут
- ⏳ Синхронизация изменений из Sheets → БД
