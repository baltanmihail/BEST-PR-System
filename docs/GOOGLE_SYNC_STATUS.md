# 📊 Статус интеграции с Google Drive/Sheets

## ✅ Что уже реализовано

### 1. Google Service (базовая интеграция)

**Файл**: `backend/app/services/google_service.py`

**Возможности**:
- ✅ Работа с Google Sheets (чтение/запись)
- ✅ Работа с Google Drive (загрузка/скачивание файлов)
- ✅ Работа с Google Docs
- ✅ Ротация 5 service accounts (для обхода лимитов API)

**Настройка**:
```bash
GOOGLE_CREDENTIALS_1_JSON={"type": "service_account", ...}
GOOGLE_CREDENTIALS_2_JSON={"type": "service_account", ...}
GOOGLE_CREDENTIALS_3_JSON={"type": "service_account", ...}
GOOGLE_CREDENTIALS_4_JSON={"type": "service_account", ...}
GOOGLE_CREDENTIALS_5_JSON={"type": "service_account", ...}

GOOGLE_SHEETS_ID=your-spreadsheet-id
GOOGLE_DRIVE_FOLDER_ID=your-folder-id
```

---

### 2. Хранение файлов в Drive

**Где используется**:
- ✅ Галерея (фото/видео выполненных задач) → `drive_id` в `files` таблице
- ✅ Предложения идей/концептов → прикрепление файлов к задачам
- ✅ Материалы задач → загрузка в Drive

**Модели**:
```python
class File(Base):
    drive_id = Column(String, nullable=False)  # Google Drive file ID
    url = Column(String, nullable=False)       # https://drive.google.com/file/d/{id}/view
```

---

## ❌ Что НЕ реализовано (пока)

### 1. Автоматическая синхронизация с Google Sheets

**Не сделано**:
- ❌ Зеркалирование таблицы задач в Google Sheets
- ❌ Зеркалирование таймлайна/календаря в Google Sheets
- ❌ Автоматическое обновление Google Sheets при создании/изменении задачи
- ❌ Синхронизация выдачи оборудования с "готовой таблицей для бота"

**Что нужно сделать**:
1. Создать Google Sheets с нужной структурой (или использовать существующую)
2. При создании задачи → добавить строку в Sheets
3. При обновлении задачи → обновить строку в Sheets
4. При выдаче оборудования → записать в отдельную таблицу

---

### 2. Таймлайн/календарь в Google Calendar

**Не сделано**:
- ❌ Создание событий в Google Calendar при дедлайнах задач
- ❌ Синхронизация календаря с задачами
- ❌ Автоматические напоминания через Google Calendar

---

## 🎯 Что делать дальше

### Вариант 1: Минимальная синхронизация (рекомендуется для начала)

**Цель**: Логирование задач и оборудования в Google Sheets

**Что сделаем**:
1. Создать/подключить Google Sheets с вкладками:
   - `Задачи` (ID, название, тип, статус, дедлайн, исполнитель, создано)
   - `Оборудование` (ID, название, пользователь, статус, даты, создано)
2. При создании задачи → добавить строку в `Задачи`
3. При выдаче оборудования → добавить строку в `Оборудование`
4. При изменении статуса → обновить соответствующую строку

**Время**: ~1-2 часа работы

---

### Вариант 2: Полная синхронизация (для продакшена)

**Цель**: Реал-тайм зеркало всех данных в Google Sheets + Calendar

**Что сделаем**:
1. Всё из Варианта 1
2. Синхронизация календаря задач с Google Calendar
3. Webhook для обновления Sheets при любых изменениях
4. Двусторонняя синхронизация (изменения в Sheets → обновления в БД)

**Время**: ~4-6 часов работы

---

## 📋 Готовая таблица для бота оборудования

**Если у тебя есть готовая структура таблицы**:
- Дай ID таблицы (или ссылку)
- Дай название листа и структуру колонок (какие поля в каком порядке)
- Я сделаю автоматическую запись/чтение из этой таблицы

---

## 🚀 Рекомендация

**Сейчас**: 
1. Сначала доведём `/public/*` до стабильности (чтобы кнопки работали)
2. Настроим фронтенд на Railway
3. Затем добавим синхронизацию с Google Sheets (Вариант 1)

**Потом** (когда система стабильна):
- Полная синхронизация (Вариант 2)
- Интеграция с Google Calendar
- Двусторонняя синхронизация

---

## Текущий статус

- ✅ Google Service готов
- ✅ Credentials могут быть загружены
- ✅ Drive интеграция работает (файлы)
- ❌ Sheets синхронизация не автоматическая
- ❌ Calendar интеграция отсутствует
