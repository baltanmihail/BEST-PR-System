# 🤖 Настройка ИИ-помощника

## Текущее состояние

ИИ-сервис сейчас работает в **режиме заглушки** (stub mode) - возвращает базовые оценки без реального AI анализа.

## Поддерживаемые провайдеры

1. **GigaChat** (российский AI)
2. **OpenAI** (GPT-4, GPT-3.5)
3. **Claude** (Anthropic)

## Настройка

### 1. Выберите провайдера

Установите переменную окружения `AI_PROVIDER`:
- `gigachat` - для GigaChat
- `openai` - для OpenAI
- `claude` - для Claude
- `none` - режим заглушки (по умолчанию)

### 2. Установите зависимости

**Для GigaChat:**
```bash
pip install gigachat
```

**Для OpenAI:**
```bash
pip install openai
```

**Для Claude:**
```bash
pip install anthropic
```

### 3. Настройте API ключи

**GigaChat:**
```bash
export GIGACHAT_API_KEY="ваш_ключ"
export AI_PROVIDER="gigachat"
```

**OpenAI:**
```bash
export OPENAI_API_KEY="sk-..."
export AI_PROVIDER="openai"
export OPENAI_MODEL="gpt-4o-mini"  # опционально, по умолчанию gpt-4o-mini
```

**Claude:**
```bash
export ANTHROPIC_API_KEY="sk-ant-..."
export AI_PROVIDER="claude"
export ANTHROPIC_MODEL="claude-3-5-sonnet-20241022"  # опционально
```

### 4. На Railway

Добавьте переменные окружения в настройках сервиса:
- `AI_PROVIDER` = `gigachat` (или `openai`, `claude`)
- `GIGACHAT_API_KEY` = ваш ключ (или `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`)

## Как это работает

1. При вызове AI endpoint сервис проверяет `AI_PROVIDER`
2. Если провайдер настроен и ключ есть - используется реальный AI
3. Если нет - возвращается заглушка с предупреждением

## Тестирование

После настройки проверьте:
```bash
POST /api/v1/ai/analyze
{
  "content": "Текст для анализа",
  "type": "text",
  "title": "Тестовый пост"
}
```

Если AI настроен правильно, вы получите реальный анализ. Если нет - заглушку с предупреждением.

## Примечания

- GigaChat может требовать дополнительную авторизацию (OAuth)
- Проверьте документацию вашего провайдера для актуальных инструкций
- При ошибках API сервис автоматически возвращает заглушку
