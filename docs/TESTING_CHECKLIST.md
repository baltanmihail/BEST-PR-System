# ✅ Чеклист для тестирования

## 🎯 Основные функции для проверки

### 1. Создание и публикация задач

**Шаги:**
1. Авторизоваться как координатор (через `/auth/telegram`)
2. Создать задачу типа Channel:
   ```json
   POST /api/v1/tasks
   {
     "title": "Тестовая задача",
     "description": "Описание",
     "type": "channel",
     "priority": "high",
     "due_date": "2026-02-20T12:00:00Z",
     "script_ready": false,
     "requires_equipment": true
   }
   ```
3. Проверить, что задача создана со статусом `draft`
4. Опубликовать задачу: `POST /api/v1/tasks/{id}/publish`
5. Проверить, что статус изменился на `open`
6. Проверить, что активные пользователи получили уведомления

**Ожидаемый результат:**
- ✅ Задача создана с этапами (Сценарий, Съёмка, Монтаж, Согласование, Публикация)
- ✅ После публикации статус = `open`
- ✅ Уведомления отправлены активным пользователям

---

### 2. Взятие задачи

**Шаги:**
1. Авторизоваться как обычный пользователь
2. Взять задачу: `POST /api/v1/tasks/{id}/assign`
3. Проверить баллы пользователя: `GET /api/v1/gamification/stats`
4. Для Channel задачи - проверить предложение оборудования

**Ожидаемый результат:**
- ✅ Задача назначена пользователю
- ✅ Начислены баллы (50/100/80 в зависимости от приоритета)
- ✅ Для Channel задачи возвращён список доступного оборудования
- ✅ Пользователь получил уведомление о назначении

---

### 3. Завершение задачи

**Шаги:**
1. Завершить задачу: `POST /api/v1/tasks/{id}/complete`
2. Проверить статистику: `GET /api/v1/gamification/stats`
3. Проверить ачивки: `GET /api/v1/gamification/achievements`
4. Проверить уведомления: `GET /api/v1/notifications`

**Ожидаемый результат:**
- ✅ Статус задачи = `completed`
- ✅ Начислены баллы за выполнение (200 + бонусы)
- ✅ Проверены и начислены ачивки (если условия выполнены)
- ✅ Пользователь получил уведомление о завершении

---

### 4. Модерация пользователей

**Шаги:**
1. Новый пользователь → `/start` в Telegram боте
2. Проверить заявку: `GET /api/v1/moderation/applications` (координатор)
3. Одобрить заявку: `POST /api/v1/moderation/applications/{id}/approve`
4. Проверить, что пользователь активирован
5. Проверить уведомление пользователю

**Ожидаемый результат:**
- ✅ Заявка создана автоматически при регистрации
- ✅ Пользователь неактивен (`is_active = false`)
- ✅ После одобрения пользователь активирован
- ✅ Пользователь получил уведомление об одобрении

---

### 5. Оборудование

**Шаги:**
1. Создать заявку: `POST /api/v1/equipment/requests`
   ```json
   {
     "equipment_id": "...",
     "start_date": "2026-02-15",
     "end_date": "2026-02-17"
   }
   ```
2. Проверить доступность: `GET /api/v1/equipment/available?start_date=...&end_date=...`
3. Координатор → одобрить: `POST /api/v1/equipment/requests/{id}/approve`
4. Проверить мои заявки: `GET /api/v1/equipment/requests`

**Ожидаемый результат:**
- ✅ Заявка создана со статусом `pending`
- ✅ Система проверяет доступность на даты
- ✅ После одобрения статус = `approved`
- ✅ Оборудование недоступно для других на эти даты

---

### 6. Геймификация

**Шаги:**
1. Выполнить несколько задач
2. Проверить статистику: `GET /api/v1/gamification/stats`
3. Проверить рейтинг: `GET /api/v1/gamification/leaderboard`
4. Проверить ачивки: `GET /api/v1/gamification/achievements`

**Ожидаемый результат:**
- ✅ Баллы начисляются корректно
- ✅ Уровень рассчитывается автоматически
- ✅ Ачивки начисляются при выполнении условий
- ✅ Рейтинг показывает ТОП-10 (координаторы исключены)

---

### 7. Уведомления

**Шаги:**
1. Проверить уведомления: `GET /api/v1/notifications`
2. Проверить счётчик: `GET /api/v1/notifications/unread-count`
3. Отметить как прочитанное: `PATCH /api/v1/notifications/{id}/read`
4. Отметить все как прочитанные: `POST /api/v1/notifications/mark-all-read`

**Ожидаемый результат:**
- ✅ Уведомления отображаются
- ✅ Счётчик непрочитанных корректный
- ✅ Можно отметить как прочитанное
- ✅ Можно отметить все сразу

---

### 8. Telegram бот

**Шаги:**
1. `/start` - проверить регистрацию
2. `/stats` - проверить статистику
3. `/tasks` - проверить список задач
4. `/leaderboard` - проверить рейтинг
5. `/equipment` - проверить заявки на оборудование
6. `/notifications` - проверить уведомления

**Ожидаемый результат:**
- ✅ Все команды работают
- ✅ Показывается правильная информация
- ✅ Обрабатываются ошибки (нет токена, неактивен и т.д.)

---

## 🐛 Известные ограничения

1. **Ачивка "Помощник"** - пока не реализована (требует логику помощи другим)
2. **Баллы за оборудование** - пока не интегрированы в завершение задач
3. **Уведомления о дедлайнах** - пока не реализованы (требует фоновые задачи)

---

## 📝 После тестирования

Если всё работает:
1. ✅ Закоммитить изменения
2. ✅ Запушить в GitHub
3. ✅ Railway автоматически задеплоит
4. ✅ Проверить логи на Railway
5. ✅ Протестировать на production

---

**Готово к тестированию! 🚀**
