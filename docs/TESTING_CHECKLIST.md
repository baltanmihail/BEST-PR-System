# ✅ Чеклист для тестирования

## 🚀 Перед запуском

### 1. Бэкенд
```powershell
cd "BEST PR System/backend"
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python run.py
```

Проверьте:
- ✅ Сервер запустился на `http://localhost:8000`
- ✅ Документация доступна на `http://localhost:8000/docs`
- ✅ Health check: `http://localhost:8000/health` возвращает `{"status": "ok"}`

### 2. Фронтенд
```powershell
cd "BEST PR System/frontend"
npm install
npm run dev
```

Проверьте:
- ✅ Сервер запустился (обычно `http://localhost:5173`)
- ✅ Страница открывается без ошибок в консоли

---

## 📋 Тестирование функций

### Публичные endpoints (без авторизации)

#### 1. Публичные задачи
- [ ] Откройте `http://localhost:5173/tasks` (или через главную)
- [ ] Видны задачи (даже без авторизации)
- [ ] Можно посмотреть детали задачи

**API тест:**
```bash
GET http://localhost:8000/api/v1/public/tasks
```

#### 2. Рейтинг
- [ ] Откройте `/leaderboard`
- [ ] Виден список пользователей с баллами
- [ ] Сортировка по баллам работает

**API тест:**
```bash
GET http://localhost:8000/api/v1/public/leaderboard
```

#### 3. Статистика
- [ ] Откройте `/stats`
- [ ] Видна общая статистика системы

**API тест:**
```bash
GET http://localhost:8000/api/v1/public/stats
```

### Регистрация

#### 4. Регистрация нового пользователя
- [ ] Откройте форму регистрации (если есть) или используйте Telegram авторизацию
- [ ] Заполните данные
- [ ] Принимается согласие на обработку данных
- [ ] Принимается пользовательское соглашение
- [ ] После регистрации пользователь получает токен
- [ ] Пользователь создаётся с `is_active = false`

**API тест:**
```bash
POST http://localhost:8000/api/v1/registration/register
{
  "telegram_auth": {...},
  "personal_data_consent": {"consent": true},
  "user_agreement": {"accepted": true, "version": "1.0"}
}
```

#### 5. Получение пользовательского соглашения
- [ ] Endpoint возвращает текст соглашения

**API тест:**
```bash
GET http://localhost:8000/api/v1/registration/agreement
```

### Поддержка

#### 6. Создание запроса в поддержку
- [ ] Откройте `/support`
- [ ] Заполните форму
- [ ] Отправьте запрос
- [ ] Получено подтверждение
- [ ] Админы получили уведомление

**API тест:**
```bash
POST http://localhost:8000/api/v1/support/request
{
  "message": "Тестовый запрос",
  "contact": "@testuser",
  "category": "technical"
}
```

### Уведомления (требует авторизации)

#### 7. Просмотр уведомлений
- [ ] Откройте `/notifications`
- [ ] Видны все уведомления
- [ ] Фильтры работают (все/непрочитанные/важные)
- [ ] Можно отметить как прочитанное
- [ ] Можно отметить все как прочитанные

**API тест:**
```bash
GET http://localhost:8000/api/v1/notifications
Authorization: Bearer <token>
```

### Активность

#### 8. Лента активности
- [ ] Откройте `/activity`
- [ ] Видны последние события
- [ ] События отображаются корректно

**API тест:**
```bash
GET http://localhost:8000/api/v1/activity/feed?limit=20&days=7
```

### Галерея

#### 9. Галерея результатов
- [ ] Откройте `/gallery`
- [ ] Видны выполненные задачи с файлами
- [ ] Можно посмотреть детали задачи

**API тест:**
```bash
GET http://localhost:8000/api/v1/gallery
```

### Главная страница

#### 10. Разный контент для разных ролей
- [ ] **Неавторизованный:** Видит мотивирующие сообщения, кнопку регистрации
- [ ] **Зарегистрированный:** Видит персонализированное приветствие, свои задачи
- [ ] **Координатор:** Видит панель координатора, статистику участников
- [ ] **VP4PR:** Видит полную панель управления

#### 11. Мотивирующий чат
- [ ] Кнопка чата в правом нижнем углу
- [ ] Чат открывается
- [ ] Сообщения меняются автоматически
- [ ] Разные сообщения для разных ролей

### Задачи

#### 12. Просмотр задач
- [ ] Откройте `/tasks`
- [ ] Видны все задачи
- [ ] Можно посмотреть детали
- [ ] Фильтры работают

#### 13. Взятие задачи (требует авторизации)
- [ ] Нажмите "Взять задачу"
- [ ] Задача назначена на пользователя
- [ ] Получены баллы за взятие
- [ ] Создана запись в activity_log

**API тест:**
```bash
POST http://localhost:8000/api/v1/tasks/{task_id}/assign
Authorization: Bearer <token>
```

#### 14. Завершение задачи
- [ ] Нажмите "Завершить задачу"
- [ ] Задача помечена как выполненная
- [ ] Получены баллы за выполнение
- [ ] Создана запись в activity_log

**API тест:**
```bash
POST http://localhost:8000/api/v1/tasks/{task_id}/complete
Authorization: Bearer <token>
```

### Предложения концептов/идей (требует авторизации)

#### 15. Создание предложения
- [ ] Откройте задачу
- [ ] Создайте предложение (концепт/идею/сценарий)
- [ ] Прикрепите файлы (если нужно)
- [ ] Предложение сохранено
- [ ] Координаторы получили уведомление

**API тест:**
```bash
POST http://localhost:8000/api/v1/tasks/{task_id}/suggestions
Authorization: Bearer <token>
{
  "type": "concept",
  "title": "Тестовый концепт",
  "content": "Описание концепта",
  "files": ["drive_id_1", "drive_id_2"]
}
```

#### 16. Просмотр предложений
- [ ] Видны все предложения по задаче
- [ ] Можно фильтровать по статусу

**API тест:**
```bash
GET http://localhost:8000/api/v1/tasks/{task_id}/suggestions
Authorization: Bearer <token>
```

### ИИ-помощник (только для координаторов)

#### 17. Анализ контента
- [ ] Откройте ИИ-помощник (если есть UI)
- [ ] Проанализируйте текст/концепт
- [ ] Получен анализ (заглушка или реальный AI)

**API тест:**
```bash
POST http://localhost:8000/api/v1/ai/analyze
Authorization: Bearer <coordinator_token>
{
  "content": "Текст для анализа",
  "type": "text",
  "title": "Тестовый пост"
}
```

### Модерация (только для координаторов)

#### 18. Просмотр заявок на модерацию
- [ ] Откройте панель модерации
- [ ] Видны заявки на регистрацию
- [ ] Можно одобрить/отклонить

**API тест:**
```bash
GET http://localhost:8000/api/v1/moderation/applications
Authorization: Bearer <coordinator_token>
```

---

## 🐛 Проверка ошибок

### Консоль браузера
- [ ] Нет ошибок в консоли (F12)
- [ ] Нет 404 ошибок для ресурсов
- [ ] Нет ошибок CORS

### Сеть
- [ ] Все API запросы возвращают 200/201
- [ ] Нет 401 (неавторизован) для публичных endpoints
- [ ] Нет 500 (ошибка сервера)

### Логи бэкенда
- [ ] Нет ошибок в логах сервера
- [ ] Миграции выполнены успешно
- [ ] Подключение к БД работает

---

## 📝 Быстрый тест через Swagger

1. Откройте `http://localhost:8000/docs`
2. Протестируйте основные endpoints:
   - `GET /api/v1/public/tasks` - публичные задачи
   - `GET /api/v1/public/leaderboard` - рейтинг
   - `GET /api/v1/public/stats` - статистика
   - `POST /api/v1/support/request` - поддержка
   - `GET /api/v1/activity/feed` - активность
   - `GET /api/v1/gallery` - галерея

---

## ✅ После тестирования

Если всё работает:
1. ✅ Закоммитьте изменения
2. ✅ Задеплойте на Railway
3. ✅ Протестируйте на production

Если есть ошибки:
1. ❌ Зафиксируйте ошибку
2. ❌ Проверьте логи
3. ❌ Исправьте и протестируйте снова

---

**Удачи с тестированием! 🚀**
