# 🔧 Критические исправления регистрации (09.01.2025)

## Проблемы

1. **Пользователь видит "✅ Сессия успешно запущена" для незарегистрированного пользователя**
   - Причина: API `/auth/qr/confirm` проверял только существование пользователя, но не проверял `is_active`
   - Если пользователь существовал, но был неактивен (`is_active = False`), API возвращал `is_registration: False`
   - Бот показывал сообщение о входе вместо предложения регистрации

2. **Ошибки при загрузке публичных данных (задачи, статистика)**
   - Недостаточное логирование ошибок
   - Пользователь не понимал, что происходит

## Исправления

### 1. Backend API (`backend/app/api/qr_auth.py`)

**Исправлено:** `confirm_qr` - проверка пользователя

**Было:**
```python
user_result = await db.execute(
    select(User).where(User.telegram_id == request.telegram_id)
)
user = user_result.scalar_one_or_none()
```

**Стало:**
```python
user_result = await db.execute(
    select(User).where(
        User.telegram_id == request.telegram_id,
        User.is_active == True,  # ВАЖНО: только активные пользователи считаются зарегистрированными
        User.deleted_at.is_(None)  # Исключаем удалённых пользователей
    )
)
user = user_result.scalar_one_or_none()
```

**Результат:**
- Если пользователь не существует ИЛИ не активен → `is_registration: True`
- Если пользователь существует И активен → `is_registration: False`

### 2. Бот (`backend/bot/handlers.py`)

**Добавлено логирование:**
- В `callback_qr_confirm` добавлено логирование для отладки:
  ```python
  logger.info(f"QR confirm for user {user.id}: is_registration={is_registration}, response keys={list(response.keys())}")
  ```

- В `callback_view_tasks` и `callback_view_stats` добавлено детальное логирование ошибок:
  ```python
  logger.error(f"Error loading public tasks: {response.get('error')}, status_code: {response.get('status_code')}")
  ```

## Теперь работает так:

1. **Незарегистрированный пользователь сканирует QR:**
   - API проверяет: пользователь существует И активен?
   - Если НЕТ → `is_registration: True`
   - Бот показывает: "QR-код подтверждён! Вы ещё не зарегистрированы..."
   - Предлагает зарегистрироваться в боте или на сайте

2. **Зарегистрированный пользователь сканирует QR:**
   - API проверяет: пользователь существует И активен?
   - Если ДА → `is_registration: False`
   - Бот показывает: "✅ Сессия успешно запущена на устройстве"
   - Автоматический вход на сайт

## Проверка

1. Убедитесь, что пользователь НЕ существует в базе ИЛИ `is_active = False`
2. Отсканируйте QR-код через бота
3. **Ожидаемое поведение:**
   - Бот: "QR-код подтверждён! Вы ещё не зарегистрированы..."
   - Кнопки: "Зарегистрироваться в боте" и "Зарегистрироваться на сайте"
   - НЕ показывается "✅ Сессия успешно запущена"

4. Зарегистрируйте пользователя через бота или сайт
5. После регистрации и активации (`is_active = True`):
   - При следующем сканировании QR → "✅ Сессия успешно запущена"

---

**Дата исправления:** 2025-01-09
