# 🤖 Развёртывание Telegram бота на Railway

## 📋 Варианты запуска

### ✅ Вариант 1: Отдельный сервис на Railway (рекомендуется для production)

Это создаст отдельный сервис, который будет работать параллельно с API.

### ⚠️ Вариант 2: Вместе с API (не рекомендуется, но проще)

Бот и API работают в одном процессе.

---

## 🚀 Пошаговая инструкция (Вариант 1: Отдельный сервис)

### Шаг 1: Подготовка

1. Убедитесь, что у вас есть:
   - ✅ Аккаунт на Railway
   - ✅ Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
   - ✅ API уже работает на Railway

#### Как получить Telegram Bot Token:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям (имя бота, username)
4. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

### Шаг 2: Создание нового сервиса

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите ваш проект **"BEST PR System"**
3. Нажмите **"+ New"** (в правом верхнем углу)
4. Выберите **"GitHub Repo"**
5. Выберите тот же репозиторий, что и для API
6. Railway создаст новый сервис

---

### Шаг 3: Настройка переменных окружения

1. В новом сервисе откройте **"Variables"**
2. Добавьте следующие переменные:

#### Обязательные:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
API_URL=https://best-pr-system.up.railway.app
DATABASE_URL=postgresql+asyncpg://... (та же БД, что у API)
```

#### Как получить DATABASE_URL:
1. Откройте ваш **PostgreSQL сервис** в Railway
2. Перейдите в **"Variables"**
3. Скопируйте `DATABASE_URL`
4. Замените `postgresql://` на `postgresql+asyncpg://`
5. Вставьте в переменные бота

#### Опциональные (если нужны):
```
ENVIRONMENT=production
LOG_LEVEL=INFO
```

---

### Шаг 4: Настройка Start Command (ВАЖНО!)

**Проблема:** Railway использует `Procfile`, который перезаписывает Start Command.

**Решение:**

1. Откройте ваш новый сервис (бот) в Railway
2. Перейдите в **"Settings"** (вкладка сверху)
3. Прокрутите до **"Deploy"**
4. Найдите **"Override Start Command"** (или просто **"Start Command"**)
5. **Включите переключатель "Override"** (если есть)
6. Введите команду: `cd backend && python bot/main.py`
7. Нажмите **"Save"**

**⚠️ ВАЖНО:** 
- Убедитесь, что переключатель "Override" включён
- Или удалите/переименуйте `Procfile` временно (но это повлияет на API сервис)
- Лучше использовать явный Start Command с Override

**Альтернатива:** Если Railway всё равно использует Procfile:
1. В настройках сервиса найдите **"Nixpacks"** или **"Build Settings"**
2. Убедитесь, что **"Detect"** не установлен на "Procfile"
3. Или создайте файл `.railwayignore` с `Procfile` (но это может сломать API)

---

### Шаг 5: Настройка деплоя

1. В Railway Dashboard откройте сервис бота
2. Перейдите в **"Settings"**
3. Убедитесь, что:
   - **Root Directory:** не указан (или `BEST PR System`)
   - **Build Command:** не нужен (Python автоматически)
   - **Start Command:** `cd backend && python bot/main.py`

---

### Шаг 6: Деплой

1. Railway автоматически начнёт деплой при создании сервиса
2. Или нажмите **"Deploy"** → **"Redeploy"**
3. Дождитесь завершения деплоя (~2-3 минуты)

---

### Шаг 7: Проверка

1. Откройте **"View Logs"** в сервисе бота
2. Должны быть сообщения:
   ```
   🤖 Telegram бот запускается...
   INFO: Application startup complete
   ```

3. Проверьте бота в Telegram:
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Должен ответить приветствием

---

## 🔧 Альтернативный вариант: Вместе с API (Вариант 2)

Если хотите запустить бота в том же сервисе, что и API:

### Шаги:

1. Откройте ваш **API сервис** в Railway Dashboard
2. Перейдите в **"Settings"** → **"Deploy"**
3. Найдите **"Start Command"**
4. Измените на:
   ```
   cd backend && python -m alembic upgrade head && python run_with_bot.py
   ```
5. Или измените `Procfile`:
   ```
   web: cd backend && python -m alembic upgrade head && python run_with_bot.py
   ```
6. Сохраните и перезапустите сервис

⚠️ **Внимание:** 
- Это запустит API и бота в одном процессе
- Может быть нестабильно для production
- Если один упадёт, упадёт и другой
- **Рекомендуется использовать отдельный сервис (Вариант 1)**

---

## 📝 Структура файлов

```
BEST PR System/
├── Procfile              # Для API
├── Procfile.bot          # Для бота (если используете)
├── backend/
│   ├── bot/
│   │   ├── main.py       # Точка входа бота
│   │   └── handlers.py    # Обработчики команд
│   └── ...
```

---

## 🐛 Решение проблем

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"

**Решение:**
1. Проверьте переменные окружения в Railway
2. Убедитесь, что токен скопирован полностью (без пробелов)
3. Перезапустите сервис

### Проблема: "API error: Connection refused"

**Решение:**
1. Проверьте `API_URL` - должен быть `https://best-pr-system.up.railway.app`
2. Убедитесь, что API сервис работает
3. Проверьте, что API доступен по этому URL

### Проблема: "Database connection failed"

**Решение:**
1. Убедитесь, что `DATABASE_URL` правильный (с `+asyncpg`)
2. Проверьте, что это та же БД, что у API
3. Убедитесь, что PostgreSQL сервис работает

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи в Railway → **"View Logs"**
2. Убедитесь, что бот запущен (нет ошибок)
3. Проверьте токен в [@BotFather](https://t.me/BotFather) - может быть отозван
4. Попробуйте перезапустить сервис

---

## ✅ Чеклист

- [ ] Создан новый сервис в Railway
- [ ] Подключён тот же GitHub репозиторий
- [ ] Установлены переменные окружения:
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `API_URL`
  - [ ] `DATABASE_URL`
- [ ] Настроен Start Command: `cd backend && python bot/main.py`
- [ ] Деплой завершён успешно
- [ ] В логах нет ошибок
- [ ] Бот отвечает на `/start` в Telegram

---

## 🎯 После запуска

1. Протестируйте команды:
   - `/start` - регистрация
   - `/stats` - статистика
   - `/tasks` - задачи
   - `/leaderboard` - рейтинг
   - `/equipment` - оборудование
   - `/notifications` - уведомления

2. Проверьте интеграцию:
   - Создайте задачу через API
   - Возьмите задачу через бота (если будет реализовано)
   - Проверьте уведомления

---

## 📊 Мониторинг

### Логи бота:
- Railway Dashboard → Сервис бота → **"View Logs"**
- Видите все сообщения, ошибки, API запросы

### Метрики:
- Railway Dashboard → Сервис бота → **"Metrics"**
- CPU, Memory, Network usage

---

## 🔄 Обновление бота

При обновлении кода:
1. Закоммитьте изменения в Git
2. Запушьте в GitHub
3. Railway автоматически перезапустит бота

Или вручную:
- Railway Dashboard → Сервис бота → **"Deploy"** → **"Redeploy"**

---

**Готово! Теперь ваш Telegram бот работает на Railway! 🚀**
