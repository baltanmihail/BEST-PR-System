# Инструкция по использованию системы управления оборудованием

## 🚀 Быстрый старт

### 1. Автоматические процессы

Система работает автоматически:
- ✅ При создании заявки - автоматически обновляется календарь
- ✅ При одобрении заявки - обновляется календарь + отправляется уведомление
- ✅ При отклонении заявки - отправляется уведомление с причиной
- ✅ Каждые 6 часов - обновляются статусы оборудования и проверяются напоминания

### 2. Ручной запуск синхронизации

Для координаторов доступны endpoints для ручного запуска:

```bash
# Синхронизация календаря занятости
POST /api/v1/equipment/calendar/sync
Authorization: Bearer <coordinator_token>

# Обновление статусов оборудования
POST /api/v1/equipment/statuses/sync
Authorization: Bearer <coordinator_token>

# Двусторонняя синхронизация (таблица → БД)
POST /api/v1/equipment/sync/bidirectional
Authorization: Bearer <coordinator_token>
```

## 📋 Структура календаря

Календарь занятости оборудования находится в листе **"Календарь занятости оборудования"** основной таблицы оборудования.

### Формат:
- **Строка 1:** Период (например, "Январь 2025 - Июнь 2025")
- **Строка 2:** Числа дней (01, 02, 03, ...)
- **Строка 3:** Дни недели (Пн, Вт, Ср, ...)
- **Строка 4:** Пустая
- **Строки 5+:** Номер оборудования | Дата1 | Дата2 | ...

### Цвета:
- 🔴 **Красный** - одобренные заявки (по датам взятия и возврата)
- 🟡 **Жёлтый** - заявки на рассмотрении
- ⚪ **Белый** - свободные даты
- ❌ Отклонённые заявки не отображаются

### Номера заявок:
Номер заявки ставится в ячейках:
- Дата выдачи (начало периода)
- Дата возврата (конец периода)
- Дата съёмки (если указана)

## 🔄 Автоматическое обновление статусов

Статусы оборудования обновляются автоматически на основе дат:

1. **На складе** - если дата возврата прошла или дата выдачи ещё не наступила
2. **Занят, съёмка предстоит** - если период аренды активен и дата съёмки ещё не прошла
3. **Занят, съёмка окончена** - если период аренды активен и дата съёмки прошла

### Обновляемые поля:
- Статус оборудования
- Дата выдачи (из ближайшей одобренной заявки)
- Дата возврата (из ближайшей одобренной заявки)
- "У кого сейчас?" (из заявки)

## 🔔 Напоминания

Напоминания отправляются автоматически:
- За **1 день до выдачи** оборудования (пользователю и координаторам)
- За **1 день до возврата** оборудования (пользователю и координаторам)

### Получатели:
- Пользователь, взявший оборудование
- Координаторы PR-FR
- VP4PR

## 📢 Уведомления

Уведомления отправляются при изменении статуса заявки:

1. **Одобрение** - "Ваша заявка одобрена!"
2. **Отклонение** - "Ваша заявка отклонена" (с причиной)
3. **Возврат на рассмотрение** - "Ваша заявка находится на рассмотрении"
4. **Восстановление** - "Ваша заявка восстановлена"

## 🔧 Настройка

### Переменные окружения

```bash
# ID таблицы с оборудованием (опционально, будет найден автоматически)
GOOGLE_EQUIPMENT_SHEETS_ID=...

# ID таблицы с таймлайном занятости (опционально)
GOOGLE_EQUIPMENT_TIMELINE_SHEETS_ID=...
```

### Периодические задачи

Периодические задачи запускаются автоматически при старте приложения:
- Первый запуск через 1 час после старта
- Затем каждые 6 часов

Можно изменить интервалы в `main.py`:
```python
await asyncio.sleep(6 * 60 * 60)  # 6 часов
```

## 📝 Логирование

Все операции логируются:
- ✅ Успешные операции - уровень INFO
- ⚠️ Предупреждения - уровень WARNING
- ❌ Ошибки - уровень ERROR с полной трассировкой

Примеры логов:
```
✅ Календарь занятости оборудования обновлён
✅ Обновлено статусов оборудования: 5, дат: 3, 'У кого сейчас?': 2
✅ Отправлено 3 напоминаний
✅ Отправлено 2 уведомлений об изменении статусов
```

## 🐛 Отладка

### Проверка работы календаря

1. Создайте заявку на оборудование
2. Одобрите заявку через API
3. Проверьте календарь в Google Sheets - должны появиться красные ячейки

### Проверка статусов

1. Запустите синхронизацию статусов:
   ```bash
   POST /api/v1/equipment/statuses/sync
   ```
2. Проверьте лист "Вся оборудка" - статусы должны обновиться

### Проверка напоминаний

Напоминания проверяются автоматически каждые 6 часов. Для тестирования можно временно изменить интервал в `main.py`.

## ✅ Готово к использованию!

Все компоненты реализованы и интегрированы. Система готова к работе!
