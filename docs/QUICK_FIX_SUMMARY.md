# ✅ Быстрая сводка исправлений

## Что сделано прямо сейчас

### 1. Ссылки в боте теперь указывают на FRONTEND
- Добавлена переменная `FRONTEND_URL` в `config.py`
- Все ссылки в боте используют `settings.FRONTEND_URL` вместо хардкода бэкенда
- После настройки фронтенда на Railway пользователи будут видеть **сайт**, а не JSON

### 2. Исправлены ошибки с TaskStatus ENUM
- Создан `TaskStatusType(TypeDecorator)` в `models/task.py`
- Все запросы в `api/public.py` используют `.value` для enum
- Это должно исправить ошибки "type taskstatus does not exist"

### 3. Приветствие бота улучшено
- Жирный текст для важных строк (`<b>...</b>`)
- Указание позиции/роли (VP4PR, координатор, участник)
- Кликабельные ссылки с эмодзи 🔗
- При повторном `/start` активным пользователям **не отправляется лого**

---

## Что нужно сделать на Railway

### Вариант 1: Переименовать домены (рекомендуется)

**Backend** (текущий `best-pr-system`):
1. Settings → Networking → Custom Domain
2. Generate: `best-pr-api` → получишь `best-pr-api.up.railway.app`

**Frontend** (новый сервис):
1. New Service → Deploy from GitHub → `BEST PR System/frontend`
2. Settings → Networking → Custom Domain
3. Generate: `best-pr-system` → получишь `best-pr-system.up.railway.app`

**Переменные**:
```bash
# Backend:
FRONTEND_URL=https://best-pr-system.up.railway.app

# Frontend:
VITE_API_URL=https://best-pr-api.up.railway.app/api/v1
```

---

### Вариант 2: Оставить как есть (проще)

**Backend**: `best-pr-system.up.railway.app` (API)  
**Frontend**: новый сервис → дефолтный Railway домен (например, `bestpr-production-xxxx.up.railway.app`)

**Переменные**:
```bash
# Backend:
FRONTEND_URL=https://bestpr-production-xxxx.up.railway.app

# Frontend:
VITE_API_URL=https://best-pr-system.up.railway.app/api/v1
```

---

## Почему инлайн-кнопки не работают

Кнопки обращаются к `/api/v1/public/*` которые возвращают 500 из-за ошибок с TaskStatus.

**Исправлено**:
- ✅ `TaskStatusType` создан
- ✅ Запросы используют `.value`
- ✅ После редеплоя бэкенда кнопки должны заработать

---

## Google Drive/Sheets статус

- ✅ **Google Service готов** (Drive/Sheets/Docs)
- ✅ **5 service accounts** поддерживается
- ✅ **Файлы в Drive** работают (галерея, материалы)
- ❌ **Автосинхронизация с Google Sheets** не реализована (можем добавить следующим шагом)
- ❌ **Таймлайн в Google Calendar** не реализован

Подробности → `GOOGLE_SYNC_STATUS.md`

---

## Следующие шаги

1. **Настроить фронтенд на Railway** (см. `FRONTEND_URL_SETUP.md`)
2. **Добавить переменную** `FRONTEND_URL` на Railway
3. **Протестировать** `/start` в боте → должен открываться **сайт**, а не JSON
4. **Проверить инлайн-кнопки** → должны работать без ошибок

Если всё заработает → можем добавить Google Sheets синхронизацию.
