# ✅ Проверка перед коммитом

## 🔍 Обязательные проверки

### 1. TypeScript ошибки

**Перед каждым коммитом проверяй:**

```bash
cd frontend
npm run build
```

**Частые ошибки:**
- `TS6133: 'variable' is declared but its value is never read` - неиспользуемая переменная
- `TS2307: Cannot find module` - неправильный импорт
- `TS2322: Type 'X' is not assignable to type 'Y'` - несоответствие типов

**Решение:**
- Удалить неиспользуемые переменные/импорты
- Исправить импорты
- Исправить типы

### 2. Проверка линтера

```bash
cd frontend
npm run lint
```

### 3. Проверка форматирования

```bash
cd frontend
npm run format
```

---

## 🚨 Частые проблемы

### Проблема: Неиспользуемые переменные

**Пример:**
```typescript
const autoRegister = urlParams.get('auto_register') === 'true'  // ❌ Не используется
```

**Решение:**
```typescript
// Просто удалить строку
```

### Проблема: Неиспользуемые импорты

**Пример:**
```typescript
import { Link } from 'react-router-dom'  // ❌ Не используется
```

**Решение:**
```typescript
// Удалить импорт
```

---

## 📝 Чеклист перед коммитом

- [ ] `npm run build` проходит без ошибок
- [ ] `npm run lint` проходит без ошибок
- [ ] Все неиспользуемые переменные удалены
- [ ] Все неиспользуемые импорты удалены
- [ ] Типы корректны

---

**ВАЖНО:** Всегда проверяй `npm run build` перед коммитом!
