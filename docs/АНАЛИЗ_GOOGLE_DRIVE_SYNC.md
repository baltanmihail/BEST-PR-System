# Анализ текущей реализации Google Drive и Google Sheets синхронизации

## 1. Что означает сообщение о GOOGLE_DRIVE_FOLDER_ID?

**Сообщение в логах:**
```
💡 Установите GOOGLE_DRIVE_FOLDER_ID=11A1lVlBkHKLjGtrEmndS__5MOqNDqEJn для использования этой папки
```

**Объяснение:**
- При первом запуске система автоматически создаёт структуру папок в Google Drive (если credentials настроены)
- Создаётся главная папка "BEST PR System" в указанной корневой папке (`1Zxtqs4otBMhltOFCJG0-y8gBHWXvQGzI`)
- Система выводит ID созданной папки и предлагает сохранить его в переменную окружения `GOOGLE_DRIVE_FOLDER_ID`
- **Нужно ли добавлять?** **ДА, рекомендуется!** Это ускорит работу системы, так как ей не нужно будет каждый раз искать папку по имени

**Как добавить:**
1. Скопируйте ID из логов: `11A1lVlBkHKLjGtrEmndS__5MOqNDqEJn`
2. Добавьте в переменные окружения (Railway/Heroku/.env):
   ```
   GOOGLE_DRIVE_FOLDER_ID=11A1lVlBkHKLjGtrEmndS__5MOqNDqEJn
   ```
3. Перезапустите приложение

---

## 2. Текущая реализация Google Drive интеграции

### ✅ Что реализовано:

1. **Базовая структура папок** (`drive_structure.py`):
   - Автоматическое создание структуры при первом запуске:
     - `BEST PR System/` (главная папка)
     - `Tasks/` (папка для задач)
     - `Gallery/` (галерея проектов)
     - `Equipment/` (оборудование BEST Channel)
     - `Support/` (файлы поддержки)
     - `Users/` (профили пользователей)
   - Создание папок для задач при создании задачи:
     - `Tasks/{task_id}_{task_name}/`
     - `Tasks/{task_id}_{task_name}/materials/`
     - `Tasks/{task_id}_{task_name}/final/`
     - `Tasks/{task_id}_{task_name}/drafts/`

2. **GoogleService** (`google_service.py`):
   - Ротация между 5 credentials для увеличения квоты (300 запросов/мин вместо 60)
   - Rate limiting (60 запросов/мин на каждый credential)
   - Кэширование метаданных файлов
   - Методы для работы с Google Drive:
     - `create_folder()` - создание папок
     - `upload_file()` - загрузка файлов
     - `list_files()` - список файлов
     - `delete_file()` - удаление файлов
     - `get_file_metadata()` - метаданные файла
   - Методы для работы с Google Sheets:
     - `read_sheet()` - чтение данных
     - `write_sheet()` - запись данных
     - `append_to_sheet()` - добавление данных

3. **API endpoints** (`api/drive.py`):
   - `POST /drive/folders` - создание папки
   - `POST /drive/files/upload` - загрузка файла
   - `GET /drive/files` - список файлов
   - `DELETE /drive/files/{file_id}` - удаление файла
   - `GET /drive/files/{file_id}/metadata` - метаданные файла
   - `POST /drive/initialize` - инициализация структуры

4. **Интеграция с задачами** (`task_service.py`):
   - Автоматическое создание папок при создании задачи (в фоне)

### ❌ Что НЕ реализовано:

1. **Двусторонняя синхронизация Google Drive ↔ System:**
   - ❌ Отслеживание изменений на Google Drive (создание/изменение/удаление файлов)
   - ❌ Автоматическое обновление БД при изменениях на Drive
   - ❌ Синхронизация изменений из системы обратно в Drive (обновление файлов ТЗ, метаданных)
   - ❌ Webhooks или polling для отслеживания изменений

2. **Полная синхронизация Google Sheets:**
   - ❌ Создание Google Sheets документа с таймлайнами (есть только заглушка)
   - ❌ Создание листов: "Общий", "SMM", "Design", "Channel", "PR-FR"
   - ❌ Заполнение таблицы задачами с цветовой кодировкой
   - ❌ Гиперссылки из ячеек на карточки задач
   - ❌ Отображение этапов задач в таблице
   - ❌ Отображение бронирования оборудования

3. **Синхронизация изменений из Google Sheets обратно в систему:**
   - ❌ Отслеживание изменений в Google Sheets (изменение дедлайнов, статусов)
   - ❌ Обновление БД при ручных правках в таблице
   - ❌ Создание задач из таблицы

4. **Дополнительные функции:**
   - ❌ Автоматическое создание файлов ТЗ для задач в Google Docs
   - ❌ Синхронизация материалов задач (фото, видео, тексты)
   - ❌ Интеграция с галереей проектов

---

## 3. Требования из документации

### Из `Архитектура.md` и `Правки по архитектуре.md`:

1. **Двусторонняя синхронизация:**
   - Изменения в системе → Google Drive (автоматически)
   - Изменения на Google Drive → система (автоматически)
   - Изменения в системе → Google Sheets (автоматически)
   - Изменения в Google Sheets → система (автоматически)

2. **Google Sheets с таймлайнами:**
   - Структура листов: "Общий", "SMM", "Design", "Channel", "PR-FR"
   - Формат: весь семестр с группировкой по месяцам
   - Цветовая кодировка по типам задач и статусам
   - Гиперссылки из ячеек на карточки задач (ссылки на сайт)
   - Отображение этапов задач
   - Отображение бронирования оборудования

3. **Отслеживание изменений:**
   - Webhooks (предпочтительно) или polling каждые 5 минут
   - Обработка изменений файлов на Drive
   - Обработка изменений в Sheets (дедлайны, статусы)

4. **Ограничения:**
   - Не более 60 запросов в минуту на один credential (уже учтено в GoogleService)
   - Батчинг запросов для избежания превышения лимитов

---

## 4. План реализации

### Фаза 1: Двусторонняя синхронизация Google Drive (приоритет: ВЫСОКИЙ)

1. **Сервис отслеживания изменений на Drive:**
   - Реализовать polling каждые 5 минут для отслеживания изменений
   - Хранить timestamp последней проверки для каждой папки
   - Обнаруживать новые файлы, изменённые файлы, удалённые файлы
   - Обновлять БД при обнаружении изменений

2. **Синхронизация изменений из системы в Drive:**
   - При создании/изменении задачи → обновить файлы ТЗ в Drive
   - При изменении дедлайна → обновить метаданные в Drive
   - При загрузке материалов → сохранить в папку задачи

3. **Обработка конфликтов:**
   - Приоритет: изменения в системе имеют приоритет над изменениями на Drive (или наоборот, настраиваемо)

### Фаза 2: Полная синхронизация Google Sheets (приоритет: ВЫСОКИЙ)

1. **Создание Google Sheets документа:**
   - Создать документ "BEST PR System - Таймлайны" в корневой папке
   - Создать листы: "Общий", "SMM", "Design", "Channel", "PR-FR"
   - Сохранить ID документа в настройках

2. **Заполнение таблицы задачами:**
   - Структура: дни месяца в строках, задачи в ячейках
   - Цветовая кодировка по типам задач (SMM=зелёный, Design=синий, Channel=оранжевый, PR-FR=фиолетовый)
   - Гиперссылки на карточки задач (ссылки на сайт)
   - Отображение этапов задач в отдельных строках
   - Отображение бронирования оборудования

3. **Автоматическое обновление:**
   - При создании/изменении задачи → обновить таблицу
   - При изменении дедлайна → обновить ячейки
   - При изменении статуса → обновить цвет ячеек

### Фаза 3: Синхронизация изменений из Google Sheets обратно в систему (приоритет: СРЕДНИЙ)

1. **Отслеживание изменений в Sheets:**
   - Polling каждые 5 минут для проверки изменений
   - Обнаруживать изменения дедлайнов, статусов, создание задач

2. **Обновление БД:**
   - При изменении дедлайна в таблице → обновить задачу в БД
   - При изменении статуса → обновить статус задачи
   - При создании задачи в таблице → создать задачу в БД (опционально)

### Фаза 4: Дополнительные функции (приоритет: НИЗКИЙ)

1. **Автоматическое создание файлов ТЗ:**
   - При создании задачи → создать Google Doc с ТЗ
   - Разные ТЗ для разных ролей (SMM, Design, Channel, PR-FR)

2. **Синхронизация материалов:**
   - Автоматическое отображение файлов из папки задачи в системе
   - Загрузка материалов через бота/сайт → сохранение в Drive

3. **Интеграция с галереей:**
   - Автоматическое добавление финальных работ в галерею
   - Синхронизация галереи с папкой Gallery на Drive

---

## 5. Технические детали реализации

### 5.1 Отслеживание изменений на Google Drive

**Метод: Polling (каждые 5 минут)**

```python
# Псевдокод
class DriveSyncService:
    async def sync_drive_changes(self):
        # Для каждой папки задачи
        for task_folder in task_folders:
            # Получить список файлов с modifiedTime > last_sync_time
            changed_files = drive_service.list_files(
                folder_id=task_folder.drive_id,
                modified_after=task_folder.last_sync_time
            )
            
            # Обновить БД для каждого изменённого файла
            for file in changed_files:
                await self.update_file_in_db(file)
            
            # Обновить timestamp последней синхронизации
            task_folder.last_sync_time = now()
```

### 5.2 Создание Google Sheets документа

**Структура таблицы:**

```
Лист "Общий":
| Дата | Задача 1 | Задача 2 | ... |
|------|---------|---------|-----|
| 01.01 | [Ссылка] | [Ссылка] | ... |
| 02.01 | ...     | ...     | ... |

Лист "SMM":
| Дата | Задача SMM 1 | Задача SMM 2 | ... |
|------|-------------|-------------|-----|
| 01.01 | [Ссылка]    | [Ссылка]    | ... |
```

**Цветовая кодировка:**
- SMM = зелёный (#4CAF50)
- Design = синий (#2196F3)
- Channel = оранжевый (#FF9800)
- PR-FR = фиолетовый (#9C27B0)

**Гиперссылки:**
- Формула: `=HYPERLINK("https://best-pr-system.up.railway.app/tasks/{task_id}", "Название задачи")`

### 5.3 Отслеживание изменений в Google Sheets

**Метод: Polling (каждые 5 минут)**

```python
# Псевдокод
class SheetsSyncService:
    async def sync_sheets_changes(self):
        # Читать данные из таблицы
        sheet_data = sheets_service.read_sheet("Общий!A1:Z100")
        
        # Сравнить с данными в БД
        for row in sheet_data:
            task_id = extract_task_id_from_hyperlink(row[1])
            new_deadline = parse_date(row[0])
            
            # Если дедлайн изменился
            if task.deadline != new_deadline:
                await self.update_task_deadline(task_id, new_deadline)
```

---

## 6. Рекомендации

1. **Начать с Фазы 1** (двусторонняя синхронизация Drive) - это основа для всего остального
2. **Затем Фаза 2** (синхронизация Sheets) - это критично для пользователя
3. **Фаза 3** (синхронизация из Sheets обратно) - можно реализовать позже
4. **Фаза 4** (дополнительные функции) - по мере необходимости

**Важно:**
- Учитывать rate limits (60 запросов/мин на credential)
- Использовать батчинг для множественных операций
- Логировать все операции синхронизации
- Обрабатывать ошибки gracefully (не падать при ошибках API)
