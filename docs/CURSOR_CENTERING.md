# 🎯 Исправление центрирования курсора

## Проблема

Курсор смещён влево-вниз от места нажатия мыши.

## Текущая реализация

Курсор использует `transform: translate(-50%, -50%)` для центрирования:
```typescript
style={{
  width: '40px',
  height: '40px',
  transform: 'translate(-50%, -50%)', // Центрируем точно на позиции мыши
}}
```

И позиционируется так:
```typescript
containerRef.current.style.left = `${currentPos.current.x}px`
containerRef.current.style.top = `${currentPos.current.y}px`
```

## Решение

Если курсор всё ещё смещён, попробуй:

### Вариант 1: Увеличить размер контейнера
```typescript
// В Cursor2.tsx, в стиле контейнера:
style={{
  width: '40px',
  height: '40px',
  transform: 'translate(-50%, -50%)',
  // Добавь для более точного центрирования:
  marginLeft: 0,
  marginTop: 0,
}}
```

### Вариант 2: Изменить способ позиционирования (если нужно)
```typescript
// Вместо translate(-50%, -50%) использовать прямое вычисление:
if (containerRef.current) {
  const offsetX = 20 // половина ширины (40px / 2)
  const offsetY = 20 // половина высоты (40px / 2)
  containerRef.current.style.left = `${currentPos.current.x - offsetX}px`
  containerRef.current.style.top = `${currentPos.current.y - offsetY}px`
  // И убрать transform из стиля
}
```

### Вариант 3: Проверить модель курсора
Возможно, модель курсора сама имеет смещение. Проверь:
- Масштаб модели (`model.scale.set(0.35, 0.35, 0.35)`)
- Поворот модели (`model.rotation.set(0, 5.4, 0)`)
- Центр модели (возможно, нужно `model.position.set(0, 0, 0)`)

---

## Проверка

1. Открой сайт на десктопе
2. Наведи курсор на разные элементы
3. Проверь, что курсор точно следует за мышью
4. Если смещён — используй Вариант 2 (прямое вычисление)

---

## Если ничего не помогает

Можно временно отключить кастомный курсор:
```typescript
// В Cursor2.tsx
if (!enabled || isMobile) return null
// Или просто:
return null // Отключить кастомный курсор
```
