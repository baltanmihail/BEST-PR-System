# 🧪 Руководство по тестированию API

## ✅ Статус: Всё работает!

Ваш API доступен по адресу: **https://best-pr-system.up.railway.app/**

---

## 📋 Проверка базовых endpoints

### 1. Корневой endpoint
```
GET https://best-pr-system.up.railway.app/
```
**Ожидаемый ответ:**
```json
{
  "message": "BEST PR System API",
  "version": "0.1.0",
  "docs": "/docs",
  "api_prefix": "/api/v1"
}
```
✅ **Статус:** Работает (200 OK)

---

### 2. Health check
```
GET https://best-pr-system.up.railway.app/health
```
**Ожидаемый ответ:**
```json
{
  "status": "ok"
}
```
✅ **Статус:** Работает (200 OK)

---

### 3. Документация Swagger UI
```
GET https://best-pr-system.up.railway.app/docs
```
✅ **Статус:** Работает! Открывается интерактивная документация API

---

### 4. Альтернативная документация (ReDoc)
```
GET https://best-pr-system.up.railway.app/redoc
```
✅ **Статус:** Должна работать (альтернативный формат документации)

---

## 🔐 Тестирование API endpoints

### Шаг 1: Авторизация через Telegram

1. Откройте документацию: https://best-pr-system.up.railway.app/docs
2. Найдите endpoint `POST /api/v1/auth/telegram`
3. Нажмите "Try it out"
4. Вставьте данные от Telegram Web App:
   ```json
   {
     "id": 5079636941,
     "first_name": "Ваше Имя",
     "last_name": "Ваша Фамилия",
     "username": "ваш_username",
     "auth_date": 1704470400,
     "hash": "ваш_hash_от_telegram"
   }
   ```
5. Нажмите "Execute"
6. Скопируйте `access_token` из ответа

---

### Шаг 2: Авторизация в Swagger UI

1. В Swagger UI нажмите кнопку **"Authorize"** (справа вверху)
2. Вставьте токен в формате: `Bearer ваш_токен`
3. Нажмите "Authorize"
4. Теперь вы можете тестировать защищённые endpoints

---

### Шаг 3: Тестирование календаря

#### Месячный вид календаря
```
GET /api/v1/calendar?view=month&start_date=2026-01-01
```
**Параметры:**
- `view`: `month` (обязательно)
- `start_date`: Дата в формате `YYYY-MM-DD` (обязательно)
- `task_type`: `smm` | `design` | `channel` | `prfr` (опционально)

**Пример запроса:**
```
GET /api/v1/calendar?view=month&start_date=2026-01-01
```

**Ожидаемый ответ:**
```json
{
  "view": "month",
  "month": 1,
  "year": 2026,
  "first_day": "2026-01-01",
  "last_day": "2026-01-31",
  "days": [...],
  "events": [...]
}
```

---

#### Недельный вид календаря
```
GET /api/v1/calendar?view=week&start_date=2026-01-15
```
**Параметры:**
- `view`: `week` (обязательно)
- `start_date`: Дата в формате `YYYY-MM-DD` (обязательно)

**Ожидаемый ответ:**
```json
{
  "view": "week",
  "start_date": "2026-01-13",
  "end_date": "2026-01-19",
  "days": [...]
}
```

---

#### Таймлайн (горизонтальная шкала)
```
GET /api/v1/calendar?view=timeline&start_date=2026-01-01&end_date=2026-07-01
```
**Параметры:**
- `view`: `timeline` (обязательно)
- `start_date`: Дата начала в формате `YYYY-MM-DD` (обязательно)
- `end_date`: Дата окончания в формате `YYYY-MM-DD` (обязательно)

**Ожидаемый ответ:**
```json
{
  "view": "timeline",
  "start_date": "2026-01-01",
  "end_date": "2026-07-01",
  "items": [...]
}
```

---

### Шаг 4: Тестирование endpoints для задач

#### Получить список задач
```
GET /api/v1/tasks
```
**Параметры:**
- `skip` (опционально): количество пропущенных записей (по умолчанию 0)
- `limit` (опционально): количество записей (по умолчанию 100, максимум 100)
- `task_type` (опционально): фильтр по типу (`smm`, `design`, `channel`, `prfr`)
- `status` (опционально): фильтр по статусу (`draft`, `open`, `assigned`, и т.д.)
- `priority` (опционально): фильтр по приоритету (`low`, `medium`, `high`, `critical`)

**Пример запроса:**
```
GET /api/v1/tasks?task_type=smm&status=open&limit=10
```

---

#### Получить детали задачи
```
GET /api/v1/tasks/{task_id}
```
**Параметры:**
- `task_id` (в пути): UUID задачи

**Пример запроса:**
```
GET /api/v1/tasks/123e4567-e89b-12d3-a456-426614174000
```

---

#### Создать задачу (только координаторы)
```
POST /api/v1/tasks
```
**Требуется:** Роль координатора или VP4PR

**Тело запроса (обычная задача):**
```json
{
  "title": "Создать пост для Instagram",
  "description": "Нужно создать пост о мероприятии",
  "type": "smm",
  "priority": "high",
  "due_date": "2026-01-15T12:00:00Z"
}
```

**Тело запроса (Channel задача с автоматическими этапами):**
```json
{
  "title": "Промо-ролик для BIP",
  "description": "Создать промо-ролик для презентации BEST",
  "type": "channel",
  "priority": "high",
  "due_date": "2026-02-20T12:00:00Z",
  "script_ready": false,
  "requires_equipment": true
}
```

**Результат:** Автоматически создадутся этапы:
- "Сценарий" (если `script_ready: false`) - 5 дней, зелёный
- "Съёмка" - 2 дня, зелёный
- "Монтаж" - 3 дня, зелёный
- "Согласование" - 1 день, жёлтый
- "Публикация" - в день дедлайна, красный

---

#### Обновить задачу
```
PATCH /api/v1/tasks/{task_id}
```
**Тело запроса (все поля опциональны):**
```json
{
  "title": "Обновлённое название",
  "status": "in_progress",
  "priority": "critical"
}
```

---

#### Взять задачу (с автоматическим предложением оборудования)
```
POST /api/v1/tasks/{task_id}/assign
```
**Доступно:** Всем авторизованным пользователям

**Для задач типа Channel:**
- Автоматически предлагает доступное оборудование на даты съёмки
- Возвращает список предложений в ответе

**Ожидаемый ответ:**
```json
{
  "task": {...},
  "equipment_suggestions": [
    {
      "id": "...",
      "name": "Камера Sony A7III",
      "category": "camera",
      "available_dates": {
        "start": "2026-02-16",
        "end": "2026-02-17"
      }
    }
  ]
}
```

---

#### Удалить задачу
```
DELETE /api/v1/tasks/{task_id}
```
**Требуется:** Создатель задачи или VP4PR

---

## 📊 Проверка логов

### HTTP логи (в Railway Dashboard)

Вы должны видеть:
- ✅ `GET /` → 200 OK
- ✅ `GET /health` → 200 OK
- ✅ `GET /docs` → 200 OK
- ⚠️ `GET /favicon.ico` → 404 (это нормально, не критично)

### Deploy логи

Должны быть:
- ✅ `Application startup complete`
- ✅ `Uvicorn running on http://0.0.0.0:8080`
- ✅ `BEST PR System API starting up...`
- ✅ `Environment: production`

---

## 🐛 Решение проблем

### Проблема: "Not Found" на `/docs`

**Решение:**
1. Проверьте, что приложение запущено (смотрите deploy logs)
2. Убедитесь, что вы используете правильный URL: `https://best-pr-system.up.railway.app/docs`
3. Попробуйте очистить кэш браузера (Ctrl+Shift+R)

---

### Проблема: "401 Unauthorized"

**Причина:** Не авторизованы или токен истёк

**Решение:**
1. Получите новый токен через `/api/v1/auth/telegram`
2. Обновите токен в Swagger UI (кнопка "Authorize")

---

### Проблема: "403 Forbidden"

**Причина:** Недостаточно прав (например, пытаетесь создать задачу без роли координатора)

**Решение:**
1. Проверьте вашу роль в базе данных
2. Обратитесь к VP4PR для изменения роли

---

### Проблема: "404 Not Found" на `/favicon.ico`

**Это нормально!** FastAPI не предоставляет favicon по умолчанию. Это не влияет на работу API.

Если хотите добавить favicon:
1. Добавьте файл `favicon.ico` в папку `backend/app/static/`
2. Настройте статические файлы в `main.py`

---

## ✅ Чеклист тестирования

### Базовые endpoints
- [x] Корневой endpoint `/` работает
- [x] Health check `/health` работает
- [x] Test endpoint `/test` работает
- [x] Документация `/docs` открывается

### Новые endpoints (требуют тестирования)
- [ ] Календарь `/api/v1/calendar` (месяц/неделя/таймлайн)
- [ ] Авторизация через Telegram работает
- [ ] Получение списка задач работает
- [ ] Получение деталей задачи работает
- [ ] Создание задачи с этапами работает (для координаторов)
- [ ] Автоматическое предложение оборудования при взятии Channel задачи
- [ ] Обновление задачи работает
- [ ] Удаление задачи работает (для создателя/VP4PR)

---

## 🎯 Следующие шаги

1. **Протестируйте авторизацию:**
   - Откройте `/docs`
   - Попробуйте авторизоваться через Telegram
   - Получите токен

2. **Протестируйте endpoints:**
   - Получите список задач
   - Создайте тестовую задачу (если вы координатор)
   - Обновите задачу

3. **Проверьте фильтрацию:**
   - Попробуйте фильтры по типу, статусу, приоритету
   - Проверьте пагинацию

4. **Продолжайте разработку:**
   - День 4: Расширение функционала задач (назначения, этапы)
   - День 5: Интеграция с Telegram ботом
   - И так далее...

---

## 📝 Полезные ссылки

- **API документация:** https://best-pr-system.up.railway.app/docs
- **ReDoc:** https://best-pr-system.up.railway.app/redoc
- **Health check:** https://best-pr-system.up.railway.app/health
- **Railway Dashboard:** https://railway.app/dashboard

---

## 💡 Советы

1. **Используйте Swagger UI** для тестирования - это самый удобный способ
2. **Сохраняйте токены** в безопасном месте (не коммитьте в Git!)
3. **Проверяйте логи** в Railway Dashboard при проблемах
4. **Используйте фильтры** для поиска нужных задач

---

**Всё готово к тестированию! 🚀**

