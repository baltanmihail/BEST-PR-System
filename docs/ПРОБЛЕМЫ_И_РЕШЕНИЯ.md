# Проблемы и решения по Google Sheets/Drive интеграции

## 🔴 Обнаруженные проблемы

### 1. Таблица таймлайна не создаётся при старте
**Проблема:** В `main.py` строка 139 создавался `SheetsSyncService()` без обязательного параметра `google_service`.

**Решение:** Исправлено - теперь передаётся `GoogleService()`:
```python
from app.services.google_service import GoogleService
google_service = GoogleService()
sheets_sync = SheetsSyncService(google_service)
```

### 2. Папки для шаблонов создаются, но шаблоны не сохраняются
**Проблема:** Код создания папок есть в `drive_structure.py`, но шаблоны не сохраняются на Drive при создании.

**Решение:** Добавлен код сохранения шаблонов в `create_system_templates.py` (строки 284-298).

### 3. Таблица таймлайна создаётся пустой
**Проблема:** Таблица создаётся, но не заполняется данными при старте.

**Решение:** Нужно добавить автоматическую синхронизацию при первом создании таблицы или вызвать endpoint синхронизации.

## ✅ Что реализовано

### Google Sheets таймлайн
- ✅ Создание таблицы "BEST PR System - Таймлайны"
- ✅ Создание листов: "Общий", "SMM", "Design", "Channel", "PR-FR"
- ✅ Гиперссылки на задачи (формат: `=HYPERLINK("URL"; "Название")`)
- ✅ Цветовая кодировка по типам координаторов:
  - SMM: зелёный (#4CAF50)
  - Design: синий (#2196F3)
  - Channel: оранжевый (#FF9800)
  - PR-FR: фиолетовый (#9C27B0)
- ✅ Цвета статусов задач
- ✅ Цвета этапов (из `status_color`)
- ✅ Отображение дедлайнов
- ✅ Отображение этапов с эмодзи статусов

### Google Drive структура
- ✅ Папка "Task Templates" создаётся при инициализации
- ✅ Подпапки для координаторов: SMM, Design, Channel, PR-FR, VP4PR
- ✅ Сохранение шаблонов в JSON формате на Drive

### Системные шаблоны
- ✅ Создание системных шаблонов при первом запуске
- ✅ Сохранение шаблонов на Google Drive в соответствующие папки

## ⚠️ Что нужно проверить

1. **Вызывается ли синхронизация таймлайна?**
   - Endpoint: `POST /api/v1/calendar/sync/sheets`
   - Нужно вызвать после создания таблицы или настроить автоматическую синхронизацию

2. **Создаются ли шаблоны?**
   - Проверить логи: `📋 Создание системных шаблонов задач...`
   - Проверить БД: таблица `task_templates`

3. **Сохраняются ли шаблоны на Drive?**
   - Проверить логи: `✅ Сохранён шаблон '{name}' на Google Drive: {file_id}`
   - Проверить папки на Google Drive

## 🔧 Рекомендации

1. **Добавить автоматическую синхронизацию таймлайна:**
   - При первом создании таблицы вызвать `sync_calendar_to_sheets_async`
   - Или настроить периодическую синхронизацию (например, раз в день)

2. **Улучшить логирование:**
   - Добавить больше логов при создании таблицы
   - Логировать URL созданной таблицы

3. **Добавить проверку существования:**
   - Проверять, существует ли таблица перед созданием
   - Проверять, созданы ли папки перед сохранением шаблонов
