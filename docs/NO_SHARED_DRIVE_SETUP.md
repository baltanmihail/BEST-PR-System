# Настройка без Shared Drive (для работы с обычным Google Drive)

## 📋 Когда использовать эту инструкцию

Используйте эту инструкцию, если:
- ❌ У вас **нет доступа** к Shared Drive (Team Drive) организации
- ❌ Вы не можете получить права "Content Manager" или "Manager" на Shared Drive
- ✅ Вы хотите использовать **обычный Google Drive сервисного аккаунта**

## ⚠️ Важные отличия от Shared Drive

### Квота хранилища
- **Shared Drive**: Файлы используют квоту организации (общая квота для всех)
- **Обычный Google Drive**: Файлы используют квоту сервисного аккаунта (обычно 15 ГБ на аккаунт)

### Ownership файлов
- **Shared Drive**: Можно передавать ownership между аккаунтами одного домена
- **Обычный Google Drive**: Ownership передать между разными доменами **невозможно** (ограничение Google Drive API)
  - Пользователь получит только права "Редактор" (Editor)
  - Файлы останутся собственностью сервисного аккаунта

### Расположение файлов
- **Shared Drive**: Файлы находятся в организационном хранилище
- **Обычный Google Drive**: Файлы находятся в Google Drive сервисного аккаунта
  - Для доступа нужно поделиться файлами/папками вручную с нужными пользователями
  - Или настроить автоматический sharing при создании файлов

## 🚀 Быстрая настройка

### Шаг 1: Очистка переменных окружения в Railway

В Railway Dashboard для backend сервиса:

1. **Очистите или удалите `GOOGLE_DRIVE_FOLDER_ID`:**
   ```env
   # Оставьте ПУСТЫМ или удалите переменную полностью
   GOOGLE_DRIVE_FOLDER_ID=
   ```

2. **Оставьте или установите `GOOGLE_DRIVE_OWNER_EMAIL` (опционально):**
   ```env
   # Можно оставить пустым - ownership не будет передаваться
   # Или укажите email для автоматического sharing (но ownership не передастся)
   GOOGLE_DRIVE_OWNER_EMAIL=
   # или
   GOOGLE_DRIVE_OWNER_EMAIL=baltanmihail@gmail.com
   ```

3. **Убедитесь, что Google Credentials настроены:**
   ```env
   GOOGLE_CREDENTIALS_1_JSON={"type":"service_account",...}
   ```

### Шаг 2: Проверка сервисного аккаунта

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Найдите проект с сервисным аккаунтом
3. Убедитесь, что включены следующие API:
   - ✅ Google Drive API
   - ✅ Google Sheets API
   - ✅ Google Docs API

4. **Важно**: Проверьте квоту Google Drive сервисного аккаунта:
   - По умолчанию: **15 ГБ** на сервисный аккаунт
   - При необходимости создайте дополнительные сервисные аккаунты и используйте ротацию credentials

### Шаг 3: Деплой

```bash
cd "BEST PR System"
git add .
git commit -m "Настройка для работы без Shared Drive"
git push origin main
```

## ✅ Проверка после деплоя

1. **Проверьте логи в Railway:**
   - ✅ `✅ ROOT_FOLDER_ID ... недоступен (возможно, нет доступа к Shared Drive)`
   - ✅ `📁 Создаём папку в корне Google Drive сервисного аккаунта вместо Shared Drive...`
   - ✅ `✅ Папка создана в корне Google Drive: [folder_id]`
   - ✅ `✅ Google Drive структура инициализирована`

2. **Доступ к файлам:**
   - Файлы будут созданы в Google Drive сервисного аккаунта
   - Для доступа к файлам нужно поделиться папкой "BEST PR System" с нужными пользователями
   - Или настроить автоматический sharing (см. ниже)

## 🔧 Настройка автоматического sharing

Если вы хотите, чтобы файлы автоматически делились с пользователями при создании:

1. **Укажите `GOOGLE_DRIVE_OWNER_EMAIL`** в переменных окружения:
   ```env
   GOOGLE_DRIVE_OWNER_EMAIL=baltanmihail@gmail.com
   ```
   
2. Система автоматически даст пользователю права "Редактор" (Editor) на все создаваемые файлы
3. **Важно**: Ownership останется у сервисного аккаунта, но пользователь сможет редактировать

## 📂 Где найти файлы

### Через Google Cloud Console

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Перейдите в **IAM & Admin** → **Service Accounts**
3. Найдите ваш сервисный аккаунт (email заканчивается на `@...iam.gserviceaccount.com`)
4. Нажмите на email аккаунта
5. Перейдите на вкладку **Permissions** (если доступно)

### Через Google Drive API

1. Используйте [Google Drive API Explorer](https://developers.google.com/drive/api/v3/reference/files/list)
2. Авторизуйтесь с credentials сервисного аккаунта
3. Выполните запрос `files.list` с фильтром `name='BEST PR System'`

### Через скрипт Python

Создайте временный скрипт для получения ID папки:

```python
from app.services.drive_structure import DriveStructureService

service = DriveStructureService()
folder_id = service.get_bot_folder_id()
print(f"Папка бота: {folder_id}")
print(f"Ссылка: https://drive.google.com/drive/folders/{folder_id}")
```

## 🔄 Миграция с Shared Drive на обычный Google Drive

Если вы ранее использовали Shared Drive и хотите перейти:

1. **Экспорт данных (если есть доступ):**
   - Скачайте важные файлы из старой папки в Shared Drive
   - Сохраните ID важных документов (Google Sheets, Google Docs)

2. **Очистка переменных окружения:**
   - Удалите или очистите `GOOGLE_DRIVE_FOLDER_ID`
   - Оставьте `GOOGLE_DRIVE_OWNER_EMAIL` пустым или укажите новый email

3. **Деплой:**
   - Система автоматически создаст новую структуру в корне Google Drive сервисного аккаунта

4. **Импорт данных (если нужно):**
   - Загрузите файлы в новую папку через Google Drive веб-интерфейс
   - Или используйте скрипты для массовой загрузки

## ❓ FAQ

**Q: Можно ли использовать несколько сервисных аккаунтов для увеличения квоты?**
A: Да! Используйте ротацию credentials (GOOGLE_CREDENTIALS_1_JSON, GOOGLE_CREDENTIALS_2_JSON и т.д.). Каждый аккаунт даёт 15 ГБ.

**Q: Как поделиться папкой с командой?**
A: 
1. Найдите папку "BEST PR System" в Google Drive сервисного аккаунта (через API или временный скрипт)
2. Используйте Google Drive веб-интерфейс для sharing (нужны credentials сервисного аккаунта)
3. Или укажите `GOOGLE_DRIVE_OWNER_EMAIL` - файлы будут автоматически делиться с этим пользователем

**Q: Можно ли передать ownership файлов пользователю?**
A: **Нет**, если сервисный аккаунт и пользователь в разных доменах. Google Drive API не позволяет передавать ownership между разными доменами. Пользователь получит только права "Редактор".

**Q: Что делать, если квота сервисного аккаунта закончилась?**
A:
1. Создайте дополнительные сервисные аккаунты
2. Добавьте их credentials в переменные окружения (GOOGLE_CREDENTIALS_2_JSON, GOOGLE_CREDENTIALS_3_JSON и т.д.)
3. Система автоматически будет использовать ротацию credentials

**Q: Где хранятся файлы, если я не могу получить доступ к Shared Drive?**
A: Файлы хранятся в Google Drive сервисного аккаунта. Для доступа нужно либо использовать API, либо настроить sharing через `GOOGLE_DRIVE_OWNER_EMAIL`.

**Q: Можно ли очистить старые файлы из Shared Drive?**
A: Если у вас нет доступа к Shared Drive, вы не сможете удалить файлы напрямую. Обратитесь к администратору Shared Drive для удаления старых файлов. Новые файлы будут создаваться в Google Drive сервисного аккаунта.

## 🔐 Безопасность

- ✅ Сервисный аккаунт имеет ограниченные права (только доступ к Drive/Sheets/Docs API)
- ✅ Файлы изолированы в Google Drive сервисного аккаунта
- ⚠️ Не публикуйте credentials сервисного аккаунта публично
- ⚠️ Регулярно проверяйте использование квоты и активности аккаунта
