# ⚡ Быстрое решение: Запуск бота вместе с API

## Проблема
Railway использует `Procfile`, который перезаписывает Start Command. Бот не запускается отдельно.

## ✅ Решение: Запустить бота вместе с API

### Шаг 1: Изменить Procfile

Файл `Procfile` уже изменён на:
```
web: cd backend && python -m alembic upgrade head && python run_with_bot.py
```

### Шаг 2: Добавить переменную окружения

1. Откройте ваш **API сервис** в Railway Dashboard
2. Перейдите в **"Variables"**
3. Добавьте:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
   ```
4. Сохраните

### Шаг 3: Перезапустить сервис

1. Railway Dashboard → API сервис → **"Deploy"** → **"Redeploy"**
2. Дождитесь завершения (~2-3 минуты)

### Шаг 4: Проверка

1. Откройте **"View Logs"** в API сервисе
2. Должны быть сообщения:
   ```
   🚀 Starting API server on 0.0.0.0:8080
   🤖 Starting Telegram bot...
   ```

3. Проверьте бота в Telegram:
   - Откройте бота → `/start`
   - Должен ответить!

---

## ✅ Готово!

Теперь и API, и бот работают в одном сервисе.

⚠️ **Примечание:** Это менее надёжно, чем отдельный сервис, но точно работает и проще в настройке.

---

## 🔄 Если хотите отдельный сервис позже

См. `docs/TELEGRAM_BOT_TROUBLESHOOTING.md` для решения проблемы с Override Start Command.
