# 🚀 Быстрый деплой и тестирование на Railway

## ✅ Быстрый старт

### Шаг 1: Закоммитить изменения

```powershell
# Перейдите в корневую папку проекта
cd "BEST PR System"

# Проверьте, что изменено
git status

# Добавьте все изменения
git add .

# Создайте коммит
git commit -m "Больше админов"

# Отправьте в GitHub
git push origin main
```

### Шаг 2: Railway автоматически задеплоит

Railway отслеживает изменения в репозитории и автоматически:
1. ✅ Обнаружит новый коммит
2. ✅ Пересоберёт проект
3. ✅ Запустит миграции Alembic
4. ✅ Запустит сервер

**Время деплоя:** ~2-3 минуты

---

## 🔍 Проверка деплоя

### 1. Проверьте логи в Railway Dashboard

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите ваш проект `BEST-PR-System`
3. Перейдите во вкладку **"Deployments"**
4. Откройте последний деплой
5. Проверьте логи - должны быть:
   ```
   ✅ Application startup complete
   ✅ Uvicorn running on http://0.0.0.0:8080
   ✅ BEST PR System API starting up...
   ```

### 2. Проверьте базовые endpoints

#### Health check:
```
GET https://best-pr-system.up.railway.app/health
```

#### Тестовый endpoint:
```
GET https://best-pr-system.up.railway.app/test
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "API работает!",
  "endpoints": {
    "docs": "/docs",
    "health": "/health",
    "api_prefix": "/api/v1"
  }
}
```

#### Документация:
```
https://best-pr-system.up.railway.app/docs
```

---

## 🧪 Тестирование новых endpoints

### 1. Календарь API

#### Месячный вид:
```
GET https://best-pr-system.up.railway.app/api/v1/calendar?view=month&start_date=2026-01-01
```

#### Недельный вид:
```
GET https://best-pr-system.up.railway.app/api/v1/calendar?view=week&start_date=2026-01-15
```

#### Таймлайн:
```
GET https://best-pr-system.up.railway.app/api/v1/calendar?view=timeline&start_date=2026-01-01&end_date=2026-07-01
```

**Параметры:**
- `view`: `month` | `week` | `timeline` (обязательно)
- `start_date`: дата в формате `YYYY-MM-DD` (обязательно для timeline)
- `end_date`: дата в формате `YYYY-MM-DD` (обязательно для timeline)
- `task_type`: `smm` | `design` | `channel` | `prfr` (опционально)

### 2. Создание задачи с этапами (Channel)

#### С готовым сценарием:
```json
POST https://best-pr-system.up.railway.app/api/v1/tasks
Authorization: Bearer YOUR_TOKEN

{
  "title": "Промо-ролик для BIP",
  "description": "Создать промо-ролик для презентации BEST",
  "type": "channel",
  "priority": "high",
  "due_date": "2026-02-20T12:00:00Z",
  "script_ready": true,
  "requires_equipment": true
}
```

#### С разработкой сценария:
```json
POST https://best-pr-system.up.railway.app/api/v1/tasks
Authorization: Bearer YOUR_TOKEN

{
  "title": "Промо-ролик для TW",
  "description": "Создать промо-ролик для TW",
  "type": "channel",
  "priority": "high",
  "due_date": "2026-03-14T12:00:00Z",
  "script_ready": false,
  "requires_equipment": true
}
```

**Результат:** Автоматически создадутся этапы:
- Если `script_ready: false` → этап "Сценарий" (5 дней)
- "Съёмка" (2 дня)
- "Монтаж" (3 дня)
- "Согласование" (1 день, жёлтый)
- "Публикация" (в день дедлайна, красный)

### 3. Взятие задачи (с автоматическим предложением оборудования)

```
POST https://best-pr-system.up.railway.app/api/v1/tasks/{task_id}/assign
Authorization: Bearer YOUR_TOKEN
```

**Результат:** Для задач типа `channel` автоматически вернётся список доступного оборудования на даты съёмки.

---

## 🤖 Telegram бот (опционально)

**ВАЖНО:** Бот запускается отдельно. Если хотите запустить бота на Railway:

### Вариант 1: Отдельный сервис (рекомендуется)

1. В Railway Dashboard создайте **новый сервис**
2. Выберите **"GitHub Repo"** и тот же репозиторий
3. Настройте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `API_URL=https://best-pr-system.up.railway.app`
   - `DATABASE_URL` (та же БД)
4. Создайте отдельный `Procfile.bot`:
   ```
   worker: cd backend && python bot/main.py
   ```

### Вариант 2: Вместе с API (не рекомендуется для production)

Измените `Procfile`:
```
web: cd backend && python -m alembic upgrade head && python run_with_bot.py
```

⚠️ **Внимание:** Это запустит API и бота в одном процессе, что может быть нестабильно.

---

## 📊 Мониторинг

### Логи в реальном времени:

1. Railway Dashboard → Ваш сервис → **"View Logs"**
2. Видите все запросы, ошибки, вывод приложения

### HTTP логи:

Railway Dashboard → Ваш сервис → **"Metrics"** → **"HTTP"**

Там вы увидите:
- Количество запросов
- Время ответа
- Статус коды (200, 404, 500, etc.)

---

## 🐛 Решение проблем

### Проблема: "Build failed"

**Причина:** Ошибки в коде или зависимости

**Решение:**
1. Проверьте логи деплоя
2. Убедитесь, что `requirements.txt` содержит все зависимости
3. Проверьте синтаксис Python кода

### Проблема: "Application error" или 502

**Причина:** Приложение упало при старте

**Решение:**
1. Проверьте логи → **"View Logs"**
2. Убедитесь, что все переменные окружения установлены
3. Проверьте `DATABASE_URL` (должен быть `postgresql+asyncpg://...`)

### Проблема: "Database connection failed"

**Причина:** Неправильный `DATABASE_URL` или БД недоступна

**Решение:**
1. Railway Dashboard → PostgreSQL → **"Variables"**
2. Скопируйте реальный `DATABASE_URL`
3. Замените `postgresql://` на `postgresql+asyncpg://`
4. Обновите переменную в вашем сервисе

### Проблема: Миграции не выполняются

**Решение:**
1. Проверьте логи деплоя (там должны быть логи Alembic)
2. Или выполните вручную:
   ```
   Railway Dashboard → Service → "Deployments" → "View Logs"
   ```
   Или через Railway CLI:
   ```bash
   railway run alembic upgrade head
   ```

---

## ✅ Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Код отправлен в GitHub (`git push`)
- [ ] Переменные окружения настроены в Railway
- [ ] `DATABASE_URL` правильный (с `+asyncpg`)
- [ ] `TELEGRAM_BOT_TOKEN` установлен (если нужен бот)
- [ ] Проверены логи после деплоя
- [ ] `/test` endpoint работает
- [ ] `/docs` открывается

---

## 🎯 Быстрое тестирование

### 1. После деплоя сразу проверьте:

```bash
# Health check
curl https://best-pr-system.up.railway.app/health

# Test endpoint
curl https://best-pr-system.up.railway.app/test

# Docs
# Откройте в браузере: https://best-pr-system.up.railway.app/docs
```

### 2. Протестируйте через Swagger UI:

1. Откройте `https://best-pr-system.up.railway.app/docs`
2. Найдите endpoint `/api/v1/calendar`
3. Нажмите "Try it out"
4. Заполните параметры:
   - `view`: `month`
   - `start_date`: `2026-01-01`
5. Нажмите "Execute"
6. Проверьте результат

---

## 📝 Полезные команды

### Проверка статуса деплоя:
```bash
# Через Railway CLI (если установлен)
railway status

# Или через веб-интерфейс
# Railway Dashboard → Deployments
```

### Просмотр логов:
```bash
# Через Railway CLI
railway logs

# Или через веб-интерфейс
# Railway Dashboard → View Logs
```

---

**Готово! Теперь ваш API работает на Railway и готов к тестированию! 🚀**
