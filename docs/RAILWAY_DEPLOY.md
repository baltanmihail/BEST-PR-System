```powershell
cd "BEST PR System"
git status
git add .
git commit -m "Шо, опять..."
git push origin main
```
# 🚂 Railway Deployment Guide

## Структура сервисов

На Railway должно быть **2 сервиса**:

### 1. Backend сервис (`best-pr-api`)
**Запускает:** API + Telegram бот вместе

**Root Directory:** `backend/`

**Start Command:** `python -m run_with_bot` (или `uvicorn app.main:app --host 0.0.0.0 --port 8080` с запуском бота отдельно)

**Port:** `8080`

**Переменные окружения:**

#### Обязательные:
```
DATABASE_URL=postgresql://...
ENVIRONMENT=production
TELEGRAM_BOT_TOKEN=...
TELEGRAM_ADMIN_IDS=5079636941,другие_id
FRONTEND_URL=https://best-pr-system.up.railway.app
```

#### Google Drive (опционально):
```
GOOGLE_CREDENTIALS_1_JSON={"type":"service_account",...}
GOOGLE_CREDENTIALS_2_JSON={"type":"service_account",...}
GOOGLE_CREDENTIALS_3_JSON={"type":"service_account",...}
GOOGLE_CREDENTIALS_4_JSON={"type":"service_account",...}
GOOGLE_CREDENTIALS_5_JSON={"type":"service_account",...}
GOOGLE_DRIVE_FOLDER_ID=1jCFjvHRh13JaxwxgK36JodhXgQTT18oU
```

#### Другие (опционально):
```
CORS_ORIGINS=https://best-pr-system.up.railway.app
LOG_LEVEL=INFO
API_V1_PREFIX=/api/v1
```

**Важно:**
- Telegram бот запускается **вместе с API** в одном процессе через `run_with_bot.py`
- **Не создавай отдельный сервис для бота** - это вызовет конфликты (`TelegramConflictError`)
- `ENVIRONMENT=production` обязателен - без него бот не запустится

---

### 2. Frontend сервис (`best-pr-system`)
**Запускает:** React приложение

**Root Directory:** `frontend/`

**Build Command:** `npm ci && npm run build`

**Start Command:** `npm run preview` (или `npm run serve` для production)

**Port:** `8080` (или другой, который Railway назначает)

**Переменные окружения:**

#### Обязательные:
```
VITE_API_URL=https://best-pr-api.up.railway.app/api/v1
```

**Важно:**
- Убедись, что `VITE_API_URL` указывает на **backend сервис** (`best-pr-api`)
- После изменения переменных окружения нужно **перезапустить деплой**

---

## Почему не 3 сервиса?

❌ **Неправильно:**
- Backend
- Frontend
- **Telegram Bot** (отдельно)

✅ **Правильно:**
- Backend (API + Bot вместе)
- Frontend

**Причина:** Telegram бот запускается через `run_with_bot.py` в том же процессе, что и API. Если создать отдельный сервис для бота, будет конфликт - два процесса будут пытаться получать обновления от Telegram одновременно.

---

## Настройка Networking

### Backend сервис:
- **Public Networking** ✅
- Домен: `best-pr-api.up.railway.app`
- Доступен из интернета для фронтенда

### Frontend сервис:
- **Public Networking** ✅
- Домен: `best-pr-system.up.railway.app`
- Доступен из интернета для пользователей

---

## Проверка после деплоя

### Backend:
1. Проверь логи - должен быть запущен API и бот
2. Открой `https://best-pr-api.up.railway.app/docs` - должна открыться Swagger документация
3. Открой `https://best-pr-api.up.railway.app/health` - должен вернуть `{"status":"ok"}`

### Frontend:
1. Открой `https://best-pr-system.up.railway.app` - должен открыться сайт
2. Проверь консоль браузера (F12) - не должно быть ошибок подключения к API

### Telegram бот:
1. Отправь `/start` боту в Telegram
2. Бот должен ответить без ошибок
3. Нажми кнопки в боте - они должны работать без ошибок

---

## Если бот не работает

1. **Проверь логи backend сервиса:**
   - Должно быть: `🤖 Starting Telegram bot...`
   - Должно быть: `Run polling for bot @BESTPRSystemBot`
   - **Не должно быть:** `TelegramConflictError`

2. **Проверь переменные окружения:**
   - `ENVIRONMENT=production` установлен?
   - `TELEGRAM_BOT_TOKEN` правильный?
   - Бот запущен только в одном месте?

3. **Проверь, нет ли старых деплоев:**
   - Railway Dashboard → Backend сервис → Deployments
   - Удали старые деплои или останови их

---

## Troubleshooting

### Ошибка: `TelegramConflictError`
**Причина:** Бот запущен в нескольких местах одновременно
**Решение:** Убедись, что бот запущен только в backend сервисе, останови локальный бот или старые деплои

### Ошибка: `ValueError: No valid Google credentials found`
**Причина:** Google credentials не установлены
**Решение:** Добавь `GOOGLE_CREDENTIALS_*_JSON` переменные или убедись, что они правильные

### Ошибка: `403 Forbidden` на фронтенде
**Причина:** `VITE_API_URL` неправильный или CORS не настроен
**Решение:** Проверь `VITE_API_URL` и `CORS_ORIGINS` в backend

---

Всё готово! 🎉
