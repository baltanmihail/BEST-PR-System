# 🚂 Деплой на Railway

## 📋 Подготовка к деплою

### 1. Создание Railway проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo" (если репозиторий уже на GitHub)
   ИЛИ "Empty Project" (если деплоите вручную)

### 2. Подключение репозитория (если через GitHub)

1. Подключите GitHub аккаунт к Railway
2. Выберите репозиторий `BEST PR System`
3. Railway автоматически определит проект

---

## 🗄️ Настройка базы данных

### 1. Добавить PostgreSQL

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway создаст базу данных автоматически

### 2. Получить DATABASE_URL

1. Откройте созданную базу данных
2. Перейдите во вкладку "Variables"
3. Скопируйте значение `DATABASE_URL` (будет что-то вроде: `postgresql://postgres:password@host:port/railway`)

**ВАЖНО**: Для async SQLAlchemy нужно преобразовать URL:
- Замените `postgresql://` на `postgresql+asyncpg://`
- Или используйте переменную окружения `DATABASE_URL` как есть, код автоматически преобразует

---

## ⚙️ Настройка переменных окружения

### В Railway проекте:

1. Откройте ваш сервис (Backend)
2. Перейдите во вкладку "Variables"
3. Добавьте следующие переменные:

#### Обязательные:
```env
# Database (Railway автоматически создаст, но можно переопределить)
DATABASE_URL=postgresql+asyncpg://postgres:password@host:port/railway

# FastAPI
SECRET_KEY=your-very-secret-key-change-this-in-production
API_V1_PREFIX=/api/v1
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-from-botfather
TELEGRAM_ADMIN_IDS=5079636941

# Google APIs
GOOGLE_SHEETS_ID=your-google-sheets-id
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# Для ротации клиентов (как в существующем боте):
GOOGLE_CREDENTIALS_1_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
GOOGLE_CREDENTIALS_2_JSON={...}
# И т.д. для всех credentials

# CORS (укажите ваш домен Railway)
CORS_ORIGINS=https://your-app.railway.app,http://localhost:3000

# Environment
ENVIRONMENT=production
LOG_LEVEL=INFO
```

---

## 🚀 Настройка деплоя

### Вариант 1: Через GitHub (рекомендуется)

1. Railway автоматически определит проект
2. Если нет, создайте `railway.json` в корне проекта (уже создан)

### Вариант 2: Через Railway CLI

1. Установите Railway CLI:
```powershell
# Windows (через npm)
npm install -g @railway/cli

# Или через scoop
scoop install railway
```

2. Войдите в Railway:
```powershell
railway login
```

3. Инициализируйте проект:
```powershell
cd "BEST PR System"
railway init
```

4. Свяжите с существующим проектом или создайте новый

5. Деплой:
```powershell
railway up
```

---

## 📁 Файлы для Railway (уже созданы)

### 1. Procfile
```
web: cd backend && alembic upgrade head && python run.py
```

### 2. railway.json
Уже создан в корне проекта

### 3. nixpacks.toml
Уже создан в корне проекта

---

## 🔄 Применение миграций

### Автоматически при деплое:

Добавлено в `Procfile` и `railway.json`:
```bash
alembic upgrade head && python run.py
```

### Вручную через Railway CLI:

```powershell
railway run alembic upgrade head
```

### Через Railway Dashboard:

1. Откройте ваш сервис
2. Перейдите в "Deployments"
3. Откройте последний деплой
4. Используйте "Shell" для выполнения команд

---

## 🧪 Тестирование деплоя

### 1. Проверка health check

После деплоя проверьте:
```
https://your-app.railway.app/health
```

Должен вернуть: `{"status": "ok"}`

### 2. Проверка документации API

```
https://your-app.railway.app/docs
```

### 3. Проверка корневого endpoint

```
https://your-app.railway.app/
```

---

## 🔧 Настройка домена (опционально)

1. В Railway проекте откройте ваш сервис
2. Перейдите во вкладку "Settings"
3. В разделе "Networking" нажмите "Generate Domain"
4. Или подключите свой домен

---

## 📊 Мониторинг

### Логи

1. В Railway Dashboard откройте ваш сервис
2. Перейдите во вкладку "Deployments"
3. Выберите деплой и смотрите логи

### Метрики

Railway автоматически показывает:
- CPU использование
- Memory использование
- Network traffic

---

## 🐛 Решение проблем

### Проблема: Приложение не запускается

**Проверьте:**
1. Логи в Railway Dashboard
2. Все переменные окружения установлены
3. DATABASE_URL правильный
4. PORT используется из переменной окружения `$PORT`

### Проблема: Ошибки миграций

**Решение:**
```powershell
# Подключитесь через Railway CLI
railway run alembic upgrade head

# Или проверьте логи
railway logs
```

### Проблема: База данных не подключается

**Проверьте:**
1. DATABASE_URL правильный (из Variables базы данных)
2. Для async нужно `postgresql+asyncpg://`
3. База данных создана и доступна

---

## 🔐 Безопасность

### ✅ Что сделать:

1. **Измените SECRET_KEY** на случайную строку:
   ```python
   import secrets
   secrets.token_urlsafe(32)
   ```

2. **Ограничьте CORS_ORIGINS** только вашими доменами

3. **Не коммитьте** `.env` файлы в Git

4. **Используйте** Railway Variables для секретов

---

## 📝 Чеклист перед деплоем

- [ ] Все переменные окружения установлены в Railway
- [ ] DATABASE_URL правильный (postgresql+asyncpg://)
- [ ] SECRET_KEY изменён на случайный
- [ ] TELEGRAM_BOT_TOKEN установлен
- [ ] GOOGLE credentials настроены
- [ ] CORS_ORIGINS указаны правильно
- [ ] Миграции применятся автоматически (в startCommand)
- [ ] Procfile или railway.json создан
- [ ] Код закоммичен в Git
- [ ] Репозиторий подключен к Railway

---

## 🎉 После успешного деплоя

1. Проверьте health endpoint
2. Проверьте документацию API
3. Протестируйте авторизацию
4. Настройте мониторинг (опционально)
5. Настройте автоматический деплой из GitHub (если нужно)

---

## 📞 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
