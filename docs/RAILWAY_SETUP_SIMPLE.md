# 🚀 Простая настройка Railway - ШАГ ЗА ШАГОМ

## 📋 Что нужно сделать (всего 3 шага)

---

## ✅ ШАГ 1: Создать базу данных и получить DATABASE_URL

### Что делать:

1. В Railway проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Подождите, пока Railway создаст базу данных
4. Откройте созданную базу данных
5. Перейдите во вкладку **"Variables"**
6. Найдите `DATABASE_URL` и **скопируйте его**

### ⚠️ ВАЖНО: Измените URL!

Скопированный URL будет начинаться с `postgresql://`

**Замените** `postgresql://` на `postgresql+asyncpg://`

**Пример:**
```
Было:  postgresql://postgres:abc123@containers-us-west-123.railway.app:5432/railway
Стало: postgresql+asyncpg://postgres:abc123@containers-us-west-123.railway.app:5432/railway
```

**Скопируйте изменённый URL** - он понадобится в Шаге 3.

---

## ✅ ШАГ 2: Создать SECRET_KEY

### Что делать:

Откройте PowerShell и выполните:

```powershell
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Скопируйте результат** - это ваш SECRET_KEY (он понадобится в Шаге 3).

**Пример результата:**
```
K8jL3mN9pQ2rS5tU7vW0xY1zA4bC6dE8fG0hI1jK2lM3nO4pQ5rS6tU7vW8xY9z
```

---

## ✅ ШАГ 3: Добавить все переменные в Railway

### Где добавить:

1. В Railway проекте откройте ваш сервис (Backend)
2. Перейдите во вкладку **"Variables"**
3. Нажмите **"+ New Variable"** для каждой переменной

### Список переменных (12 штук):

#### 1. DATABASE_URL
```
Ключ: DATABASE_URL
Значение: (вставьте изменённый URL из Шага 1)
Пример: postgresql+asyncpg://postgres:abc123@host:port/railway
```

#### 2. SECRET_KEY
```
Ключ: SECRET_KEY
Значение: (вставьте ключ из Шага 2)
Пример: K8jL3mN9pQ2rS5tU7vW0xY1zA4bC6dE8fG0hI1jK2lM3nO4pQ5rS6tU7vW8xY9z
```

#### 3. TELEGRAM_BOT_TOKEN
```
Ключ: TELEGRAM_BOT_TOKEN
Значение: 8275006557:AAFomMYi17esDTbsWBUUDHb154pnKxoqyKY
```

#### 4. TELEGRAM_ADMIN_IDS
```
Ключ: TELEGRAM_ADMIN_IDS
Значение: 5079636941
```

#### 5. GOOGLE_SHEETS_ID
```
Ключ: GOOGLE_SHEETS_ID
Значение: 1nNqA_cvUnzPADO3DcwD6ldzHIwnghR7ypKuzec4bk0I
```

#### 6. GOOGLE_CREDENTIALS_1_JSON
```
Ключ: GOOGLE_CREDENTIALS_1_JSON
Значение: (откройте файл BEST Channel Bot/credentials-1.json, скопируйте ВСЁ содержимое)
```

**Как скопировать JSON:**
1. Откройте файл `BEST Channel Bot/credentials-1.json` в блокноте
2. Нажмите Ctrl+A (выделить всё)
3. Нажмите Ctrl+C (скопировать)
4. В Railway вставьте как одну строку (без переносов)

#### 7. GOOGLE_CREDENTIALS_2_JSON
```
Ключ: GOOGLE_CREDENTIALS_2_JSON
Значение: (скопируйте содержимое BEST Channel Bot/credentials-2.json)
```

#### 8. GOOGLE_CREDENTIALS_3_JSON
```
Ключ: GOOGLE_CREDENTIALS_3_JSON
Значение: (скопируйте содержимое BEST Channel Bot/credentials-3.json)
```

#### 9. GOOGLE_CREDENTIALS_4_JSON
```
Ключ: GOOGLE_CREDENTIALS_4_JSON
Значение: (скопируйте содержимое BEST Channel Bot/credentials-4.json)
```

#### 10. GOOGLE_CREDENTIALS_5_JSON
```
Ключ: GOOGLE_CREDENTIALS_5_JSON
Значение: (скопируйте содержимое BEST Channel Bot/credentials-5.json)
```

#### 11. ENVIRONMENT
```
Ключ: ENVIRONMENT
Значение: production
```

#### 12. LOG_LEVEL
```
Ключ: LOG_LEVEL
Значение: INFO
```

---

## ✅ Готово!

После добавления всех 12 переменных:

1. Railway автоматически пересоберёт проект
2. Подождите 2-3 минуты
3. Проверьте логи: Railway Dashboard → Deployments → Logs
4. Откройте ваш домен: `https://your-app.railway.app/health`
5. Должно вернуть: `{"status": "ok"}`

---

## 🆘 Если что-то не работает

### Проблема: Ошибка в логах

1. Откройте Railway Dashboard → Deployments → Logs
2. Найдите ошибку (обычно красным цветом)
3. Проверьте:
   - Все ли 12 переменных добавлены?
   - Правильный ли формат DATABASE_URL? (должен начинаться с `postgresql+asyncpg://`)
   - Правильный ли формат JSON? (должен быть в одну строку)

### Проблема: Не могу скопировать JSON

**Вариант 1: Через Python**
```powershell
python -c "import json; f=open('BEST Channel Bot/credentials-1.json'); print(json.dumps(json.load(f)))"
```

**Вариант 2: Онлайн**
1. Откройте файл в блокноте
2. Скопируйте всё
3. Зайдите на [jsonformatter.org](https://jsonformatter.org/)
4. Вставьте, нажмите "Minify"
5. Скопируйте результат (одна строка)

---

## 📝 Чеклист

- [ ] База данных PostgreSQL создана в Railway
- [ ] DATABASE_URL скопирован и изменён (добавлен `+asyncpg`)
- [ ] SECRET_KEY создан через Python
- [ ] Все 12 переменных добавлены в Railway
- [ ] JSON файлы скопированы правильно (в одну строку)
- [ ] Railway пересобрал проект
- [ ] Health check работает: `/health` возвращает `{"status": "ok"}`

---

## 📚 Подробнее

- **Подробная инструкция**: [RAILWAY_VARIABLES.md](./RAILWAY_VARIABLES.md)
- **Как создать SECRET_KEY**: [GENERATE_SECRET_KEY.md](./GENERATE_SECRET_KEY.md)
- **Общий деплой**: [docs/RAILWAY_DEPLOY.md](./docs/RAILWAY_DEPLOY.md)
