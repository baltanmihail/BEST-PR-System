# 🚀 Быстрый старт для тестирования

## 1. Запуск бэкенда

```powershell
# Перейти в папку бэкенда
cd "BEST PR System/backend"

# Создать виртуальное окружение (если ещё не создано)
python -m venv venv

# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
python run.py
```

**Проверка:**
- Откройте `http://localhost:8000/docs` - должна открыться Swagger документация
- Откройте `http://localhost:8000/health` - должен вернуться `{"status": "ok"}`

## 2. Запуск фронтенда

```powershell
# В новом терминале, перейти в папку фронтенда
cd "BEST PR System/frontend"

# Установить зависимости (если ещё не установлены)
npm install

# Запустить dev сервер
npm run dev
```

**Проверка:**
- Откройте адрес из вывода (обычно `http://localhost:5173`)
- Страница должна загрузиться без ошибок

## 3. Быстрое тестирование через Swagger

1. Откройте `http://localhost:8000/docs`
2. Протестируйте публичные endpoints:
   - `GET /api/v1/public/tasks` - нажмите "Try it out" → "Execute"
   - `GET /api/v1/public/leaderboard`
   - `GET /api/v1/public/stats`
   - `GET /api/v1/activity/feed`
   - `GET /api/v1/gallery`

## 4. Тестирование фронтенда

### Публичные страницы (без авторизации):
- ✅ `/` - Главная страница
- ✅ `/tasks` - Список задач
- ✅ `/leaderboard` - Рейтинг
- ✅ `/stats` - Статистика
- ✅ `/activity` - Лента активности
- ✅ `/gallery` - Галерея результатов
- ✅ `/support` - Поддержка (можно отправить запрос)

### Страницы с авторизацией:
- 🔐 `/notifications` - Уведомления (нужен токен)

## 5. Проверка консоли

Откройте DevTools (F12) и проверьте:
- **Console** - нет ошибок
- **Network** - все запросы возвращают 200/201

## 6. Типичные проблемы

### Бэкенд не запускается:
- Проверьте, что Python 3.11+ установлен
- Проверьте, что все зависимости установлены
- Проверьте, что порт 8000 свободен

### Фронтенд не подключается к API:
- Проверьте, что бэкенд запущен
- Проверьте файл `.env.local` в `frontend/`
- Проверьте консоль браузера на ошибки CORS

### Ошибки миграций:
- Убедитесь, что база данных создана
- Проверьте `DATABASE_URL` в переменных окружения
- Запустите миграции вручную: `cd backend; python -m alembic upgrade head`

---

**Готово! Начинайте тестировать! 🎉**
