# 🔧 Исправление ошибки деплоя фронтенда

## Проблема

```
npm error Missing: serve@14.2.5 from lock file
```

Это происходит потому, что `package-lock.json` не синхронизирован с `package.json` после добавления `serve`.

## Решение

### Вариант 1: Обновить package-lock.json локально (рекомендуется)

```powershell
cd "BEST PR System/frontend"
npm install
```

Это обновит `package-lock.json` с новой зависимостью `serve`.

Затем закоммитьте изменения:
```powershell
git add frontend/package-lock.json
git commit -m "Update package-lock.json with serve dependency"
git push
```

### Вариант 2: Использовать npm install вместо npm ci

Измените `railway.json` в папке `frontend`:

```json
{
  "build": {
    "buildCommand": "npm install && npm run build"
  }
}
```

Но лучше использовать Вариант 1 - это правильный подход.

---

## После исправления

Railway автоматически пересоберёт проект и деплой должен пройти успешно.
