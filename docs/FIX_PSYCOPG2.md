# 🔧 Решение проблемы с psycopg2-binary

## ⚠️ Проблема

При установке зависимостей возникает ошибка:
```
Error: pg_config executable not found.
```

Это происходит потому что `psycopg2-binary` пытается собраться из исходников на Windows.

## ✅ Решение

### Вариант 1: Пропустить psycopg2-binary (рекомендуется для разработки)

`psycopg2-binary` нужен только для **синхронных** миграций. Мы используем **async** драйверы, поэтому он не критичен.

**Установите зависимости без psycopg2-binary:**

```powershell
# Активируйте venv
.\venv\Scripts\Activate.ps1

# Установите основные пакеты
pip install fastapi uvicorn sqlalchemy alembic asyncpg aiosqlite python-jose python-dotenv pydantic pydantic-settings python-multipart aiohttp aiofiles python-dateutil pytz
```

Для разработки используйте SQLite (уже настроено в `.env`):
```env
DATABASE_URL=sqlite:///./best_pr_system.db
```

### Вариант 2: Установить psycopg2-binary вручную

Если всё-таки нужен psycopg2-binary:

1. **Установите PostgreSQL** (если ещё не установлен)
2. **Добавьте PostgreSQL в PATH**
3. **Или используйте предкомпилированный wheel:**

```powershell
pip install psycopg2-binary --only-binary :all:
```

### Вариант 3: Использовать только async драйверы

Мы используем:
- `asyncpg` для PostgreSQL (async)
- `aiosqlite` для SQLite (async)

Этих драйверов достаточно для работы. `psycopg2-binary` нужен только если вы хотите использовать синхронные миграции, но мы используем async миграции через Alembic.

---

## 🎯 Рекомендация

**Для разработки**: Используйте SQLite и не устанавливайте psycopg2-binary. Всё будет работать!

**Для продакшена на Railway**: Railway автоматически установит все зависимости, включая psycopg2-binary, если он нужен.

---

## ✅ Проверка

После установки проверьте:

```powershell
# Проверить установленные пакеты
pip list | Select-String "sqlalchemy|alembic|asyncpg|aiosqlite"

# Должны быть:
# - sqlalchemy
# - alembic
# - asyncpg (для PostgreSQL)
# - aiosqlite (для SQLite)
```

Если все эти пакеты установлены - всё готово к работе! 🎉
