# 🚀 Быстрый старт: Telegram бот на Railway

## ⚡ Самый простой способ (5 минут)

### 1. Получите токен бота
- Откройте [@BotFather](https://t.me/BotFather)
- `/newbot` → следуйте инструкциям
- Скопируйте токен

### 2. Создайте новый сервис в Railway

1. Railway Dashboard → Ваш проект → **"+ New"** → **"Service"**
2. Выберите **"GitHub Repo"** → тот же репозиторий

### 3. Настройте переменные

В новом сервисе → **"Variables"** → добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
API_URL=https://best-pr-system.up.railway.app
DATABASE_URL=postgresql+asyncpg://... (скопируйте из PostgreSQL сервиса)
```

**Как скопировать DATABASE_URL:**
1. Railway Dashboard → PostgreSQL сервис → **"Variables"**
2. Скопируйте `DATABASE_URL`
3. Замените `postgresql://` на `postgresql+asyncpg://`
4. Вставьте в переменные бота

### 4. Настройте Start Command

1. Сервис бота → **"Settings"** → **"Deploy"**
2. **"Start Command"**: `cd backend && python bot/main.py`
3. Сохраните

### 5. Деплой

Railway автоматически задеплоит. Дождитесь завершения (~2-3 минуты).

### 6. Проверка

1. Откройте **"View Logs"** в сервисе бота
2. Должно быть: `🤖 Telegram бот запускается...`
3. Откройте бота в Telegram → `/start`
4. Должен ответить!

---

## ✅ Готово!

Теперь бот работает. Команды:
- `/start` - регистрация
- `/stats` - статистика
- `/tasks` - задачи
- `/leaderboard` - рейтинг
- `/equipment` - оборудование
- `/notifications` - уведомления

---

## 🐛 Если не работает

1. **Проверьте логи** → Railway → Сервис бота → "View Logs"
2. **Проверьте переменные** → все ли установлены правильно
3. **Проверьте токен** → правильный ли он в @BotFather
4. **Перезапустите сервис** → "Deploy" → "Redeploy"

---

**Подробная инструкция:** `docs/TELEGRAM_BOT_DEPLOY.md`
