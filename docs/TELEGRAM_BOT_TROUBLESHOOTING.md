# 🐛 Решение проблем с Telegram ботом на Railway

## Проблема: Start Command возвращается обратно

### Симптомы:
- Вы меняете Start Command в Railway Dashboard
- После деплоя команда возвращается к `cd backend && python run.py`
- Бот не запускается, запускается только API

### Причина:
Railway автоматически использует `Procfile`, который перезаписывает Start Command.

---

## ✅ Решение 1: Использовать Override (рекомендуется)

### Шаги:

1. **Откройте сервис бота** в Railway Dashboard
2. Перейдите в **"Settings"** → **"Deploy"**
3. Найдите **"Start Command"**
4. **Включите переключатель "Override"** или **"Custom Start Command"**
5. Введите: `cd backend && python bot/main.py`
6. **Сохраните**

### Проверка:
После деплоя в логах должно быть:
```
🤖 Telegram бот запускается...
```

А НЕ:
```
🚀 Starting server on 0.0.0.0:8080
```

---

## ✅ Решение 2: Изменить Procfile для бота

Если Railway всё равно использует Procfile, можно временно изменить его:

1. **ВНИМАНИЕ:** Это повлияет на API сервис!
2. Измените `Procfile`:
   ```
   web: cd backend && python -m alembic upgrade head && python run.py
   worker: cd backend && python bot/main.py
   ```
3. В Railway Dashboard → Сервис бота → **"Settings"** → **"Service Type"**
4. Измените на **"Worker"** (вместо "Web")
5. Railway будет использовать `worker:` из Procfile

---

## ✅ Решение 3: Использовать run_with_bot.py (вместе с API)

Если хотите запустить бота в том же сервисе, что и API:

1. Измените `Procfile`:
   ```
   web: cd backend && python -m alembic upgrade head && python run_with_bot.py
   ```
2. Добавьте переменную окружения в API сервис:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   ```
3. Перезапустите API сервис

⚠️ **Не рекомендуется для production**, но работает.

---

## ✅ Решение 4: Создать отдельный Procfile для бота

1. Создайте файл `Procfile.bot` в корне проекта (уже создан)
2. В Railway Dashboard → Сервис бота → **"Settings"**
3. Найдите **"Build Command"** или **"Nixpacks Config"**
4. Укажите использовать `Procfile.bot` вместо `Procfile`

Или переименуйте `Procfile` в `Procfile.api` и создайте новый `Procfile`:
```
worker: cd backend && python bot/main.py
```

Но тогда нужно будет указать `Procfile.api` для API сервиса.

---

## 🔍 Как проверить, что бот запустился

### Правильные логи:
```
🤖 Telegram бот запускается...
INFO: Application startup complete
```

### Неправильные логи (запускается только API):
```
🚀 Starting server on 0.0.0.0:8080
INFO: Uvicorn running on http://0.0.0.0:8080
```

---

## 🎯 Рекомендуемое решение

**Для отдельного сервиса бота:**

1. Создайте новый сервис
2. В **"Settings"** → **"Deploy"** → **"Start Command"**
3. **Включите "Override"**
4. Введите: `cd backend && python bot/main.py`
5. Сохраните и задеплойте

Если это не работает:
- Проверьте, что сервис создан как отдельный (не тот же, что API)
- Убедитесь, что переменные окружения установлены
- Проверьте логи на ошибки

---

## 📝 Чеклист

- [ ] Создан отдельный сервис для бота
- [ ] Start Command установлен: `cd backend && python bot/main.py`
- [ ] Переключатель "Override" включён
- [ ] Установлены переменные: `TELEGRAM_BOT_TOKEN`, `API_URL`, `DATABASE_URL`
- [ ] В логах есть: `🤖 Telegram бот запускается...`
- [ ] Бот отвечает на `/start` в Telegram

---

**Если ничего не помогает:**
Используйте `run_with_bot.py` - запустит и API, и бота вместе (менее надёжно, но точно работает).
