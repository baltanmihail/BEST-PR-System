# 🔧 Исправление ошибки подключения к базе данных

## ❌ Ошибка

```
ValueError: invalid literal for int() with base 10: 'port'
```

или

```
Ошибка подключения к базе данных через asyncpg
```

## 🔍 Причина

В переменной `DATABASE_URL` в Railway указан **шаблон URL с переменными** (`${{...}}`) или **пример URL** вместо **реального URL** из вашей базы данных.

**Неправильно (шаблон с переменными):**
```
postgresql://${{PGUSER}}:${{POSTGRES_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:5432/${{PGDATABASE}}
```
Это шаблон Railway, а не реальный URL!

**Неправильно (пример):**
```
postgresql+asyncpg://postgres:password@host:port/railway
                                                    ^^^^
                                                    Это слово, а не число!
```

**Правильно (реальный URL):**
```
postgresql+asyncpg://postgres:abc123@containers-us-west-123.railway.app:5432/railway
                                                                        ^^^^
                                                                        Это число (5432)!
```

---

## ✅ Решение (ШАГ ЗА ШАГОМ)

### Шаг 1: Получить реальный DATABASE_URL

**⚠️ ВАЖНО**: НЕ копируйте шаблон с `${{...}}`! Нужен реальный URL!

**Способ 1: Через Connection Info (рекомендуется)**

1. Откройте [Railway Dashboard](https://railway.app)
2. Откройте ваш проект
3. Найдите вашу **PostgreSQL базу данных**
4. Нажмите на неё
5. Перейдите во вкладку **"Connect"** или **"Connection Info"**
6. Там будет показан **реальный URL** с реальными значениями
7. **Скопируйте его** - будет что-то вроде:
   ```
   postgresql://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
   ```

**Способ 2: Через Variables (если есть реальный URL)**

1. В Railway Dashboard откройте вашу базу данных
2. Перейдите во вкладку **"Variables"**
3. Найдите `DATABASE_URL`
4. Railway может показывать **два варианта**:
   - Шаблон: `postgresql://${{PGUSER}}:...` - **НЕ используйте!**
   - Реальный URL: `postgresql://postgres:abc123@host:5432/railway` - **используйте этот!**
5. Если видите только шаблон, используйте Способ 1

### Шаг 2: Изменить URL для async

**ВАЖНО**: Замените `postgresql://` на `postgresql+asyncpg://`

**Пример:**
```
Было:  postgresql://postgres:abc123@host.railway.app:5432/railway
Стало: postgresql+asyncpg://postgres:abc123@host.railway.app:5432/railway
```

### Шаг 3: Обновить переменную в Railway

1. В Railway Dashboard откройте ваш **сервис (Backend)** (не базу данных!)
2. Перейдите во вкладку **"Variables"**
3. Найдите переменную `DATABASE_URL`
4. Нажмите на неё (или кнопку редактирования)
5. **Замените** значение на изменённый URL из Шага 2
6. Нажмите **"Save"** или **"Update"**

### Шаг 4: Проверить результат

1. Railway автоматически пересоберёт проект
2. Подождите 2-3 минуты
3. Проверьте логи: Railway Dashboard → Deployments → Logs
4. Не должно быть ошибок подключения

---

## 🔍 Как проверить, что URL правильный

**Правильный URL должен:**
- ✅ Начинаться с `postgresql+asyncpg://`
- ✅ Содержать `@` (user:password@host)
- ✅ Содержать `:` после хоста (host:port)
- ✅ Порт должен быть **числом** (например, `5432`), а не словом `port`
- ✅ Заканчиваться на `/railway` или `/database_name`

**Неправильный URL содержит:**
- ❌ Слово `port` вместо числа
- ❌ Слово `host` вместо реального хоста
- ❌ Слово `password` вместо реального пароля

---

## 📝 Примеры

### ✅ ПРАВИЛЬНО:
```
postgresql+asyncpg://postgres:abc123def456@containers-us-west-123.railway.app:5432/railway
```

### ❌ НЕПРАВИЛЬНО:
```
postgresql+asyncpg://postgres:password@host:port/railway
```

---

## 🆘 Если всё ещё не работает

1. **Проверьте логи** в Railway Dashboard → Deployments → Logs
2. **Убедитесь**, что скопировали URL из **базы данных**, а не из примера
3. **Убедитесь**, что заменили `postgresql://` на `postgresql+asyncpg://`
4. **Убедитесь**, что порт - это число (5432), а не слово (port)

---

## 💡 Важно

**НИКОГДА не используйте примеры URL из документации!** Всегда копируйте реальный URL из Railway Variables вашей базы данных.
