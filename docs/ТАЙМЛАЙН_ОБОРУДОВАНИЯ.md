# Таймлайн занятости оборудования

## ✅ Что реализовано

### 1. Отдельный таймлайн занятости оборудования

**Сервис:** `EquipmentTimelineSyncService`
- Создаёт отдельную таблицу "BEST PR System - Таймлайн занятости оборудования" в папке Equipment
- Аналогично таймлайну задач, но для оборудования
- Отображает занятость оборудования по датам

**Структура таблицы:**
- Лист "Общий" - все заявки на оборудование
- Заголовки: Оборудование | Дата1 | Дата2 | ...
- В ячейках: информация о заявках (пользователь, статус, просрочка)

### 2. Автоматический поиск таблицы оборудования

**Обновлён `EquipmentSheetsSync`:**
- Автоматически ищет таблицу оборудования в папке Equipment
- Поддерживает несколько вариантов названий:
  - "Учёт оборудки"
  - "Учёт оборудования"
  - "Вся оборудка"
  - "Оборудование"
- Fallback на старый ID, если таблица ещё не перенесена

### 3. API Endpoints

**Новые endpoints:**
```http
# Синхронизация таймлайна занятости с Google Sheets
POST /api/v1/equipment/timeline/sync?month=1&year=2025&statuses=pending,approved

# Получение таймлайна занятости (для отображения)
GET /api/v1/equipment/timeline?month=1&year=2025&statuses=pending,approved
```

**Существующие endpoints:**
- `GET /api/v1/equipment/{equipment_id}/booked-dates` - занятые даты для конкретного оборудования
- `POST /api/v1/equipment/sync/from-sheets` - синхронизация оборудования из Sheets

### 4. Упрощённая форма заявки

**Что убрано:**
- ❌ ФИО (берётся из профиля пользователя)
- ❌ Согласие на обработку данных (уже есть при регистрации)
- ❌ Дополнительные поля, которые уже есть в профиле

**Что осталось:**
- ✅ Выбор оборудования
- ✅ Даты выдачи и возврата
- ✅ Связь с задачей (опционально)
- ✅ Комментарий (опционально)

## 📋 Структура данных

### Таймлайн занятости

**Формат ответа API:**
```json
{
  "month": 1,
  "year": 2025,
  "timeline": [
    {
      "date": "2025-01-01",
      "requests": [
        {
          "request_id": "uuid",
          "equipment_name": "Камера Sony A7III",
          "equipment_id": "uuid",
          "user_name": "Иван Иванов",
          "status": "approved",
          "is_overdue": false
        }
      ]
    }
  ],
  "total_requests": 10
}
```

### Google Sheets таблица

**Лист "Общий":**
- Строка 1: Заголовки (Оборудование | 01.01 | 02.01 | ...)
- Строки 2+: Название оборудования | Ячейки с заявками

**Формат ячеек:**
- `⏳ Имя Пользователя` - заявка на рассмотрении
- `✅ Имя Пользователя` - заявка одобрена
- `📦 Имя Пользователя` - оборудование выдано
- `⚠️ Просрочено` - дедлайн просрочен
- `📅 Возврат` - дата возврата

## 🔄 Поток работы

### Создание заявки:
1. Пользователь выбирает оборудование
2. Выбирает даты выдачи и возврата
3. Опционально связывает с задачей
4. Добавляет комментарий
5. Заявка создаётся в БД (ФИО берётся из профиля)
6. Автоматически синхронизируется в Google Sheets

### Синхронизация таймлайна:
1. Вызывается `POST /api/v1/equipment/timeline/sync`
2. Читаются все заявки за указанный месяц
3. Группируются по оборудованию
4. Записываются в Google Sheets таблицу
5. Применяется форматирование (цвета, фиксация строк)

### Отображение таймлайна:
1. Вызывается `GET /api/v1/equipment/timeline`
2. Возвращаются данные для календаря
3. Фронтенд отображает занятость по датам
4. Показываются статусы заявок, просрочки

## ⚙️ Настройка

### Переменные окружения:
```bash
# ID таблицы с оборудованием (опционально, будет найден автоматически)
GOOGLE_EQUIPMENT_SHEETS_ID=

# ID таблицы с таймлайном занятости (создаётся автоматически)
GOOGLE_EQUIPMENT_TIMELINE_SHEETS_ID=
```

## 🚀 Что дальше (опционально)

1. **Визуализация на фронтенде:**
   - Календарь с цветами для занятости
   - Фильтры по оборудованию, статусам
   - Клик по ячейке → детали заявки

2. **Улучшения:**
   - Автоматическая синхронизация таймлайна (каждые 5 минут)
   - Уведомления о просроченных заявках
   - Статистика использования оборудования

3. **Интеграция в бот:**
   - Команда `/equipment_timeline` для просмотра занятости
   - Упрощённый процесс подачи заявки
