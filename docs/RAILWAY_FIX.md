# 🔧 Исправление ошибки деплоя на Railway

## ❌ Проблема

Ошибка при деплое:
```
error: undefined variable 'pip'
```

## ✅ Решение

### Что было исправлено:

1. **Удалён `nixpacks.toml`** - он использовал неправильный синтаксис
2. **Обновлён `railway.json`** - добавлено обновление pip перед установкой
3. **Создан `runtime.txt`** - указывает версию Python для Railway

### Файлы для Railway:

- ✅ `Procfile` - команда запуска
- ✅ `railway.json` - конфигурация Railway
- ✅ `runtime.txt` - версия Python (опционально)
- ❌ `nixpacks.toml` - удалён (не нужен)

---

## 🚀 Что делать дальше

### 1. Закоммитьте изменения:

```powershell
cd "BEST PR System"
git add .
git commit -m "fix: Remove nixpacks.toml and fix Railway deployment config"
git push
```

### 2. Railway автоматически пересоберёт проект

Railway использует стандартный Python buildpack, который:
- Автоматически определит Python проект
- Установит зависимости из `backend/requirements.txt`
- Запустит команду из `Procfile` или `railway.json`

---

## ⚙️ Настройка переменных окружения в Railway

**ВАЖНО**: Убедитесь, что в Railway установлены все переменные окружения!

1. Откройте ваш проект в Railway
2. Перейдите в ваш сервис (Backend)
3. Откройте вкладку "Variables"
4. Добавьте все необходимые переменные:

```env
# Database (Railway автоматически создаст для PostgreSQL)
DATABASE_URL=postgresql+asyncpg://postgres:password@host:port/railway

# FastAPI
SECRET_KEY=your-very-secret-key-change-this
API_V1_PREFIX=/api/v1
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Telegram
TELEGRAM_BOT_TOKEN=your-token
TELEGRAM_ADMIN_IDS=5079636941

# Environment
ENVIRONMENT=production
LOG_LEVEL=INFO
PORT=${{PORT}}  # Railway автоматически установит
```

---

## 📝 Структура для Railway

```
BEST PR System/
├── backend/              # Backend код
│   ├── app/              # Приложение
│   ├── requirements.txt  # Зависимости
│   ├── run.py            # Скрипт запуска
│   └── alembic/          # Миграции
├── Procfile              # Команда запуска (Railway использует это)
├── railway.json          # Конфигурация Railway
└── runtime.txt           # Версия Python (опционально)
```

---

## 🔍 Проверка деплоя

После деплоя проверьте:

1. **Health check**: `https://your-app.railway.app/health`
2. **API docs**: `https://your-app.railway.app/docs`
3. **Логи**: Railway Dashboard → Deployments → Logs

---

## 🐛 Если всё ещё не работает

### Проверьте логи в Railway:

1. Откройте Railway Dashboard
2. Перейдите в ваш сервис
3. Откройте вкладку "Deployments"
4. Выберите последний деплой
5. Смотрите логи

### Частые проблемы:

1. **Нет переменных окружения** - добавьте все необходимые
2. **Неправильный DATABASE_URL** - используйте из Variables базы данных
3. **Ошибки миграций** - проверьте логи миграций
4. **Порт не указан** - Railway автоматически устанавливает `$PORT`

---

## 📞 Полезные ссылки

- [Railway Python Guide](https://docs.railway.app/guides/python)
- [Railway Environment Variables](https://docs.railway.app/develop/variables)
