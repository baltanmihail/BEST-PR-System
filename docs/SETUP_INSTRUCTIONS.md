# 🚀 Инструкции по настройке и запуску

## 📋 Быстрая установка

### 1. Переход в правильную директорию

```powershell
# Перейти в папку проекта
cd "BEST PR System\backend"
```

### 2. Создание виртуального окружения

```powershell
# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1
```

Если возникает ошибка с политикой выполнения скриптов:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 3. Установка зависимостей

```powershell
# Сначала обновить pip
python -m pip install --upgrade pip

# Установить все зависимости
pip install -r requirements.txt
```

**Если возникает ошибка с psycopg2-binary:**
- Это нормально для Windows без PostgreSQL
- `psycopg2-binary` нужен только для синхронных миграций
- Мы используем async, поэтому можно пропустить
- Для разработки используйте SQLite (уже в requirements.txt)

### 4. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
# Database
DATABASE_URL=sqlite:///./best_pr_system.db

# FastAPI
SECRET_KEY=your-secret-key-here-change-in-production
API_V1_PREFIX=/api/v1
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ADMIN_IDS=5079636941  # Через запятую

# Google (пока можно оставить пустым)
GOOGLE_SHEETS_ID=
GOOGLE_DRIVE_FOLDER_ID=

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO
```

### 5. Создание миграций

```powershell
# Создать начальную миграцию
alembic revision --autogenerate -m "Initial migration"

# Применить миграции
alembic upgrade head
```

### 6. Запуск сервера

```powershell
# Запустить FastAPI сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен по адресу: http://localhost:8000
Документация API: http://localhost:8000/docs

---

## 🔧 Решение проблем

### Проблема: "alembic не распознан"
**Решение**: Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### Проблема: "uvicorn не распознан"
**Решение**: То же самое - активируйте venv и установите зависимости.

### Проблема: Ошибка с psycopg2-binary
**Решение**: 
- Это нормально для Windows без установленного PostgreSQL
- Мы используем async драйверы (asyncpg, aiosqlite)
- Для разработки используйте SQLite
- `psycopg2-binary` можно установить позже, если понадобится

### Проблема: Ошибки с путями
**Решение**: Используйте полные пути или убедитесь, что находитесь в правильной директории:
```powershell
cd "C:\Users\click\OneDrive\Рабочий стол\МГТУ\Python Projects\BESTMoscowBot\BEST PR System\backend"
```

---

## 📝 Проверка установки

После установки проверьте:

```powershell
# Проверить Python
python --version  # Должно быть 3.11+

# Проверить установленные пакеты
pip list | Select-String "fastapi|alembic|sqlalchemy"

# Проверить структуру проекта
ls app\models\
```

---

## ✅ Готово!

Если всё установлено правильно, вы сможете:
- ✅ Запустить сервер: `uvicorn app.main:app --reload`
- ✅ Открыть документацию: http://localhost:8000/docs
- ✅ Создать миграции: `alembic revision --autogenerate -m "message"`
