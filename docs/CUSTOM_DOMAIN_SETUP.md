# 🌐 Настройка кастомного домена (без .up.railway.app)

## Цель

Сделать красивый домен типа **`best-pr-system.ru`** или **`bestpr.moscow`** вместо `best-pr-system.up.railway.app`.

---

## Вариант 1: Купить свой домен (рекомендуется для продакшена)

### Шаг 1: Купить домен

**Регистраторы доменов (работают из России)**:
- **REG.RU** — `.ru`, `.moscow`, `.рф`, `.com` (от 199₽/год)
- **Timeweb** — `.ru`, `.com`, `.net` (от 199₽/год)
- **Beget** — `.ru`, `.com`, `.рф` (от 249₽/год)

**Рекомендуемые варианты**:
- `best-pr-system.ru` — классический
- `bestpr.moscow` — креативный (подчёркивает московское отделение)
- `best-moscow.ru` — короткий и понятный

### Шаг 2: Настроить DNS

После покупки домена:

1. Зайди в панель управления доменом (у регистратора)
2. Найди раздел **DNS записи** или **Управление DNS**
3. Добавь следующие записи:

**Для корневого домена** (например, `best-pr-system.ru`):
```
Type: CNAME
Name: @ (или оставь пустым)
Value: best-pr-frontend-production-xxxx.up.railway.app
TTL: 3600
```

**Для поддомена API** (опционально, `api.best-pr-system.ru`):
```
Type: CNAME
Name: api
Value: best-pr-system.up.railway.app
TTL: 3600
```

### Шаг 3: Настроить домен на Railway

#### Frontend сервис:
1. Railway → Frontend Service → **Settings** → **Networking**
2. **Custom Domain** → **Add Custom Domain**
3. Введи: `best-pr-system.ru` (или твой домен)
4. Railway попросит подтвердить — следуй инструкциям
5. Жди до 24 часов (обычно 5-15 минут) пока DNS обновится

#### Backend сервис (если используешь поддомен):
1. Railway → Backend Service → **Settings** → **Networking**
2. **Custom Domain** → **Add Custom Domain**
3. Введи: `api.best-pr-system.ru`
4. Подтверди

### Шаг 4: Обновить переменные окружения

**Backend**:
```bash
FRONTEND_URL=https://best-pr-system.ru
CORS_ORIGINS=https://best-pr-system.ru,https://api.best-pr-system.ru
```

**Frontend**:
```bash
VITE_API_URL=https://api.best-pr-system.ru/api/v1
# или если API на том же домене:
VITE_API_URL=https://best-pr-system.ru/api/v1
```

---

## Вариант 2: Использовать Railway домены (бесплатно, но с .up.railway.app)

Если не хочешь покупать домен, можно использовать Railway домены:

### Красивые имена на Railway:

**Frontend**:
```
best-pr-system.up.railway.app
```

**Backend**:
```
best-pr-api.up.railway.app
```

### Как получить нужное имя:

1. **Frontend Service** → **Settings** → **Networking** → **Public Networking**
2. Нажми **Generate Domain**
3. Railway предложит случайное имя — **удали его**
4. Нажми **Generate Domain** снова и **быстро** введи нужное имя (например, `best-pr-system`)
5. Если имя свободно — Railway даст тебе `best-pr-system.up.railway.app`

**Примечание**: Популярные имена (как `best-pr-system`) могут быть заняты. Попробуй варианты:
- `best-moscow-pr`
- `bestpr-moscow`
- `best-pr-2025`

---

## Вариант 3: Использовать бесплатные домены

### Freenom (бесплатные домены `.tk`, `.ml`, `.ga`, `.cf`, `.gq`):

⚠️ **Минусы**:
- Не надёжны (могут отозвать в любой момент)
- Низкий авторитет у поисковиков
- Могут быть заблокированы в некоторых странах

**Не рекомендуется для серьёзного проекта.**

---

## Вариант 4: Поддомен от университета (если доступно)

Если МГТУ предоставляет поддомены студентам:
- `best-pr.bmstu.ru`
- `bestpr.students.bmstu.ru`

Обратись в IT-отдел университета.

---

## Рекомендация

### Для тестирования (сейчас):
Используй **Railway домены** (Вариант 2):
- Frontend: `best-pr-moscow.up.railway.app` (или что доступно)
- Backend: `best-pr-api.up.railway.app`

### Для продакшена (потом):
Купи домен (Вариант 1):
- `bestpr.moscow` — 500₽/год
- `best-moscow.ru` — 199₽/год

---

## Быстрая инструкция (по пунктам)

### Сейчас (бесплатно, Railway):

1. **Frontend Service** → Settings → Networking → Generate Domain
2. Попробуй ввести: `best-pr-moscow` → получишь `best-pr-moscow.up.railway.app`
3. **Backend**: переменная `FRONTEND_URL=https://best-pr-moscow.up.railway.app`
4. **Frontend**: переменная `VITE_API_URL=https://best-pr-system.up.railway.app/api/v1`

### Потом (свой домен):

1. Купи домен на **REG.RU** (например, `bestpr.moscow`)
2. В DNS добавь: `CNAME @ → best-pr-frontend.up.railway.app`
3. В Railway → Frontend → Custom Domain → `bestpr.moscow`
4. Обнови переменные: `FRONTEND_URL=https://bestpr.moscow`

---

## Проверка

После настройки:
- Открой свой домен → должен быть **React фронтенд** (не JSON)
- В боте `/start` → кнопка "Зарегистрироваться" → открывается **сайт**
- Всё работает!
