[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "python311Packages.setuptools",
  "python311Packages.wheel",
  "libjpeg",
  "zlib",
  "libtiff",
  "freetype",
  "lcms2",
  "libwebp",
  "openjpeg",
  "libimagequant"
]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel || python -m ensurepip --upgrade --default-pip",
  "pip install --no-cache-dir -r backend/requirements.txt || python -m pip install --no-cache-dir -r backend/requirements.txt"
]

[phases.build]
cmds = [
  "python -c 'import qrcode; from PIL import Image; print(\"✅ QR code dependencies OK\")' || echo '⚠️ QR code check failed - will retry during runtime'"
]
